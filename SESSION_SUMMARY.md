# ComChat 開発セッションサマリー

**最終更新**: 2026-07-14（**セッション56＝「ホスト誤退出」バグ修正＋ファイルD&D受付**（コミット 92a20a0=fix / ec6a7df=feat・app.js v132 / style.css v69・APP_VERSION **v4.19β**・**push＆デプロイ済み・実機確認待ち**）。冒頭に**v4.18β（カメラオフ画像＋トリミングUI）はユーザー実機確認OK**→`v4.18-stable`タグを b497c97 に付与・push済み。**正式版宣言はユーザー指示により保留中**。①**ホスト誤退出バグ（ユーザー報告：M4 MBA=ホスト在室のままなのにiPhone SE3に「ホストが退出しました」が出てウェルカム画面へ・再発性あり）**＝根本原因はクライアントがホストへのデータ接続のclose/errorを**無条件で「ホスト退出」と断定して即hangup**していたこと（自分側要因＝iPhone画面ロック・Safariバックグラウンド化・Wi-Fi瞬断でも同じcloseが発火し区別不能）。修正＝close/error時は`cleanupPeer(quietReconnect)`→**猶予付き再接続 startHostReconnect()**（最大3回=HOST_RECONNECT_MAX_ATTEMPTSフィールド・シグナリング未復旧は_waitForSignalingで500ms×最大8秒待機・満員/ロックは再試行無意味なので即断・全滅時のみhangup＋理由別メッセージ「回復できませんでした/満員/ロック」）。ホスト側の同一ID静か受け入れ（セッション53）が受け皿になり、成功時は状態同期・peer-list経由のメッシュ復旧・メディアコールも既存経路で自動回復。**本物のホスト明示退出はroom-closedが先に届いて従来どおり即終了**（この経路は無変更）。visibilitychangeにゾンビ接続点検も追加（closeイベント未発火のまま死んだ接続への保険。failed/closedのみ対象・自己回復しうる'disconnected'は対象外）。hangupで_hostReconnectingを確実にリセット。**検証＝ブラウザ2タブ×実PeerJSクラウドで実ルームを作り3シナリオ**：(A)room-closedなしの生close（スリープ復帰相当）→「再接続しています…」→自動復旧・通話継続・ホスト側もconn/call/タイル完全復旧（**修正前は即誤退室**）(B)ホスト正規退室→room-closedで即終了・再接続起動なし (C)ホストpeer.destroy()（クラッシュ相当・試行1回に制限）→トースト履歴「ホストとの接続が切れました。再接続しています…」→「ホストとの接続を回復できませんでした。ルームは終了しました」→ウェルカム画面。②**D&D**＝チャット欄（.chat-container）へのファイルドロップ→既存sendFileへ、カメラオフ画像パネルへの画像ドロップ→startCameraOffImageEdit（調整エディタ）へ。**入口を増やすだけでプロトコル・検証は無変更**。documentレベルdragover/drop preventDefaultで「誤ドロップでファイルがページとして開かれ通話が吹き飛ぶ」事故も防止。dragenter/leaveのカウンタ方式ハイライト・送信中は理由トースト・複数ファイルは先頭のみ＋案内・非画像は拒否トースト。検証8項目全PASS（カウンタ/単一送信/送信中拒否/複数先頭のみ/ファイルなし無視/非画像拒否/画像でエディタ起動/ページ全体preventDefault）。node --check OK・新規コンソールエラーゼロ。**実機確認のポイント**＝①iPhoneでルーム入室→スリープや他アプリで数十秒放置→復帰して画面タップ→「再接続しました」が出て通話が続くか（従来の誤「ホストが退出しました」が出ないか）②ホストが本当に退室した時は即座に従来メッセージで終了するか③Macでチャット欄にファイルをD&D→送信、カメラオフ画像パネルに画像をD&D→調整エディタ④ドロップゾーン外に落としても何も起きないか。**追記（2026-07-15）：実機確認全項目OK→`v4.19-stable`を1cfd3cdに付与・push済み**。D&D・誤退室バグとも問題なし。特に機内モードテスト（機内モードON→10秒→OFF）で「再接続しました」→通話復帰を実機確認＝修正前なら誤って強制退室になっていた状況からの自動復旧を確認できた（「再接続しています…」トーストは切断直後＝ユーザーがコントロールセンター操作中に出て数秒で消えるため見えないのが正常）。数十秒程度のスリープでは接続自体が生き残り再接続不要（トーストなし）だったのも設計どおり。**次回**＝保留中の正式版宣言（β外し）の相談再開）

**（旧）最終更新（セッション55）**: 2026-07-14（**セッション55＝カメラオフ画像のトリミング・拡大縮小調整UIを追加**（コミット c84b091・app.js v130 / style.css v68・APP_VERSION **v4.18β**のまま・**push＆デプロイ済み・実機確認待ち**）。冒頭に**セッション53の監査9件はユーザーが全件実機確認済みOKと報告**→v4.17分の宿題は完全クローズ（stableタグはv4.17-stableとして付与済みのため追加作業なし）。本題＝セッション54のカメラオフ画像はアップロード画像をそのまま保存するためobject-fit:coverの機械的な中央切り出しになり構図を調整できなかった→アップロード直後に**16:9の調整フレーム（200px・タイル表示と同じcover基準）**を表示し、ドラッグ（位置）・スライダー＋2本指ピンチ（1〜3倍・フレーム中心不動点）で構図を決め「決定」で**640×360 JPEGへ焼き込んでから**既存のsetCameraOffImage（保存・配布）に渡す方式。**設計判断＝焼き込み方式**：オフセット/ズーム値を配布して受信側で再現する案は却下（プロトコル変更＋全員の描画同期が必要になる）。焼き込みなら配布プロトコル・受信ガード・updateCameraOffVisual・タイル描画が**一切無変更**で、セッション54の検証資産がそのまま有効。トレードオフ＝後から再調整は不可（再アップロードで対応。元画像もlocalStorage保存すれば再調整可能だが使用量が倍になるため見送り）。実装はSonnet 1体に委任し本体（Fable）が全差分裏取り＋独自検証。**実装詳細**＝①編集ソースは長辺1280pxへ事前縮小（巨大写真のiPhone描画・メモリ負荷対策。最終640×360の2倍なので画質劣化なし）②Pointer Eventsでマウス/タッチ共通（frameにtouch-action:none・imgはpointer-events:none）・2本指はMap管理の距離比ピンチ③クランプで画像が常にフレームを完全被覆（隙間なし）④座標変換はCSS配置(left:ox,top:oy,width:imgW*s)の逆写像 sx=-ox/s, sy=-oy/s, sw=frameW/s, sh=frameH/s ⑤パネルは外側クリックで編集中でも閉じられるためshowCameraOffImagePanel冒頭で編集状態をリセットする安全網⑥旧resizeCameraOffImageToDataURLは呼び出し元ゼロを確認して削除。**本体の独自検証**＝4象限色分け画像(1600×900)で実スライダーinput→ズーム2倍の不動点計算（ox=-100,oy=-56.25）・実PointerEventでドラッグ→クランプ(0,0)・決定→焼き込み640×360の全面が左上象限の純赤（JPEG誤差±3のみ）＝座標変換の正しさをピクセル単位で確認。キャンセル（編集破棄・通常表示復帰・パネルは開いたまま）・安全網（強制クローズ→開き直しでリセット）・localStorage保存も全PASS。node --check OK・フレッシュロードでコンソールエラーゼロ（エラーログ2件は前セッションのテストスタブ残骸と特定済み・製品コード起因ではない）。**実機確認のポイント（v4.18βまとめて）**＝①その他メニュー→カメラオフ時の画像→アップロード→**指ドラッグとピンチで構図調整**→決定→カメラオフで自タイルに調整どおりの画像が出るか②相手側でも同じ構図で見えるか（後入りピアも）③縦長写真で顔の位置を合わせられるか④画像を削除で黒背景＋中央名へ復帰⑤画面共有中のタイル表示。**次回**＝v4.18βの実機確認受領→stableタグ付与＆正式版宣言（β外し）の相談）

**（旧）最終更新（セッション54）**: 2026-07-14（**セッション54＝カメラオフ時のカスタム画像表示（Zoomライク）を実装**（コミット 4d162f5・app.js v129 / style.css v67・APP_VERSION **v4.18β**・**push＆デプロイ済み・実機確認待ち**）。ユーザー要望＝カメラオフ時に黒背景＋中央名の代わりに自分の好きな画像を表示（画面共有時も）。設計判断＝**方式A（データチャネル配布＋各自ローカル描画）を採用**：静止画をcanvas.captureStreamで映像トラック化する方式Bは帯域浪費＋監査済みのフィルターパイプラインに触れるため却下。方式Aは既存camera-stateの自然な拡張で、映像トラックはdisabledのまま（帯域ゼロ）・画像は動画圧縮を通らず綺麗・プレゼンターモード/ピン留めもタイル描画共通のため自動対応。実装はSonnet 1体に委任し本体（Fable）が全差分裏取り＋パネル幅の1件を自ら修正。**仕組み**＝①設定UI：その他メニュー「カメラオフ時の画像」→パネル（プレビュー＋アップロード＋画像を削除。bg-image-panelの作法流用）②取り込み：長辺640pxに縮小→JPEG q0.8（400,000字超はq0.6再試行、なお超過は「画像が大きすぎます」で中断）→localStorage `comchat_cameraoff_image` に永続化（次回通話でも有効）③配布：設定/解除時にbroadcast＋sendStatesToに追加で新規参加・後入り・復帰ピアへ自動同期④受信ガード：_allowMessageレート制限＋型/data:image/プレフィックス/400,000字上限の検証（不正は黙って破棄）⑤描画：タイルに`<img.camera-off-avatar>`（object-fit:cover）を常設し、新設updateCameraOffVisual()に中央名/画像の表示切替を一元化（camera-state受信・toggleVideo・addVideoElement・camera-off-image受信の全経路が同関数を呼ぶ。**画像がある時は中央名を出さない**＝名前は左下video-labelで見える）⑥後片付け：cleanupPeer/conn close/hangupでcameraStatesと同条件でpeerCameraOffImagesも削除・クリア（自分のcameraOffImageはユーザー設定なので保持）。**本体が直した1件**＝プレビューimgのwidth:100%がパネル(.bg-image-panel=内容物幅で広がる)を画像元サイズまで押し広げていた→固定幅200pxに。検証＝node --check OK・Chromiumプレビューの実window.comChatで挙動テスト全PASS（リサイズ640px/JPEG/サイズ上限・localStorage永続と再読込復元・カメラON中は画像非表示→OFFで全面表示＋中央名消滅・解除で黒背景＋中央名に復帰・受信側の画像優先表示とnull解除・巨大/偽スキーム/非文字列/undefined全て破棄・1秒20通超のフラッディング破棄・sendStatesToへの同梱（未設定時は送らない）・cleanupPeerでの片付け）・実クリックでメニュー→パネル→カメラトグルの一連の流れをスクリーンショット確認・フレッシュ初期化でコンソールエラーゼロ（記録された2件のTypeErrorはテストスタブがページ遷移時に踏んだもので製品コード起因ではない）。**実機確認のポイント**＝①その他メニュー→カメラオフ時の画像→アップロード（iPhoneの写真選択）→カメラオフで自タイルに画像が出るか②相手側の画面でも自分のタイルに画像が出るか（相手が後から入室した場合も）③画像を削除で黒背景＋中央名に戻るか④画面共有中のタイル表示⑤既存機能の回帰（入退室チャイム・背景フィルター・ファイル送受信）。**次回**＝実機確認の受領→正式版宣言の相談もまとめて（v4.17の監査9件修正の実機確認も未受領のまま本セッションでv4.18βへ進んだ点に注意：今回の実機確認でセッション53の回帰確認ポイントも併せて見るのが効率的）。

**（旧）最終更新（セッション53）**: 2026-07-13（**セッション53＝正式版宣言前のリリース監査（全コードのバグ再調査＋applyBgFilter理論的リーク精査）**（コミット f886663・app.js v128・APP_VERSION **v4.17β**・**push＆デプロイ済み・実機確認待ち**）。セッション50で合意した「正式版宣言前のリリース監査」を実施。Sonnet 3体に読み取り専用のバグ調査を並列委任（app.js前半/中盤/後半+HTML/CSS。後半担当にはセッション48見送りのapplyBgFilterリーク深掘りを特別課題として指定）→報告10件＋参考2件を本体（Fable）が全件コードで裏取りし**9件を修正・1件を見送り**。修正＝**①(高)満員ルームからの正規再接続の締め出し**: 6人満員時に'disconnected'固着したメンバーが同一ピアIDで再接続すると、handleConnectionの満員判定（connections.size≥5・旧エントリ残存で常に真）が同一ID再接続の救済ロジックより**先に**評価されるためroom-fullで弾かれ復帰不能だった→判定順を「同一ID再接続の掃除→満員判定」に入れ替え（新規ピアの満員拒否は従来どおり）**②(中)瞬断再接続での偽の退出→参加通知**: 同一ID再接続受け入れ時のcleanupPeerが通常退室と同経路のため「退出しました」チャイム＋直後のuser-joinで「参加しました」チャイムが全員に鳴っていた→cleanupPeerに`quietReconnect`オプションを追加（退出通知を出さず名簿も保持→保持により再接続後のuser-joinがisNew=falseとなり入室音も自然に抑制。万一新接続が確立しなければその接続のclose/errorハンドラが名簿ごと片付ける）**③(中)入退室音の永久無音化**: notifyAudioContextがバックグラウンド化等でsuspendedに落ちると、_playNotifyToneはresume()投げっぱなし＋同期stateチェックで毎回スキップ→誰も再確認せずセッション残り全体で無音になりえた→speakingAudioContextと同じ作法のpointerdown契機の再試行（registerNotifyResumeRetry/unregister・成功またはhangupで解除・多重登録なし）を追加 **④(中・特別課題の結論)applyBgFilter並走リークは「再現困難」ではなく通常操作で確実に踏めると判定を訂正**: genを進めるのはapplyBgFilter内の2箇所だけという前提に対し、同じ「停止＋クリーンアップ」を行うhangup()とcancelPreCall()がgen加算を欠いていた。フィルター選択直後（MediaPipeモデルDL数秒の待機中）に退室/キャンセルすると、セットアップがgen一致のまま続行し、孤立セグメンター・孤立canvas/captureStreamトラック・DOMから切り離されたまま再生し続けるvideo要素・退室後のウェルカム画面への「背景フィルターを適用しました」誤トースト（Firefox系ではbgSourceVideo null参照例外）を生成→両経路にgen加算1行ずつ追加（既存の3箇所のgenチェックが全て正しく効くようになる最小修正）**⑤(中)ImageCapture失敗catchのgenチェック漏れ**: フィルターのオン→オフ→オン連打で旧世代のcatchが新世代の有効なimageCaptureをnullで上書きし映像固着しうる→gen一致時のみnull化 **⑥(中)ファイル送信ループに中断機構なし**: broadcastがconnectionsをライブ参照するため、送信中に退室→再入室すると旧ループが新ルームの全員へfile-metaなしチャンクを数分流し続け、hangupのisSendingFileリセットにより新規送信との並走もありえた→_fileSendGen世代トークン（hangupで加算・sendFileが各チャンク前に確認して静かに退く・finallyのUI状態リセットも世代一致時のみ=再入室後の新送信を巻き戻さない）**⑦(中)file-metaのDoS耐性**: 他の表示系メッセージと違い_allowMessageが無く、進捗DOM無制限追加・複数id並行でのメモリ膨張・ファイル名長無制限だった→レート制限＋同時転送10件キャップ（正規は1人1転送=最大5並行なので余裕2倍）＋ファイル名100字切り詰め（user-joinの50字と同じ発想）を追加 **⑧(低)受信ファイルのDLリンク不達**: 数分かかる受信中にチャットが300件トリムに達すると進捗要素ごと押し出され、file-done時のリンクがdetachedノードに構築されて永遠に現れなかった→finalizeFileProgressで切り離し検知時に末尾へ再アタッチ **⑨(低)画面共有者ラベルのなりすまし**: screen-share-startだけ自己申告data.usernameを無検証・無制限長で表示していた（chat/memo等は名簿解決で統一済み）→usernames解決に統一。**見送り1件**＝peer-list先行登録とuser-join到達順による一時的な旧名表示（数百msのちらつきのみ・user-join到達で自己修復）。**参考2件も許容と確認**＝再接続直後の品質判定が1回だけ良好誤表示（次の3秒ポーリングで自己修復）・通知音の初回生成がジェスチャ外だと1回スキップ（設計コメントどおり）。検証＝node --check OK・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（満員時の同一ID再接続受け入れ＋旧conn close＋名簿保持＋消音/新規ピアはroom-full拒否維持/quietReconnect後のuser-joinは無音・真の新規参加は入室音/通常退室は退出音＋名簿削除/suspended通知コンテキスト→再試行ハンドラ登録→pointerdownでresume→解除→running時は登録なし/cancelPreCall・hangupのgen加算＋hangupの_fileSendGen加算/file-meta名前100字切り詰め（Map・DOMとも）・10件キャップ・レート制限/トリム押し出し→完了時再アタッチ＋リンク生成/共有者ラベル名簿解決＋自己申告無視/送信ループ=waitForBuffersスタブで実バックプレッシャーを模倣し中断でfile-done不送出＆チャンク部分送信＆finallyスキップ・非中断は完走＆状態リセット）・フレッシュ初期化でコンソールエラーゼロ。**実機確認のポイント**＝すべて既存機能の裏側の修正のため回帰確認が主眼：①通常の通話・入退室（チャイム・トースト）・チャット・ファイル送受信（完走とDL）②画面共有・背景フィルター（ぼかし/画像）・録音・デバイス切替・ピン留め・接続品質バー ③フィルター選択直後に退室/プレコールでキャンセルしても誤トーストが出ないか ④iPhoneスリープ→復帰後も入退室音が鳴るか。**セッション締め**：ユーザー指示によりセーブポイント`v4.17-stable`を f886663 に付与・push（セッション48と同じく**実機確認前の確定**である点に注意。βはユーザーの正式版宣言まで維持・次の開発サイクル開始時にv4.18βへ）。これでセッション50合意のリリース監査（バグ再調査＋applyBgFilterリーク）は**完了**＝正式版宣言（β外し）の前提タスクは消化済み。次回＝実機確認の受領→問題なければ正式版宣言の相談 or 新規相談）

**（旧）最終更新（セッション52）**: 2026-07-12（**セッション52＝接続品質インジケーター＋チャットログのダウンロード＋入退室のサウンド通知を実装**（コミット 4c0cb02・app.js v126 / style.css v66・APP_VERSION **v4.16β**・**push＆デプロイ済み・実機確認待ち**）。実装はSonnet 3体に順次委任（①品質 ②チャットログ ③サウンド）し、本体（Fable）が全差分を裏取り＋2件を自ら修正＋バージョン一括更新。内容＝**(A)接続品質インジケーター**: 各リモートタイルの名前ラベル内に電波バー3本（良好=緑3・やや不安定=黄2・不安定=赤1・計測中=灰）。showCallScreenから3秒毎に`updateConnectionQuality()`が全callの`peerConnection.getStats()`をポーリングし、RTT（transport.selectedCandidatePairId優先→nominated&succeededなcandidate-pairへフォールバック）と**区間パケットロス率**（inbound-rtpのaudio+video合算を`_qualityPrev`との差分で算出＝累積平均でなく直近悪化を検知）で判定（poor: rtt>0.4s or loss>10% / fair: rtt>0.25s or loss>3%）。await後のstaleガード（calls.get≠callなら破棄）・多重実行ガード付き。**ラベル構造の要**＝リネーム処理2箇所（user-join受信・confirmEditUsername）が`labelDiv.textContent`でラベルを丸ごと書き換えてバーを消す構造だったため、ラベルを`.video-label-name` span＋`.net-quality` spanに分割し、リネームはname spanのみ書き換えに変更（ローカルタイルはバー無し・自回線をgetStatsで測っても意味がないため）**(B)チャットログのダウンロード**: 表示DOMは300件で切り捨てられるため専用配列`chatLog`（上限2000件）を`displayChatMessage`（自分/相手の合流点）と`createFileProgress`（ファイル送受信両方が通る＝【ファイル】名で記録）で蓄積。チャットヘッダーのタブ右に⬇ボタン（#chat-download-btn・`.chat-close-btn`流用＋PC側はdisplay:noneを上書きする専用ルール）→`downloadChatLog()`がdownloadMemoと同作法で`comchat-chat-STAMP.txt`保存（1行目に保存日時ヘッダー・各行`[HH:MM] 名前: 本文`・空ログはトーストで拒否）。**メモと違い退室時の自動保存はしない**（保存したい人だけ手動DL）・hangupでリセット **(C)入退室サウンド**: Web Audio(OscillatorNode)のsine 2音チャイム（入室=D5→G5上昇・退出=G5→D5下降・各0.12秒・ピークgain0.06のエンベロープ）＋名前入りトースト「〇〇さんが参加/退出しました」。専用`notifyAudioContext`（用途別分離の作法・hangupでclose）。**発火条件の設計が肝**＝user-joinはリネームにも流用され、新規参加者は入室時に既存全員からuser-joinを受けるため、「①名簿(usernames)未登録(isNew) かつ ②callStartTime有(入室完了後)」の2条件で、リネーム（登録済み）・メッシュ相手（peer-listで接続前に事前登録）・ホスト受理シグナル（showCallScreen前=callStartTime null）を全て自然に抑制。自分の入室時はshowCallScreenで入室音1回（トースト無し）。退出はcleanupPeer（peer-leaving/幽霊掃除/重複接続掃除）で名簿に居た相手のみ・isLeaving中は抑制。**Fable修正1**＝conn close/errorのインライン掃除経路（タブ閉じ・クラッシュ・回線断のICE検知＝cleanupPeerを通らない）で退出通知が鳴らない穴と`_qualityPrev`削除漏れを両ハンドラに追加して解消（staleガードにより明示退室のpeer-leaving→遅延closeの二重鳴りは構造上起きない）。**Fable修正2**＝notifyAudioContextの生成がshowCallScreen（getUserMedia等の複数await後）だと自動再生ポリシーでsuspendedのまま一切鳴らない恐れ→`confirmPreCall()`冒頭（クリック直後・await前=create/join両方の唯一のジェスチャ起点）で先に`ensureNotifyAudioContext()`する対策を追加。検証＝node --check OK・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（①品質: リモートのみバー生成・good/fair/poor/unknownの全遷移・staleガード実測・リネームで名前更新＆バー生存 ②サウンド: リネーム無音/新規参加で音+トースト/callStartTime null抑制/退出音+トースト/幽霊・isLeaving抑制/実音関数エラー無し ③チャットログ: 空ガード・蓄積(通常+ファイル)・a.click横取りでDL内容とファイル名確認・2000件キャップ ④conn close経路: fakeConnで退出音+名簿/qualityPrev掃除+二重close無反応）・スクリーンショット目視OK（PC: 緑3本/赤1本のバー・チャットヘッダー⬇、モバイル375px: ⬇と✕が28pxで並ぶ・バー視認可）・コンソールエラーゼロ（**実WebRTC・実音の聴感はブラウザパネルの制約で検証不能＝実機確認に委ねる**）。READMEの機能一覧も更新（接続品質を予定→実装済みへ・3機能追記）。**実機確認のポイント**＝①2台以上で通話→相手タイルの名前横に電波バーが出て、片方をWi-Fiの弱い場所へ移すと黄/赤に変わるか ②チャット・ファイル送信後に⬇でログ保存→内容と時刻 ③後から参加/退室で双方のチャイムとトースト（音量が耳障りでないか）・タブを閉じた退室でも鳴るか ④名前変更で音が鳴らないこと・入室直後に既存メンバー分の音が乱打されないこと ⑤既存回帰（通話・画面共有・背景フィルター・録音・デバイス切替・ピン留め）。全OKなら`v4.16-stable`を付与（ヘッダーのβ表示はユーザーの正式版宣言まで維持・次サイクルでv4.17βへ。残候補＝正式版宣言前のリリース監査〔セッション50合意のバグ再調査＋applyBgFilter理論的リークをまとめて〕）。**実機確認1回目（2026-07-13受領）**＝①M4 MBA＋iPad Air2でiPadを電波の悪い場所へ移動→iPad上のMBAタイルのバーが変化＝**期待通り**（バーは「自分とその相手のP2P回線」の品質でどちら側の悪化でも変わる。MBA側は一人検証のため未確認→次回2人で双方向確認できれば理想だが必須ではない）②指摘1件＝チャット保存⬇がメモタブでも表示→**修正済み**（コミット 0d6dee9・app.js v127・switchChatTabでメモタブ時は`.hidden`付与＝メモには自前の保存ボタンがあり紛らわしいため。プレビューでタブ往復・メモ側保存ボタン健在・エラーゼロ確認・push＆デプロイ済み）。**実機確認2回目（2026-07-13受領）＝全部OK → `v4.16-stable` を0d6dee9に付与・push・正式確定。セッション52完了**（ヘッダーのβ表示はユーザーの正式版宣言まで維持・次の開発サイクル開始時にAPP_VERSIONをv4.17βへ上げる。残候補＝正式版宣言前のリリース監査〔セッション50合意のバグ再調査＋applyBgFilter理論的リークをまとめて〕）。次回＝新規相談待ち）

**（旧）最終更新（セッション51）**: 2026-07-12（**セッション51＝カメラ・マイクのデバイス選択＋タイルのピン留め拡大を実装**（コミット 140721a・app.js v125 / style.css v65・APP_VERSION **v4.15β**・**push＆デプロイ済み・実機確認待ち**）。実装はSonnet 2体に順次委任（①デバイス選択 ②ピン留め）し、本体（Fable）が全差分を裏取り＋2件を自ら修正＋バージョン一括更新。内容＝**(A)デバイス選択**: その他メニューに「デバイス設定」（#device-settings-btn・歯車アイコン）→ #device-modal（qr-modalと同構造・ダーク配色・オーバーレイ/閉じる/Escで閉じる・Esc統一ハンドラに統合）。開くたびenumerateDevicesでselect再構築（deviceId空は除外・label空は「カメラN」連番・使用中デバイスをgetSettings().deviceIdでselected・devicechangeは**モーダル表示中のみ**購読しhangup時も解除）。changeで即切替＝**新トラック取得成功後に旧トラックstop**（失敗時は現デバイス維持＋showStatusエラー＋選択巻き戻し）・enabled（カメラOFF/ミュート）引き継ぎ。**送出経路との整合が肝**: 背景フィルター中はsender不変でフレーム取込元（ImageCapture/bgSourceVideo）だけ新カメラへ、画面共有中はsender不変でlocalStreamと**cameraVideoTrack（共有終了時の復帰先固定参照）を追随**、通常時は全ピアにreplaceTrack。マイクは共有音声ミキシング中なら旧sourceをdisconnectし**同じmixDestへ新sourceを繋ぎ直し**（mixDest/micMixSourceをthisに昇格）、録音中はattachRecordingSource('__self__')再呼び出しで録音ミキサーにも追随（内部ガードで非録音時は無処理）、発話アナライザーもdetach→attachで再解析。**Fable修正1**＝フィルター中＋共有中のカメラ切替でcameraVideoTrackが追随しない穴（共有中にフィルターを外して共有停止→停止済み旧トラックへ復帰し黒画面になる）をフィルター分岐の外へ移して解消 **(B)ピン留め拡大**: タイルのダブルタップ（PC/モバイル共通で**clickベース自前検出300ms・videoGridへのイベント委譲1本**・3連打の二重発火防止・.video-containerにtouch-action:manipulationでiOSズーム抑止）でthis.pinnedTileId管理、.pin-mode/.pinnedクラス＋📌バッジ（.pin-indicator右上・挙手/ミュート/ラベルと非干渉）。レイアウトはCSS主体＝PC(≥769px)はflexで主役width:100%・height:calc(100%-98px)・object-fit:contain＋下部サムネイル帯、モバイル(≤768px)は主役order:-1・width:100%＋他3列。relayoutVideoGridはpresenter分岐にpin-mode相乗り（インラインの列数/--vg-tile/モバイルwidth除去。Zoom方式・モバイルflexbox本体は無変更）。自動解除＝対象退室・画面共有開始/視聴（presenter-mode付与2箇所の直前）・通話終了/開始経路。presenter-mode中の新規ピンとタイル1枚時は無視。**Fable修正2**＝サムネイル160px固定だと狭いPC幅やチャットパネル展開時（グリッド~516px）に折り返して98px帯からクリップされるため、width:min(160px,calc(20%-6.4px))＋aspect-ratio:16/9に変更（上限6人=サムネ最大5枚が必ず1行に収まる）。検証＝node --check OK・ローカルプレビュー実測で①モバイル375px: ピン留め（主役100%＋他3列114.9px・バッジ・インラインwidth除去）/付け替え（旧バッジ確実消去）/再タップ解除（2列復元）/退室自動解除 ②PC1280px: 通常グリッド3列297px→ピンで主役916×536＋サムネ5枚160×90が1行 ③820px（グリッド516px）: サムネ96.8×54.4の16:9で1行・オーバーフローなし ④デバイスモーダル: 表示/Esc閉じ/オーバーレイ閉じ/devicechange購読の付け外し・コンソールエラーゼロ（**実カメラ・実WebRTCはブラウザパネルの制約で検証不能＝実機確認に委ねる**）。READMEの機能一覧も更新（デバイス選択を予定→実装済みへ）。**実機確認のポイント**＝①Macで通話中にその他→デバイス設定→カメラ/マイク切替（相手に映像・音声が届き続けるか）②画面共有中・背景フィルター中・録音中の切替→終了後の復帰 ③iPhoneでタイルをダブルタップ→拡大・📌・再タップで解除・対象退室で自動解除 ④画面共有を始めるとピンが解除されるか ⑤既存回帰（通話・チャット・招待リンク・QR・通話時間）。**実機確認（2026-07-12受領）＝全部OK → `v4.15-stable` を140721aに付与・push・正式確定。セッション51完了**（ヘッダーのβ表示はユーザーの正式版宣言まで維持・次の開発サイクル開始時にAPP_VERSIONをv4.16βへ上げる。残候補＝接続品質インジケーター、正式版宣言前のリリース監査〔セッション50合意のバグ再調査＋applyBgFilter理論的リークをまとめて〕）。次回＝新規相談待ち）

**（旧）最終更新（セッション50）**: 2026-07-12（**セッション50＝招待リンク＋QRコード招待＋通話経過時間表示を実装**（コミット 203148e・app.js v124 / style.css v64・APP_VERSION **v4.14β**・**push＆デプロイ済み・実機確認待ち**）。前半は相談＝①追加機能の棚卸し（お勧め順: 招待リンク&QR→通話時間表示→デバイス選択。ビデオ録画・ブレイクアウトルーム・文字起こしは非推奨と回答）②バグ再調査は今はやらず**「正式版宣言（β外し）前のリリース監査」に取っておく**と合意（セッション48見送りのapplyBgFilter理論的リークもその監査でまとめて）。実装はSonnet 2体に順次委任（①招待リンク+QR ②通話時間）し、本体（Fable）が全差分を裏取り＋Escapeでqr-modalを閉じる統一＋バージョン一括更新。内容＝**(A)招待リンク**: room-infoバーのルームIDコピー横に「招待リンクをコピー」ボタン（#copy-invite-link・鎖アイコン）。URL=`origin+pathname+?room=ID`。`?room=`付きで開くと参加フォームにサニタイズ済みIDを差し込み名前入力へフォーカス（**自動参加はしない**）＋history.replaceStateでクエリ除去（リロード時の再誘導防止）**(B)QRコード招待**: #show-qr-btnで招待URLのQRをモーダル表示（#qr-modal・hangup-modalと同流儀）。`js/vendor/qrcode.js`＝qrcode-generator v1.4.4（MIT・56KB）を同梱し`<script>`で読込。ダークテーマでもカメラで読めるよう**白パネル＋クワイエットゾーン固定**。オーバーレイクリック/閉じるボタン/Escで閉じる。ライブラリ未読込時はshowStatusでエラー表示（クラッシュしない）**(C)通話経過時間**: room-info先頭に時計アイコン＋mm:ss（1時間超はh:mm:ss）のbutton（#call-timer）。**Date.now()-callStartTimeの差分を毎回計算**（バックグラウンドタブでintervalが間引かれてもズレない）。**タップで数字⇔アイコンのみ（薄色）を切替**（アイコンが再表示のタップターゲット兼代替表示・title/aria-label同期）。**毎通話デフォルト表示・永続化なし**（showCallScreenでリセット）。全hangup経路が通る共通クリーンアップでclearInterval。検証＝node --check OK・ローカルプレビューで`?room=ABC-123xyz`→`abc123xyz`サニタイズ参加誘導・クエリ除去・QRモーダル表示・タイマー進行/切替/即時復帰・モバイル375pxで4要素折り返し表示を確認（**カメラ取得とクリップボード書込はブラウザパネルの制約で検証不能**＝実機確認に委ねる。コピー処理は実績あるcopyRoomIdと同一パターン）。READMEの機能一覧も現状に合わせて更新。**実機確認のポイント**＝①ルーム作成→招待リンクコピー→LINE等で送って別端末がID入力済みで参加できるか ②Mac画面のQRをiPhoneカメラで読んで参加できるか ③通話時間が進む・タップで消えてアイコンだけ残る・再タップで正しい時間に戻る・退室→再入室でリセット＆デフォルト表示 ④既存回帰（通話・チャット・画面共有・背景フィルター）。**実機確認（2026-07-12受領）＝全部OK → `v4.14-stable` を付与・push・正式確定。セッション50完了**（ヘッダーのβ表示はユーザーの正式版宣言まで維持・次の開発サイクル開始時にAPP_VERSIONをv4.15βへ上げる）。次回＝新規相談待ち）

**（旧）最終更新（セッション49）**: 2026-07-12（**セッション49＝実機確認受領＋リファクタリング相談**（コード変更なし・ドキュメントのみ）。①セッション48の実機確認＝**全部OK**（M4 MBA＋Windows Surface2台構成で通話・画面共有まで相互確認。v4.13-stableは実機確認済みに昇格）②「画面共有中に共有者カメラがSurfaceでは見えた」件はユーザーの記憶が不確かなため**スキップ**（技術的事実＝ComChatは映像1本をreplaceTrackで差し替える設計なので、共有者カメラが別途相手に届くことはOS問わず起こりえない。見えたなら自分自身のローカルタイルか、全画面共有への映り込みのはず）③リファクタリング相談＝**現時点では不要と判断**（コードは健全・自動テストなしで構造変更の回帰リスクが利益を上回る）。ただし将来の従量制移行に備え、**下位モデル（Sonnetクラス）で実行できる指示書 `REFACTORING_GUIDE.md` をプロジェクト直下に作成**。方式＝ES modulesではなくprototype分割（mixin方式・複数scriptタグ）で「メソッドを一字一句そのまま移動」する機械的作業に落とし込み済み。8フェーズ・1フェーズ=1セッション厳守・フェーズ別メソッド名リストと実機確認ポイント付き。実施はトリガー（ファイル肥大でツール限界/機能追加が絡まる/構造起因バグ再発）が来てから。次回＝新規相談待ち。次の開発サイクル開始時にAPP_VERSIONをv4.14βへ上げる）

**（旧）最終更新（セッション48）**: 2026-07-12（**セッション48＝全コードのバグ調査・修正**（コミット 1de745c・app.js v123・APP_VERSION v4.13β・**push＆デプロイ済み・実機確認待ち**）。Sonnetエージェント3体に読み取り専用のバグ調査を並列委任（app.js前半/中盤/後半+HTML・CSS）→報告11件を本体（Fable）が全件コードで裏取りし**10件を修正・1件を見送り**。修正＝①(高)再接続時に旧接続の遅延close/errorイベントが確立済みの新接続のエントリ（connections/usernames/ビデオタイル等）を巻き添え削除→handleConnectionのclose/error/openへstaleガード追加（handleCall側の既存対策と対）②満員/ロック中ルームへの参加が「一瞬通話画面→即キック」→connectToHostをopenでなく最初のdataメッセージ（受理=user-join等/拒否=room-full・room-locked）で判定してから入室させ、handleDataMessage側はisConnecting中スキップ+room-fullにホストID限定のなりすましガード追加 ③'disconnected'固着の旧接続が同一ピアIDの再接続を無言拒否→同一IDからの新規接続時のみ旧接続を掃除して受け入れ ④hangup後の遅延data/openイベントがthis.peer.id参照でクラッシュしうる→handleDataMessage冒頭とconn openにpeerガード ⑤画面共有ボタン連打で許可ダイアログ待ちの間にgetDisplayMediaが二重実行されストリームが孤立リーク→_screenShareStarting再入防止フラグ（finally解除）⑥buildMixedAudioTrack途中失敗時にAudioContextがcloseされずリーク→生成直後にthisへ保持 ⑦他人の共有視聴中に自分が共有開始するとsharer-tileクラスが残留し相手タイルが空白のまま→クラス除去追加（相手は後勝ちルールで自動停止するため実害はタイル残留のみ）⑧MediaPipe読み込み失敗時のCSSフォールバックで「背景画像」選択が素のカメラ映像を「適用しました」表示のまま配信→imageは失敗扱い（noneへ戻す+エラー表示）にし、CSSフォールバック動作中のimage切替も_bgFilterCssModeフラグでブロック ⑨プレコールの背景フィルターパネルにclampPanelToViewport追加（通話中側と同じ画面外はみ出し補正）⑩退室ピアの_msgRate（レート制限Map）エントリ削除漏れ。**見送り1件**＝applyBgFilterの初回セットアップ並走の理論的リーク（再現困難・既存の世代トークン機構で概ね防御済み・修正リスクが利益を上回る）。node --check構文OK・ローカルプレビューでv4.13β表示とコンソールエラーゼロを確認済み。**実機確認のポイント**＝すべて既存機能の裏側の修正のため、通常の通話・入退室・再入室・画面共有・背景フィルター（ぼかし/画像）・満員時の参加拒否表示が従来どおり動くかの回帰確認が主眼。**セッション締め**：ユーザー指示により実機確認前にセーブポイントとして`v4.13-stable`を付与・push（**通常運用と異なり実機確認は未実施**のまま確定した点に注意。βはユーザーの正式版宣言まで維持）。**セッション48完了**。**実機確認（セッション49で受領）＝全部OK**（M4 MBA＋Windows Surface（約6年前の機種）の2台構成で通話・画面共有の相互確認まで実施。v4.13-stableは実機確認済みに昇格）。次回＝新規相談待ち。次の開発サイクル開始時にAPP_VERSIONをv4.14βへ上げる）

**（旧）最終更新（セッション47）**: 2026-07-12（**セッション47＝その他メニューの項目順を変更**（コミット 6ce683b・app.js v122・APP_VERSION v4.12β・**push＆デプロイ済み・実機確認待ち**）。新順序＝①ボタンの並べ替え ②ルームをロック（hiddenクラス維持・ホスト時のみ表示の挙動は不変）③録音を開始 ④背景フィルター。index.htmlの静的な並べ替えのみでapp.jsのクリック処理はイベントデリゲーションのため無変更。運用ルールどおり新サイクル開始でAPP_VERSIONをv4.12βへ（βはユーザーの正式版宣言まで維持）。実装はSonnetエージェントに委任（node --check・grepで順序確認済み）。**実機確認**：iPhoneで並び順OKの報告を受領→**`v4.12-stable` を ef5b2d0 に付与・push・正式確定。セッション47完了**（ヘッダーのβ表示はユーザーの正式版宣言まで維持・次の開発サイクル開始時にAPP_VERSIONをv4.13βへ上げる）。次回＝新規相談待ち）

**（旧）最終更新（セッション46）**: 2026-07-12（**セッション46＝セッション45実機確認の結果対応・点滅の視認性を2段階で修正**（最終コミット cf9482f・app.js v121 / style.css v63・**push＆デプロイ済み・実機確認待ち**）。実機結果＝**セッション45の項目は持ち越し分（メモロック・Chromeぼかし・空白メモ）も含めて全てOK、指摘は点滅1件のみ**＝「滅になった時の変化が小さく点滅していると分かりにくい」。**修正1回目（75fcb57・v62）**＝keyframesの谷を`opacity:0.55→0.12`に深め45〜55%で谷を保持→**M4 MBA実機で「まだはっきり見えない」と再指摘**。**真因（スクリーンショットで特定）＝🔴絵文字が赤いピル背景`rgba(220,38,38,0.85)`の上に乗っているため、透明度をいくら深くしても「赤地から赤い点が消える」だけでコントラスト差が構造的に出ない**。**修正2回目（cf9482f・v121/v63）**＝●を🔴絵文字から**CSSの白い円**（`.rec-blink-dot`＝9px・border-radius:50%・background:#fff・レコーダー機器の定番の見た目）に変更。`_buildBlinkDot()`はtextContent('🔴')を持たない空spanに（壁時計位相同期はそのまま）。谷は0.1。プレビューで山（白点くっきり）/谷（ほぼ消灯）を位相固定スクリーンショットで比較確認・コンソールエラーゼロ（いずれも小修正のためSonnet委任せず本体が直接対応）。**バージョン表示の運用ルール変更（ユーザー決定）**＝友人に完成版でない旨を伝えるため**βはユーザーが正式版を宣言するまで付けたままにする**。stableタグは従来どおり内部セーブポイントとして付与（タグ時のβ外しコミットは不要に）。次の開発サイクル開始時に数字だけ上げる（v4.12β等）。**実機確認**：白い●の点滅＝OKの報告を受領→**`v4.11-stable` を cf9482f に付与・push・正式確定。セッション46完了**（ヘッダーのβ表示はユーザーの正式版宣言まで維持・次の開発サイクル開始時にAPP_VERSIONをv4.12βへ上げる）。これでセッション43〜46の全項目（録音表示の右上移動/iPad縦768px/白点3秒点滅・メモ単一編集者ロック・スナップショット改善・ルーム画面ダークモード・退出ボタン右端・空白メモガード強化・ヘッダーバージョン表示・ウェルカム画面コピーライト・ホスト退室確認）が実機確認済み。次回＝新規相談待ち）

**（旧）最終更新（セッション45）**: 2026-07-11（同日**セッション45＝セッション44実機フィードバック＋相談決定事項の一括実装**（コミット 9e6ac41・app.js v120 / style.css v61・**push＆デプロイ済み・実機確認待ち**）。セッション44の実機結果＝ダークモード・退出ボタン右端・その他は概ねOK、録音表示に指摘2件＋相談で新規決定4件。オーケストレーション方式（Fable 5指揮＝仕様策定・実コード裏取り・相談対応＋Sonnetサブエージェント実装＋本体レビュー＋Chromiumプレビュー検証）。**(1)iPad縦で録音表示が中央に出る**＝真因：ワイド判定`min-width:769px`に対しiPad縦のCSS幅がちょうど768pxで1px届かず中央fixedに落ちていた→**768pxに変更**（iPhone縦=767px以下は従来のまま・iPhone横向きは768px超のため右上になるが横向きでは映像エリア右上の方が邪魔にならないと合意済み）。**(2)録音ピルがM4 MBAでカメラ映像タイル右上に被る**＝セッション43の`.call-main`基準absoluteは映像グリッドが最上部から始まると被る構造だった→**ワイド画面ではピルをviewport固定`fixed top:4px right:8px`（ティール色ヘッダー帯の右端に乗る）**・確認トーストも直下`top:36px`に整列。不要になった`.call-main{position:relative}`は全absolute子孫が自前の配置基準を持つことを調査の上削除。既知の許容事項＝縦700px以下（通話中ヘッダー非表示の環境）ではピルが映像最上端に軽く重なる（ユーザー合意済み）。**(3)点滅を●のみ・約3秒周期に**（全体点滅が目障りという指摘）＝`updateRecordingIndicator()`をcreateElement/textNode組み立てに変更（innerHTML不使用＝XSS安全性は従来同等）し🔴を`.rec-blink-dot`スパンに分離、CSSアニメーションは●だけに3秒周期で適用。**実装上の罠（本体が仕様段階で検出しSonnetに対策指示）**＝経過時間表示のため`setInterval`が毎秒この関数を呼びDOMを作り直す→素朴に実装するとアニメーションが毎秒0%（opacity:1）から再スタートし一度も暗くならない。対策＝`animationDelay = -(Date.now() % 3000)ms`で壁時計に位相同期（プレビューで再構築をまたいで1.00→0.64→0.67の3秒周期が連続することを実測確認）。**(4)ヘッダーのロゴ右にバージョン表示（相談で採用）**＝`APP_VERSION = 'v4.11β'`定数（app.js冒頭・1箇所管理）を`#logo-version`スパンに表示（0.62em・opacity 0.75の控えめ表示・通話中の圧縮ヘッダーでも崩れず）。**運用ルール＝stableタグ付与の直前にβを外すコミットを打ってからタグ、次の開発開始時に次版βへ上げる**。実機でのバージョン確認（キャッシュ残り判別）にも使える。**(5)ウェルカム画面最下部にコピーライト（相談で「©のみ」と決定）**＝`© 2026 iitsubu`。Sonnetの妥当な仕様逸脱1件＝案のmargin-top:autoはjustify-content:centerの中央寄せを崩す（実測で確認）ためabsolute bottom:16px配置に変更（既存padding-bottom 80px/モバイル32pxの余白内に収まる・ヒーロー中央寄せはピクセル一致で不変）。**(6)ホスト退室確認の強化（以前の相談決定事項・未実装だったもの）**＝`showHangupModal()`で「ホスト かつ `connections.size>0`」の時のみタイトル「あなたはホストです」・説明「退室すると、在室中の参加者全員の通話が終了します」に差し替え＋`#hangup-modal`に`host-leave-warn`クラス付与。キャンセルボタンは**表示中ずっと**ティール系グローで脈打つ（`cancel-pulse` 2秒周期・ライト/ダーク両モーダル地で視認確認済み）、退室ボタンのhover scale(1.04)はこのケースのみ抑制。**precallダイアログが`.hangup-modal-btn.cancel`を共用するため必ず`#hangup-modal.host-leave-warn`スコープ**（相談決定事項どおり・プレビューでprecall側はanimationName:noneを確認）。文言とクラスは**表示のたびに両分岐で明示的にセット/リセット**＋hideでも除去（状態残留なし）。**検証**＝`node --check`OK・変更3ファイルのみ（+115/-18行）・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（ホスト+在室者=専用文言/クラス/脈打ち・ゲスト/ホスト1人きり=従来文言・hide後のクラス除去・precall非波及・768pxちょうどで右上fixed（iPad縦の再現）・375pxで従来の上部中央・1280pxでピルbottom31px<グリッドtop40px=映像非接触・トーストtop:36px右寄せ・点滅位相の連続性実測・バージョン/コピーライト表示・ウェルカム中央寄せ不変・フレッシュ初期化でコンソールエラーゼロ）・スクリーンショット目視OK。**次回＝実機確認**：(A)iPad縦で録音表示がヘッダー帯右端に出るか、(B)M4 MBAでピルが映像に被らないか、(C)●のみ3秒点滅の見え方、(D)ヘッダーのv4.11β表示（各端末でバージョン確認にも使う）、(E)ウェルカム画面のコピーライト、(F)ホストが参加者を残して退室ボタン→専用文言＋キャンセル脈打ち・ゲストや1人きりでは従来文言、(G)セッション43-44の持ち越し（A-1〜A-5＝メモロック関連・Chromeぼかし、空白メモ再現有無）→**全OKなら：APP_VERSIONを'v4.11'に更新（β外し）＋キャッシュバスター+1のコミットを打ってデプロイ→そのコミットに`v4.11-stable`を付与**）

**（旧）最終更新（セッション44）**: 2026-07-11（同日**セッション44＝ルーム画面ダークモード（新規要望）＋持ち越しB-1/B-2**（コミット 0469d99・app.js v119 / style.css v60・**push＆デプロイ済み・実機確認待ち**）。オーケストレーション方式（Fable 5指揮＝仕様策定・実コード裏取り＋Sonnetサブエージェント実装＋本体レビュー＋Chromiumプレビュー検証）。**(1)ルーム画面の常時ダークモード化（ユーザー要望・トグルなし）**＝通話中のみダーク。スコープは既存の`.container.in-call`に加え、**#filter-panel/#more-menu/#reaction-panel/#bg-image-panel/#hangup-modalがbody直下（.containerの外）にあるため`document.body`にも`in-call`を付け外し**（showCallScreen/showWelcomeScreenに各1行追加・`:has()`は旧Safari非対応のため不使用＝既存方針踏襲）。パレット＝基調#111827・パネル面#1f2937・ボーダー#374151・主要文字#e5e7eb・二次文字#9ca3af・入力欄#111827地。対象＝チャット欄一式（メッセージ・入力・ファイル転送表示）・共有メモ（textarea・ツールバー・**ロック中readOnlyは#0b1220地#6b7280字でnot-allowed維持**）・ctrl-label・room-info（ライトのピル地は透過に）・名前編集input・body直下パネル全部・退室確認モーダル・並べ替えツールバー。**ライト維持**＝ウェルカム画面・precall-dialog（入室前）・ヘッダーのティールグラデ（ブランド）・トースト/録音ピル/円形カラーボタン（元々色付き）。CSSは末尾に専用セクション追加（色のみ上書き・レイアウト系プロパティ不変・index.htmlインラインstyleの.hangup-modal-box等には詳細度で勝つ）。チャット吹き出しは自分/他人のクラスが元々存在しない（displayChatMessageはプレーンdiv）ため.chat-messagesの色継承一括で対応。**(2)B-1退出ボタンを右端へ移動**＝`DEFAULT_CONTROL_ORDER`をmore-btn→hangupの順に変更＋index.htmlの初期DOM順も一致させた。**移行措置**＝loadStoredControlOrder()で保存配列が旧デフォルト完全一致（＝並べ替えモードで何も動かさず完了を押しただけの未カスタマイズ保存）ならキー削除してnull＝新デフォルト適用。実際に並び替えた保存はそのまま尊重（既存の堅牢マージが新デフォルト相対位置で機能）。**(3)B-2空白メモ自動DL調査**＝コード監査の結論：`downloadMemo()`のガード`!text.trim()`は空白・改行・NBSPを全てカバーしており、**唯一の抜け穴はtrim()が落とせないゼロ幅文字（U+200B〜200D/U+2060/U+FEFF・iOSの入力やコピペで混入しうる）**→ガードをゼロ幅除去後判定に強化（保存する本文自体は不変）。実機で再現したら保存ファイル名がcomchat-memo-*か（録音ファイル等との取り違えでないか）の確認を。**本体レビューでSonnet実装の修正2件**＝正規表現にゼロ幅文字を**不可視のまま直埋め**していた（エディタ正規化で静かに壊れうる・レビューで中身が見えない）→`\uXXXX`エスケープ表記化、room-info内の名前編集inputが白地のまま残っていた→ダーク化追加。**検証**＝`node --check`OK・変更3ファイルのみ（+285/-15行）・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（並び順移行4ケース＝旧デフォルト保存は破棄＋キー削除/カスタマイズ保存は尊重/部分保存は堅牢マージでhangup末尾/不明id除去・メモガード5ケース＝空/空白のみ/ゼロ幅のみ/混在は保存されず・実内容は1回だけ保存・ダーク計算済みスタイル約25項目全一致・readOnlyダーク配色・showWelcomeScreenでライト完全復帰＋bodyクラス除去・モバイル375pxのボトムシートチャットもダーク・フレッシュ初期化でコンソールエラーゼロ）・デスクトップ/モバイルのスクリーンショット目視OK（黒タイル上の名前・その他メニュー・退室モーダル・メモとも可読）。**次回＝実機確認**（下記セッション43のA-1〜A-5に加えて：(A-6)ダークモードの各端末での見え方＝Mac/iPad/iPhoneでチャット/メモ/各パネル/退室モーダルの可読性・ウェルカム画面がライトのままか、(A-7)退出ボタンが右端に来ているか＝過去に並べ替えを保存した端末では「自分で並べ替えた配置」が正しく維持されるかも、(A-8)空白メモ問題の再現有無＝再現時はファイル名を控える）→全OKなら`v4.11-stable`を 0469d99 に付与）

**（旧）最終更新（セッション43）**: 2026-07-11（同日**セッション43＝セッション41/42の実機確認フィードバック3件に対応**（コミット fca1d6e・app.js v118 / style.css v59・**push＆デプロイ済み・実機確認待ち**）。実機結果＝42までの項目は概ねOK・指摘3件。オーケストレーション方式（Fable 5指揮＝原因分析・仕様策定・AskUserQuestionで方式承認＋Sonnetサブエージェント実装＋本体レビュー＋Chromiumプレビュー検証）。**(1)録音表示がMac/iPadで目障り**→ワイド画面（769px以上）では常時点滅ピル`#recording-indicator`とOK確認トースト`.recording-join-toast`の**両方を映像エリア右上へ移動**（ユーザー承認済み）。実装＝ピルのDOMを`.call-main`内へ移動し、`@media(min-width:769px)`で`.call-main{position:relative}`＋両者をabsolute right:8px（ピルtop:8px・トーストtop:40px）。確認トーストは`showRecordingJoinNotice`のappend先を`this.callMain`に変更、基底`.toast`のtoastIn（translateX(-50%)含み）が右寄せで位置飛びするため専用`toast-in-right`フェードに差し替え。**iPhone（768px以下）は従来の画面上部中央fixedのまま**（`.call-main`がposition未指定のままなのでfixedが生きる・先祖にtransformなしは調査済み）。**(2)メモの同時編集で文章が混線**→真因＝「入力中(dirty)は上書きしない」保護が800msのflush毎に切れ、相手の全文がtextareaを丸ごと置換＋カーソル末尾飛び→交互に書くと断片が刺さる（全文後勝ちの構造的限界）。対策方式をAskUserQuestionで相談し**単一編集者ロック（推奨案）で承認**（CRDTは軽量方針違反で見送り継続・現状維持＋復元強化案も提示の上）。実装＝**既存の`memo-editing`シグナル（2秒スロットル）をロック要求＋更新として再利用**（新規メッセージは解除用`memo-unlock`のみ）。受信側は`memoLockHolder`をセットしtextareaをreadOnly化（グレー地・not-allowed・閲覧コピー可）、インジケーターを`${name}さんが編集中…（終わると入力できます）`の**常時表示**に（旧「後勝ち」文言はロック方式では実態と合わないため変更）。解除＝入力3秒停止 or blurで`memo-unlock`をbroadcast、受信側は**5秒無更新のstaleタイマーでも自動解除**（送信側は入力中2秒毎に更新・最大無信号3秒<5秒でパケット1紛失も吸収）。**同時要求の衝突は既存の後勝ち規約と同じ`senderId > this.peer.id`で決定的に譲り合い**（双方同判定＝一致保証）。保持者の**退場・切断・hangupの全経路で解除**（cleanupPeer/conn close/conn error/hangupに追加）。**本体レビューでSonnet実装の実バグ1件を検出・修正**＝**【中】blur経由の解除がデバウンスflushを追い越す**（unlockが先に届くと解除を見て書き始めた他人と自分の最終テキストがすれ違い境目で混線が再発。3秒自動解除は必ずflush 800msの後だがblurだけ逆転しうる）→`_releaseMemoLock()`で未送信分を先にflushしてからunlockを送る＋未編集時のblurでは余計なunlockを流さないガード。**(3)後書きで消えたテキストが見つからない**→真因＝バグではなく仕様の限界（スナップショットが「自分の送信毎800ms＋受信上書き毎」に積まれ10件リングが数秒で溢れて肝心の内容が押し出される＋「戻す」1タップ1段階のイタチごっこ）。対策＝スナップショットを**「他人のロックセッション開始時に1回」だけ**に変更（`_flushMemoUpdate`の送信前スナップショット削除・受信上書き時はロックシグナル無しで届いた場合のフォールバックのみ）＝10件が実質10セッション分に。プレビュー検証で炙り出した「変更ゼロセッションの空振りスナップショット」も`_clearMemoLock`で掃除。ロック中は「戻す」無効化（相手の編集をrev++上書きで破壊しないため）。**検証**＝`node --check`OK・変更3ファイルのみ（+128/-11行）・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（ロック取得/readOnly/常時表示・2秒更新でスナップショット増えず・ロック中update適用＋追加スナップショットなし・unlock解除・別ピアのなりすまし解除無視・「戻す」でセッション前に復元・変更ゼロセッション掃除・辞書順タイブレーク両方向・staleタイマー・退場解除・**blur時の送信順=memo-editing→memo-update→memo-unlock**（flush先行を実測）・未編集blurは無送信・ワイド1200pxでピル/トーストとも映像エリア右上absolute・モバイル375pxで従来のfixed上部中央8px/94px・42までの回帰なし=OKタップ消去/録音者ゼロ自動撤去/showStatus巻き添え除外維持）・スクリーンショット目視OK・フレッシュ初期化でエラーゼロ（既存エラーはテストスタブ由来）。**次回＝実機確認**（複数台で：メモの同時入力で混線しないか・ロックの表示/解除感・「戻す」の復元・録音表示のMac/iPad右上とiPhone中央・42までの持ち越し=Chromeぼかしの見え方）→全OKなら`v4.11-stable`を fca1d6e に付与）

**セッション43追記（次回やることの整理・時間切れでここまで）**: 次回セッションのタスクを以下に整理する。**A. 実機確認（`v4.11-stable` 付与の前提）**＝(A-1)メモの同時入力：2台で同時に書こうとして混線しないか（片方が数秒待たされ「〇〇さんが編集中…（終わると入力できます）」が出るのが正しい動作）、(A-2)ロックの解除感：手を止めて約3秒で相手側が入力可能に戻るか・書いていた人が退室/切断してもロックが残らないか、(A-3)「戻す」：他の人に上書きされた後、1タップで相手の編集セッション前の内容に戻れるか、(A-4)録音表示：Mac/iPad（769px以上）でピルとOK確認トーストが映像エリア右上に出るか・iPhoneは従来の上部中央のままか、(A-5)持ち越し＝Chromeで背景ぼかしがZoom並みの穏やかさに見えるか（強すぎ/弱すぎなら係数h×0.03を調整）。**B. 新規タスク（ユーザー要望・未着手）**＝(B-1)**退出ボタンをコントロールバーの右端へ移動**（現在の並びは初期配置定義とlocalStorage保存の並べ替え機能（セッション31）があるため、初期配置の変更と保存済み配置との整合（堅牢マージ）の両方を考慮すること）、(B-2)**空白メモの自動ダウンロード問題のチェック**＝実機で空白のメモが退室時に保存される事象の確認。コード上は`downloadMemo()`（[app.js:1826](js/app.js)）に`if (!text.trim()) return;`ガードがあり空白・改行のみなら保存されないはずなので、再現するなら原因調査（hangup経路は[app.js:3855](js/app.js)で無条件呼び出し→ガード頼み。textarea参照タイミングやロック中readOnly状態との絡みを疑う）。実装が伴うものは通常のオーケストレーション方式（仕様裏取り→Sonnet実装→本体レビュー→Chromiumプレビュー検証→実機確認）で。

**（旧）最終更新（セッション42）**: 2026-07-11（同日**セッション42＝セッション41実装の本体レビュー修正2件＋Chrome系背景ぼかしのZoom相当化**（コミット 6c71a08・app.js v117 / style.css v58・**push＆デプロイ済み・実機確認待ち**）。オーケストレーション方式（Fable 5指揮＝レビュー・実コード裏取り・仕様策定＋Sonnetサブエージェント実装＋本体レビュー＋Chromiumプレビュー検証）。**本体レビューでセッション41実装（Sonnet）の実バグ2件を検出（いずれもプレビューで実証済み）**＝**(A)【中】録音参加確認トーストが後続トーストの巻き添えで即消える**：`showStatus()`が`.toast`を全消しするため、挙手・入退室など通常トーストが出た瞬間にOK待ちの持続トーストも消えていた（録音通知直後はトーストが頻発するため実害大）。修正＝セレクタを`.toast:not(.recording-join-toast)`に変更＋確認トーストを`top:94px`へ移動（通常トースト50px・録音ピル8pxと三者重なりなし）。**さらにこの除外の副作用（消えるべき場面で残留する新経路）を本体が検出し追加修正**＝`updateRecordingIndicator()`の「リモート録音者ゼロ」分岐（録音停止・録音者退場・自分のhangupの全経路で必ず通る合流点）で未タップの確認トーストを自動撤去（「録音していないのに録音中と出続ける」虚偽表示とウェルカム画面への残留を1箇所で防止）。**(B)【低】メモ編集中の説明文が375pxで切り詰め**：`.memo-editing-indicator`が`nowrap+ellipsis`のため、長文化した「（後から書き終えた方が反映されます）」が必要333pxに対し実幅184pxで肝心の説明が読めなかった（実測）。修正＝`white-space:normal`＋`line-height:1.35`で2行折り返し許可（3秒で消える一時表示なのでツールバー高さ変動は許容）。**背景ぼかしのZoom相当化（ユーザー要望）**＝`applySegmentationMask`の`ctx.filter`経路（Chrome系＋Safari 18+）が`blur(100px)`固定で、既定解像度（`getUserMedia({video:true})`≒640×480）では背景が完全な色の霧になっていた。修正＝ぼかし半径を高さ比例`Math.max(10, Math.round(h * 0.03))`（480p→14px・720p→22px）に変更、はみ出し描画マージンも半径+10pxに連動。**Safariフォールバック（7-pass 1/2-scale）は変更なし**（対象は旧Safariのみ・スコープ外）。**検証**＝`node --check`OK・変更3ファイルのみ（+22/-11行）・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（確認トーストが後続トーストを生存・top 94px/50px共存・録音停止で撤去・録音者退場(detachRecordingSource)で撤去・複数録音者は片方停止では残り全員停止で撤去・OK未タップのままhangupで撤去・OKタップ消去の回帰なし・メモは375pxで2行折り返し全文可読(高さ34px)）。ぼかしは本番と同一式・同一描画のテストcanvasで新旧比較＝旧100pxは局所コントラスト0.83（完全な霧）→新14pxは3.34（形・色は判別可・文字は読めない＝Zoom相当でプライバシーも維持）をスクリーンショットで目視確認。コンソールの既存エラー2件はテストスタブ由来（フレッシュ初期化ではエラーゼロ）。**次回＝実機確認**（セッション41の(1)〜(4)＋今回の(A)(B)＋Chromeでのぼかしの見え方）→全OKなら`v4.11-stable`を 6c71a08 に付与）

**（旧）最終更新（セッション41）**: 2026-07-11（セッション40（相談のみ・コード変更なし）で仕様確定した(1)〜(4)を実装。**セッション41**（コミット c6c0eac・app.js v116 / style.css v57・**push＆デプロイ済み・実機確認待ち**）。**(1)ルームID伏字の自動復帰**＝[app.js:2018](js/app.js)`shareScreen()`の`getDisplayMedia`成功直後に`roomIdRevealed`をfalseへ強制リセットし`renderRoomIdDisplay()`を呼ぶ処理を追加（セッション40で合意した通りタイマー式は不採用・「共有を始めた瞬間」にだけピンポイントで効く）。**(2)録音中の途中参加者への確認トースト**＝`sendStatesTo(conn, {newJoin})`に第2引数を追加し、新規接続確立時（[app.js:1035](js/app.js)、`handleConnection`の`conn.on('open')`）だけ`newJoin:true`を渡す。フォアグラウンド復帰時の`state-sync-request`応答（[app.js:1225](js/app.js)）は対象外＝既存の受動的な常時点滅ピルのままで十分と判断。受信側の`recording-state`ハンドラは`data.join`がtrueの場合のみ新設の`showRecordingJoinNotice(name)`（OKタップでのみ消える持続トースト・`.recording-join-toast`／`.recording-join-toast-ok`）を表示し、それ以外（実際の録音開始/終了ブロードキャスト）は従来通り3秒で自動消滅する`showStatus`のまま。**(3)共有メモの後勝ち説明**＝[app.js:1297](js/app.js)付近の`memo-editing`受信時の表示文言に「（後から書き終えた方が反映されます）」を追記。**(4)ルームロックのトースト文言**＝[app.js:1408](js/app.js)付近の文言に「・今の参加者はそのまま」を追記。**検証**＝`node --check`OK・変更3ファイルのみ（+65/-8行）・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（(1)`getDisplayMedia`スタブで共有開始→`roomIdRevealed`がfalseに強制リセットされ表示が●埋めになることを確認、(2)途中参加時のjoinフラグ付きメッセージで確認トースト表示・実際のクリック操作でOKタップにより消去・通常のjoinなしブロードキャストでは従来通り受動的トーストのままで確認トーストが出ないことを確認、(3)(4)文言が意図通り反映されていることを確認）、スクリーンショットで見た目確認（赤地トースト＋白丸OKボタン、正常に表示・消去）、コンソールエラーゼロ。**次回＝実機確認**（4端末で(1)〜(4)の体感を確認）→全OKなら`v4.11-stable`を c6c0eac に付与）

**（旧）最終更新（セッション40）**: 2026-07-10（セッション39完了（`v4.10-stable`・統合タスクリスト完遂）を受け、同日**セッション40＝相談のみ（コード変更なし）**。UI/UXの細部について4点を相談・仕様確定（実装は次回にまとめて行う）。**(1) ルームID伏字の自動復帰**＝現状はタップでの手動トグル表示/非表示のみ（[app.js:1395](js/app.js) `renderRoomIdDisplay()`）で、開示したまま画面共有を始めてしまうと室名IDが映り込むリスクが残っていた。対策としてタイマー式（数十秒で自動的に伏字へ戻す）も検討したが、**「画面共有を開始した瞬間に強制的に伏字へ戻す」方式を採用**（会話中はいつまでも開示したままで良く、狙った危険（共有中の映り込み）にだけピンポイントで効くため）。**(2)〜(4)＝セッション38/39から持ち越しの宿題「ラベルだけでは伝わらない挙動」3点**（マニュアルでなく機能を使うその場で一言出す方向）を検討し、全て仕様確定。**(2)録音中の途中参加者対応**＝録音開始/終了は既に赤トースト＋常時点滅ピル（[style.css:1249](css/style.css) `recording-dot-blink`・1.6秒周期）で伝わっているが、これは`recording-state`が接続確立時に届く受動的な表示のため、録音中の部屋に途中参加した人が気づかず流してしまう可能性がある。Zoom式の常時確認ボタンは軽量方針に反するため見送り、**「録音中の部屋に途中参加した瞬間だけ」出す一度きりの確認トースト（OKタップで消える）を追加**することで合意。**(3)共有メモの「後勝ち」説明**＝現状`memo-editing-indicator`（[app.js:1297](js/app.js)）は`${name}さんが編集中…`とだけ表示し、同時編集時の後勝ち挙動には触れていない。常設注記でなく「まさに競合しうる瞬間」の一時表示に一言添える方式を採用し、文言を`${name}さんが編集中…（後から書き終えた方が反映されます）`に変更することで合意（ルームロックのトースト同様、括弧書きで機能説明を添えるスタイルに統一）。**(4)ルームロックのトースト文言**＝[app.js:1408](js/app.js)の「ルームをロックしました（新規参加を拒否）」に**「・今の参加者はそのまま」を追記**し、既存メンバーには影響しない旨を明示することで合意。**次回＝上記(1)〜(4)をまとめて実装**（通常のオーケストレーション方式＝仕様裏取り→Sonnet実装→本体レビュー→Chromiumプレビュー検証→実機確認→バージョン付与）。

**（旧）最終更新（セッション39）**: 2026-07-07（セッション38完了（`v4.9-stable`・統合タスクリスト①〜⑧のうち②〜⑧実装完了）を受け、同日**セッション39＝残り最後の統合タスクリスト⑦共有メモを実装**（コミット 244ff91・app.js v115 / style.css v56・**push＆デプロイ済み→同日実機確認全項目OK→`v4.10-stable` を 244ff91 に付与・push・正式確定。セッション39完了**）。**設計判断（ユーザーとの相談で確定）**＝候補に挙がっていた「画面共有アノテーション」機能と比較した結果、共有メモを採用（アノテーションは同期頻度・座標変換・弱端末負荷・画面共有まわりの既存の繊細さへの追加リスクの面で見送り）。UI＝チャットパネルのタブ方式（チャット/メモの2タブ、既存パネルの開閉ロジックはそのまま流用）。オーケストレーション方式（Fable 5指揮＝実コード裏取り・仕様策定＋Sonnet実装＋本体(Fable)レビュー＋Chromiumプレビュー検証。実装中にFable 5の利用上限に到達→Sonnetへ resume で作業続行→本体役をSonnet 5が引き継ぎ最終レビュー・検証・コミットまで担当）。**仕様**＝1つの共有テキストエリア（構造化しない自由記述、上限2万字）。**同期**＝入力800msデバウンス後に全文+リビジョン番号をbroadcastする後勝ち方式（rev比較、同revはpeerId辞書順で決定的にタイブレーク＝全端末が同じ勝者を選ぶ）。自分が入力中(dirty)の間は受信更新でtextareaを上書きしない（自分の送信が後勝ちで反映されるため実質のデータロスなし）。**後入り・復帰再同期**＝既存`sendStatesTo(conn)`に1行追加するだけで自動的にカバー（screen-share-start/peer-listと同様に再同期対象外にする必要はない＝逆転レースの心配がないテキスト全文送信のため）。**編集中インジケーター**＝2秒スロットルで`memo-editing`を送信、受信側は真正名簿(`usernames.get(senderId)`)から名前解決（なりすまし防止）、3秒で自動消去。**Undo**＝CRDTやフルUndo/Redoスタックは不採用（4本柱の「軽量」に反する）。直前スナップショットを最大10件ローカル保持し「戻す」ボタンで1段階復帰、復帰後は新しい編集として再broadcastして全員に伝搬。**保存**＝録音と同じ作法(`URL.createObjectURL`)でテキストファイルをローカルダウンロード。**hangup**＝メモに内容があれば自動保存してから全状態(memoText/memoRev/memoDirty/memoSnapshots/タイマー類)を完全リセットしタブをチャットに戻す。**受信側の防御**＝`_allowMessage`レート制限・不正rev(0/負数/NaN)の拒否・2万字切り詰めを既存パターンから踏襲。**検証**＝`node --check`OK・変更3ファイルのみ（+307/-13行）・本体レビューで死んだCSS(`.chat-header-title`、旧HTML構造の残骸)を1件検出・削除。Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（デバウンス送信＋編集中シグナル・新revの適用/旧revの無視(後勝ち)・dirty中の上書き防止＋rev追従・なりすまし防止(未登録IDは「ユーザー」)・メモタブ非表示中の更新ドット・undo(rev増加+broadcast+スナップショット復元)・sendStatesToへのmemo同梱・hangupでの自動保存+完全リセット+タブ復帰・不正rev拒否・2万字切り詰め・レート制限ちょうど20件）、375px(iPhone幅)/デスクトップ幅とも崩れなし、コンソールエラーゼロ。**実機確認**＝タブ切替・未読バッジ/更新ドット・複数端末同時編集の後勝ち体感・「戻す」操作感・退室時自動保存・iPhone/iPadでの入力感、すべて実施し全項目OKの報告を受領。**これで統合タスクリスト(セッション26+27発案)は①端末検証(友人ベータで継続中)を除き全て完了**。次回＝新規相談待ち。

**（旧）最終更新（セッション38）**: 2026-07-07（同日**セッション38＝実機確認の結果対応2件**（コミット 19e792d・app.js v114 / style.css v54・**デプロイ済み・実機確認待ち**）。**セッション35＋37の実機結果**＝後勝ち成立OK・37の6ボタン化/文字選択もOKだが報告2件＝**(1)共有者のサムネイルタイルに（カメラでなく）共有画面が映り大画面と2重表示**（双方向で発生）→**回帰ではなく元々の設計**（1ピア映像1トラック＝`replaceTrack`でカメラ→共有画面に差し替えるため視聴側にカメラは届かない。2本同時送信はメッシュで帯域倍増＝弱端末の柱に反し不採用）。修正＝**視聴側でpresenter-mode中のみ共有者タイルを`display:none`**（`.call-main.presenter-mode .video-container.sharer-tile`にスコープ＝クラス残留しても通常表示に無影響。**共有音声はこのサムネイルvideoから再生されている**ため（大画面はmuted）DOM削除でなくCSS非表示＝音声継続をプレビューで実測確認）。付与＝`enterRemotePresenterMode`（後勝ち交代時は旧共有者タイルからクラス除去→新共有者に付与）＋`addVideoElement`（共有中のタイル再生成でも維持）、除去＝`exitRemotePresenterMode`の`finish()`（PiP待機中の新共有はガードで中断→新しいenterが付け替え済み＝整合）。**(2)iPadでホストのミュートアイコンが表示されないことがあった**（間欠）→**真因（コード裏取り）**＝`broadcast()`は`conn.open===false`の接続を黙ってスキップ（app.js:1692）するため、「変化時1回だけ」のmute-state等は接続確立中/iOSバックグラウンド中と重なると**次のトグルまで永遠に欠落**。修正＝**フォアグラウンド復帰時の状態再同期**：`visibilitychange`で復帰側が`state-sync-request`をbroadcast（2秒デバウンス・peer/isLeaving/接続0ガード）→各ピアが`sendStatesTo(conn)`（conn open時の初期送信から抽出したヘルパー＝user-join・mute-state・camera-state・hand-state・recording-state(録音中のみ)）で現在状態を返す。**screen-share-startとpeer-listは再同期対象外**（後勝ち逆転レース・不要再ダイヤルのリスク。共有には既存修復経路＝conn open時送信＋handleCall streamの反映あり）。応答は`_allowMessage`レート制限＋登録済み接続のみ＋conn.openチェック（なりすまし/洪水/死接続安全）。オーケストレーション方式（Fable 5指揮＝真因調査・仕様策定＋Sonnet実装＋本体レビュー＋Chromiumプレビュー検証）。**検証**＝`node --check`OK・変更3ファイルのみ（+49/-8行）・実`window.comChat`＋実MediaStream（canvas+オシレーター）で**テスト17項目全PASS**（共有者タイル非表示・非表示中もvideo再生継続＝音声維持・他タイル無影響・後勝ち交代で旧共有者復帰/新共有者非表示・再生成でも維持・終了で全復帰＋クラス残骸ゼロ・sync応答の型と値・share/peer-list除外・未登録ピア無応答・閉接続スキップ・レート制限ちょうど20セット・visibilitychange発火＋2秒デバウンス）・コンソールエラーゼロ。**実機確認の注意**＝タイル非表示中の共有音声継続はiOS実機で要確認（プレビューはChromium）。**次回＝実機確認**（下記セッション38チェックリスト＋持ち越しの36複数台録音）→全OKなら`v4.9-stable`を 19e792d に付与）

**セッション38追記（同日・実機4台確認→方式変更）**: 4端末（M4 MBA・2011MBA・iPhone SE3・iPad Air2）の実機確認結果＝**持ち越しのセッション36複数台録音は全項目OK**・後勝ちOK・ただし**共有者タイルの全消し（display:none）は不評**（「タイルごと消えると共有者の在席・状態が分からない。修正前の方がマシ」）→ユーザーから「カメラオフ時と同じ黒地＋名前の表示を被せられないか」の提案→**承認を得て方式変更（コミット 599ee07・style.css v55・デプロイ済み・実機確認待ち）**。実装＝カメラオフ表示と同じ作法（style.css:317 `.video-container.camera-off .video-element{visibility:hidden}`）の流用＝`.call-main.presenter-mode .video-container.sharer-tile .video-element{visibility:hidden}`＋`.video-center-name{display:block !important}`の2行（**CSS差し替えのみ・JS変更ゼロ**＝sharer-tileクラスの付け外しロジックは検証済みのものを継続使用。`!important`は中央名のインラインstyle（JSがカメラオフ用に制御）に共有中のみ勝つため＝終了後は自動で従来制御に復帰。`visibility:hidden`は`display:none`と違い再生を止めない＝共有音声の継続がより確実）。**検証＝プレビューでテスト20項目全PASS**（タイル在席・黒地・中央名「M4 MBA」・ミュートアイコン/左下ラベル可視・video再生継続・後勝ち交代で新旧入れ替わり・終了で全復帰＋クラス残骸ゼロ・カメラオフとの共存＝共有中も終了後も矛盾なし）＋スクリーンショットで見た目確認・コンソールエラーゼロ。**残る実機確認**＝この黒地＋名前表示の見え方（4端末）とセッション38チェックリスト5(ミュート再同期・未報告)→**同日、実機確認全項目OKの報告→`v4.9-stable` を 599ee07 に付与・push・正式確定。セッション38完了**。これでセッション26＋27の統合タスクリストは全て完了（①端末検証は友人ベータで継続・②〜⑧実装済み）。次回の候補＝⑦共有メモ（録音と役割重複のため見送りも選択肢・ユーザーの判断待ち）or 新規相談。

**（旧）最終更新（セッション37）**: 2026-07-07（同日**セッション37＝iPhone/iPad向けUX改善2件**（コミット 01a6b8e・app.js v113 / style.css v53・**デプロイ済み・実機確認待ち**）。発端＝ユーザーから「iOSは画面共有できない理由」の質問→仕組み解説（iOSはWebKit強制でgetDisplayMedia非開放・Zoom/MeetはネイティブアプリのReplayKit経由・AndroidChromeは可）→「非対応端末ではボタン自体を消して左右に余裕を」で合意（ユーザー実体験＝iPhoneでボタンが詰まりすぎてタップmiss時に**ボタン名テキストを選択してしまう**頻発）。オーケストレーション方式（Fable 5指揮＋Sonnet実装＋本体レビュー＋Chromiumプレビュー検証）。**(1)画面共有ボタンの機能検出非表示**＝`initializeUI`で`!!(navigator.mediaDevices && navigator.mediaDevices.getDisplayMedia)`を判定し（`shareScreen()`内の既存ガードと同一式＝一貫性）、非対応なら`.ctrl-item`ごと`hidden`付与。iOS決め打ちでなく機能検出＝将来Appleが対応すれば自動復活。視聴専用の`screen-share-container`は別要素で無影響・`shareScreen()`内の明示メッセージガードは二重防御で残置。**(2)ラベル誤選択防止**＝`.ctrl-label`と`.filter-option`の`user-select:none`が無接頭辞のみで旧Safari/iOSに効いていなかった（**セッション32のルームID伏字と同型の地雷**・index.html:416だけが正しかった）→`-webkit-user-select:none`併記＋`-webkit-touch-callout:none`（長押しメニュー/虫眼鏡の入口を塞ぐ）。`.reorder-toolbar-hint`はuser-select自体が無かったので一式追加。チャット本文は意図的に対象外（コピーできるべきテキスト）。**検証**＝`node --check`OK・変更3ファイルのみ（+16/-2行）・Chromiumプレビューで（a）375pxで7→6ボタン化により間隔が明確に拡大（b）`hidden`を外すコードパスが全経路ゼロ＝通話中の復活事故なし（c）並べ替え機能は非表示itemもDOM順に含めて処理＝保存データ互換・非表示維持（d）計算済みスタイルで3セレクタとも`webkitUserSelect:none`適用・チャットは`auto`のまま（e）コンソールエラーゼロ。**次回＝実機確認**（下記セッション37チェックリスト＋持ち越し中のセッション35後勝ち・36複数台録音）→全OKなら`v4.9-stable`を 01a6b8e に付与）

**（旧）最終更新（セッション36）**: 2026-07-07（セッション35の後勝ち修正に続き、同日**セッション36＝統合タスクリスト⑧音声録音を実装**（コミット dcbbbf0・app.js v112 / style.css v52・**デプロイ済み・実機確認待ち**）。手元にMBA1台のみのため後勝ちの実機確認は後回しにし録音を先行実装（ユーザー判断）。オーケストレーション方式（Fable 5指揮＝仕様策定・実コード裏取り＋Sonnet実装＋本体レビュー＋Chromiumプレビュー検証）。**仕様（セッション26の合意どおり）**＝音声のみ・自分のマイク＋全リモート音声を**録音専用AudioContext**（mixAudioContext/speakingAudioContextと分離する既存作法）でミックスし、MediaRecorderでローカル保存（`isTypeSupported`で`audio/mp4`→.m4a（Safari系）／`audio/webm;codecs=opus`→.webm（Chrome系）を自動判別・1秒タイムスライス・ファイル名`comchat-recording-YYYYMMDD-HHMMSS.ext`）。**同意の担保**＝開始/停止を`recording-state`でbroadcast→全員に赤トースト通知＋画面上部中央に常設ピル「🔴 録音中」インジケーター（z-index 9000・pointer-events:none・ゆっくり点滅・自分の録音は経過mm:ss表示・複数録音者は連名・textContent挿入＝XSS不可・名前は名簿`usernames.get(senderId)`から解決＝なりすまし防止・`_allowMessage`レート制限・真偽値`!!`正規化）。UI＝「その他」メニューの背景フィルター直後に`#record-btn`行（録音中は赤`#dc2626`の専用`recording-active`スタイル＝ティールactiveと区別）。**動的追従**＝遅延参加者へconn open時に録音状態送信・録音中に届いた新規リモート音声を`handleCall`のstreamでミックスへ追加（同一peerIdは置換＝二重ミックス防止）・退場ピアは`detachRecordingSource`で切り離し（cleanupPeer/conn close/errorの全経路）。**hangup**＝録音中なら接続closeより前に`stopRecording()`（ファイル保存＋相手のインジケーター消灯の両立）。**Sonnetの妥当な仕様逸脱1件**＝停止broadcastを`onstop`（非同期＝hangupでは接続close後になり届かない）でなく`stopRecording()`で同期送信＝Fableの仕様側の競合バグを実装時に正しく修正。**Fableレビューでの追加1件**＝AudioContextがsuspendedで生成された場合のresume（無音ファイル事故の予防・ensureSpeakingAudioContextと同じ配慮）。**非対応端末**＝MediaRecorder無しは明示メッセージ（iOS画面共有ガードと同作法）。**制約（コメント明記）**＝マイクミュート中は自分の声が無音（track.enabled=falseの自然な挙動）・自分が画面共有中の共有音声はミックスに含めない（v1の割り切り）。**検証**＝`node --check`OK・変更3ファイルのみ・Chromiumプレビューで実`window.comChat`＋実音声（オシレーター）で**テスト23項目全PASS**（開始→.m4a保存・blob非空＝実録音成立・非対応端末ガード・インジケーター表示/経過時間/連名/XSS不可・リモート状態受信/解除・退場時の片付け・**録音中hangupでもファイル保存＋全リセット**・0秒録音でも例外なく安全・二重停止無害・レート制限ちょうど20通・不正真偽値の正規化・メニュー順＝並べ替え/フィルター/録音/ロック）・コンソールの新規エラーゼロ（既存エラーは全てテストスタブ由来のpeer.destroy＝本体無害と確認）。**次回最優先＝実機確認**（下記セッション35＋36チェックリスト）→全OKなら`v4.9-stable`を dcbbbf0 に付与）

**（旧）最終更新（セッション35）**: 2026-07-07（セッション34の実機確認結果を受けた**セッション35**（Fable 5指揮・コミット bef7077・app.js v111・**push＆デプロイ済み・再実機確認待ち**）。**セッション34の実機結果**＝チェックリスト7項目中、チャットの長文/大量投稿の境界値のみ未検証（閾値＝本文2000字/名前50字/表示300件/毎秒20通を今回ユーザーに共有済み・バグではないので未検証のまま許容）、**画面共有の「後勝ち」がNG**（M4 MBA↔2011 MBA 11inchの双方向で、後から共有を掛けると後勝ちにならず両者の共有表示が崩れる）、それ以外は全てOK。**真因（コード調査で確定）**＝他人の共有を視聴中に自分が共有を開始すると、`shareScreen()`が`currentRemoteSharerId`を更新しないため旧共有者のIDが残留。旧共有者が譲る際（`enterRemotePresenterMode`の後勝ち処理内`stopScreenShare()`）にbroadcastする`screen-share-stop`を新共有者が受信すると、ガード`senderId === currentRemoteSharerId`が残留値と誤って一致し、**始めたばかりの自分の共有表示を自分で`exitRemotePresenterMode()`して畳んでいた**（このガード自体はセッション28で「第三者の視聴者を巻き込まない」ために入れた正しい防御だが、「新共有者自身が直前まで視聴者だった」ケースが未考慮だった）。**修正**＝`shareScreen()`の共有確立時に`this.currentRemoteSharerId = null`をセット（自分が共有者になった時点で「誰かの共有を見ている状態」ではないため）。1行＋コメントの最小修正・キャッシュバスターv110→v111。**検証**＝`node --check`OK・変更2ファイルのみ・Chromiumプレビューで実`window.comChat`に`getDisplayMedia`スタブを当てた挙動テスト**10項目全PASS**（視聴中→自分の共有開始でsharerIdクリア・旧共有者のstop受信でも自分の共有表示/presenter-mode/stream無傷・回帰＝通常視聴者はstopで正しく解除・無関係なstopは無害）・コンソールエラー/警告ゼロ。**次回最優先＝実機で後勝ちの再検証**（下記セッション35チェックリスト・Mac/PC 2台で双方向）→OKなら`v4.9-stable`を bef7077 に付与）

**（旧）最終更新（セッション34）**: 2026-07-06（セッション33（app.js v109・デプロイ済み）の実機フィードバック対応＝**セッション34**（コミット 6539944・app.js v110・**push＝ユーザー承認待ち／未デプロイ**）。**実機結果**＝A-1/B-2/B-3/B-4/D/通常通話は概ねOK、ただし報告3件＝**(1)項目3ロック中の再入室**（ロック中は新規参加拒否OKだが、元メンバーが退室→再入室しようとしても拒否される）→ **ゲストIDは`new Peer(null)`で毎回ランダム＝元メンバーを識別する術がないため自動許可は不可能**と判明。ユーザーと協議し**「厳格ロックのまま運用（再入室が必要ならホストが一時解除→入室→再ロック）」で対応と決定・コード変更なし**。**(2)項目4画面共有**（表示・停止はOKだが「後勝ち」検証でiPhone SE3・iPad Air2から共有しようとすると失敗）→ **B-2の回帰ではなくiOS/iPadOS Safariが`getDisplayMedia`非対応＝画面共有を発信できないプラットフォーム制約**と確定（M4 MBAの共有はOK・後勝ちは共有発信可能な2台＝Mac/PC同士でないと検証不可）。原因不明の失敗に見えないよう`shareScreen`冒頭で非対応端末を検知し明示メッセージ（視聴は可能）を追加。**(3)UI要望**＝「その他」メニューの「ボタンの並べ替え」を最上部へ移動。**加えて前回外したDoS対策B-5/B-6を実装**＝**B-5**（file-metaで`totalChunks`(1〜3200)・`size`(0〜200MB)・`id`型を検証、file-chunkで`index`範囲外/非整数を拒否、file-doneの`atob`をtry/catch保護）、**B-6**（chat/reactionにピアごと1秒20通の簡易レート制限、チャット/名前を2000/50字に切り詰め、チャット表示を直近300件に整理、初回ユーザー名入力欄に`maxlength=20`追加。ファイル転送チャンクは正規でも高頻度なため対象外＝B-5で保護）。**検証**＝`node --check`OK・変更2ファイルのみ・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（メニュー順=並べ替え先頭・maxlength・チャットの長さ2000/名前50切り詰め/名前は真正名簿解決/レート制限≤20/300件整理・B-5のmeta過大拒否/正常受理/chunk範囲外拒否/不正base64でthrowなし・iOS共有ガードの明示メッセージ・reactionレート制限）・フレッシュ初期化でコンソールエラーゼロ。**次回最優先＝実機確認**（下記セッション34チェックリスト）→全OKなら`v4.9-stable`付与。まずpush承認が必要）

**（旧）最終更新（セッション33）**: 2026-07-06（セッション32完了（`v4.8-stable`）→同日**セッション33＝全コードのバグ・脆弱性調査＋確定所見6件を修正**（コミット 0f05a88・app.js v109・**push＝実機確認前でユーザー承認待ち／未デプロイ**）。オーケストレーション方式（Fable 5指揮＋Sonnetサブエージェント2体並列で①app.jsロジック精査②横断整合性＋セキュリティ点検→本体が実コードで裏取り→Sonnetが実装→本体レビュー＋Chromiumプレビューで実インスタンス挙動検証）。**致命的バグはゼロ**。確定した低リスク所見6件を修正＝**A-1（唯一の正規利用実バグ・中〜高）背景フィルターループが停止せず増殖**（`stopBgFilterLoop`が`await grabFrame()`中の空振りで効かず、再スケジュール判定が`bgFilterType==='none'`のみのためカメラOFF（フィルター維持）経路でループが生存、カメラON/OFF毎に増殖→発熱/fps低下。世代トークン`_bgFilterLoopGen`で停止時に実行中ループを無効化し常に1本に根治）、**B-1（改造クライアント前提・中）ルームロックがメディアコール経路を守らない**（`handleIncomingCall`が`roomLocked`未チェック＝データ接続を経ず直接callするとロック中でもホスト映像/音声流出→1行封鎖・既存メンバー無影響）、**B-2（中）`screen-share-start`の送信者未検証**（`data.peerId`→`senderId`置換＝なりすまし・自己ID詐称固着・セレクタSyntaxErrorを同時防止）、**B-3（中）`peer-list`のホスト未検証**（`senderId===roomId`のみ受理＝大量connectToPeer誘発DoS防止）、**B-4（中）チャット発言者名なりすまし**（`data.username`→`usernames.get(senderId)`＝真正名簿から解決）、**D（低）hangup後のリアクション残像**（`reactionOverlay.textContent=''`でクリア）。**検証**＝`node --check`OK・変更2ファイルのみ（js/app.js・index.html v108→v109）・Chromiumプレビューで実`window.comChat`に対する挙動テスト全PASS（B-1ロック中の他人拒否/既存メンバー受理・B-2はsenderIdを共有者に採用・B-3非ホストpeer-list無視/ホスト受理・B-4真正名簿解決/不明送信者は'ユーザー'・A-1停止後の反復凍結＝ゾンビループ根絶/二重startで単一ループ化・フレッシュ初期化で`_bgFilterLoopGen=0`）、コンソールエラーは私のテストスタブ由来のみ（`if(cc.peer)`ガードで本体は無害と確認）。**今回のバッチから除外＝B-5（ファイル転送受信側の境界検証欠如＝index無検証/totalChunks無上限/atob例外未捕捉→DoS・中〜高）・B-6（メッセージのレート/長さ制限皆無＝フラッディングDoS・中）・C-1（同時参加グレア＝双方向ダイヤルで2人相互不可視・非決定的・要設計変更）・C-2（iPhone全画面視聴中のhangupで黒残骸＝実機裏取り要）・SRI無し・360〜480px帯のCSS後勝ち**は別途判断（DoS対策とグレアは設計/実機依存のため慎重に）。**次回最優先＝実機確認**（下記セッション33チェックリスト）→全OKなら`v4.9-stable`付与。まずpushの承認が必要）

**（旧）最終更新（セッション32）**: 2026-07-06（セッション31完了（`v4.7-stable`）→同日**セッション32＝統合タスクリスト⑤ルームロック＋⑥ルームID伏字化を実装**＝「その他」メニューにホスト限定「ルームをロック」トグル（🔓/🔒アイコン切替・screen-fullscreen-btnと同じ二枚方式）を追加、ロック中は`handleConnection`の定員チェック隣（新規参加の唯一の門番）で新規接続を拒否し`room-locked`を送って`close()`（room-fullと同じパターン）。ルームIDはデフォルトで伏字（実際の桁数だけ`●`を表示）にし、タップで表示/非表示をトグル（コピー機能は`this.roomId`を直接使うため無影響）。コミット db1ec5e →**本体（Fable）レビューで実バグ1件＋互換1件を検出・修正（64ea76e・app.js v108）**＝(1)**【高】`room-locked`受信が無検証**＝そのまま`hangup()`するため、メッシュ内の任意のピアが`{type:'room-locked'}`をブロードキャストすると**全員を退室させられた**。ロックの門番は`isHost`ガード付きでホストしか送らないので、`room-closed`と同型の`!isLeaving && !isHost && senderId === roomId`検証を追加（なお`room-full`も無検証だが、定員チェックはメッシュ形成時にゲスト側の`handleConnection`でも正規に発火しうる＝送信者をホストに限定できないため現状維持が正しい）。(2)**【低】`user-select:none`が無プレフィックス**＝Safari 15.3以前（2011 MBA等）では効かず伏字●がダブルタップで選択される→`-webkit-user-select`併記。プレビューで検証5ケース全PASS（詐称ピアからのroom-locked無視・ホストからは受理・自分がホスト時/isLeaving時は無視・webkitUserSelect=none適用）。デプロイ→**同日実機確認全項目OK → `v4.8-stable` を 64ea76e に付与・push・正式確定。セッション32完了**。これでセッション26＋27の統合タスクリストは⑦共有メモ・⑧音声録音（議事録ゴールの二択・どちらも中規模）を残すのみ。次回はこの二択の仕様詰めから）

**（旧）最終更新（セッション31）**: 2026-07-05（セッション30完了（`v4.6-stable`）→同日**セッション31＝ボタン並べ替え・左利き対応を実装**＝その他メニュー「ボタンの並べ替え」→編集モード（iOSホーム画面風の揺れ＋完了/初期配置に戻すツールバー）→Pointer Events自前D&D→localStorage保存・起動時適用（堅牢マージ付き）。コミット 607e427・app.js v106 / style.css v50・デプロイ済み→**同日実機確認全項目OK → `v4.7-stable` を 607e427 に付与・push・正式確定。セッション31完了**。次回の実装候補＝統合タスクリスト⑤ルームロック＋⑥ID伏字化（設計はセッション27で済み＝約30分・低リスク・守りセット））

**（旧）最終更新（セッション29）**: 2026-07-05（セッション29＝統合タスクリスト**④リアクション＋挙手**を実装（5578372・v103）→ 実機テストで不具合3件（ゴースト接続の満員誤判定・iPhone PiP残骸・ボタン7個折り返し）→原因分析・修正（3aaa04c・app.js v104 / style.css v48）→ **再実機確認OK → `v4.5-stable` を 3aaa04c に付与・push・正式確定。セッション29完了**。同セッション後半でUI相談→**次回セッション30の実装内容が確定**＝(1)リアクションの全体演出化（絵文字＋名前タグがコントロールバー上から画面上端へ昇る）(2)コントロールバー再構成（常設6個＝カメラ・マイク・画面共有・リアクション・チャット・退室＋「その他…」メニュー、フィルターはその他へ）。その後＝セッション31でボタン並べ替え（左利き対応・その他ボタンも並べ替え対象に含める・編集モード方式）→⑤ルームロック＋⑥ID伏字化）
**プロジェクト**: ComChat (完全無料P2Pビデオ会議アプリ)
**進捗状況（セッション32／⑤ルームロック＋⑥ルームID伏字化の実装）**: セッション27で設計済みの統合タスクリスト残り2件を実装した（**コミット db1ec5e→本体レビュー修正 64ea76e・app.js v108 / style.css v51・デプロイ済み→同日実機確認全項目OK→`v4.8-stable` 正式確定**）。**【⑤ルームロック】** ルームの入口はホスト1箇所（ルームID＝ホストPeer ID／参加者は必ずホストへ`peer.connect()`し`handleConnection`を通る＝定員チェックも同じ1箇所）という設計どおり、`handleConnection`の定員チェック直前に門番を追加＝`if (this.isHost && this.roomLocked)`なら`conn.on('open', ...)`で`{type:'room-locked'}`を送信後150msで`close()`（**room-fullと全く同じパターンを踏襲**＝`connections`に登録しないため、`connectToHost`/`connectToPeer`の「登録されていなければcallを発信しない」ガードがそのまま効き、追加の分岐は不要）。受信側は`handleDataMessage`に`case 'room-locked'`を追加、`room-full`と同様に`hangup()`＋エラートースト。**v1は設計判断どおり「ロック＝新規参加を全拒否」のシンプル版**（既存メンバーの再接続もロック中は同じ門番に引っかかるが、ロック中はそもそも想定しない運用として許容＝セッション27の設計判断を踏襲）。UI＝「その他」メニューにホスト限定行`#room-lock-btn`（🔓/🔒の2枚SVGを`.locked`クラスで切替＝`screen-fullscreen-btn`の`fs-enter-icon`/`fs-exit-icon`と同方式、ONは既存`.more-menu-item.active`のティール表示を再利用）。`updateRoomInfo()`で`!this.isHost`なら`hidden`クラスを付与しゲストには表示しない。`hangup()`で`roomLocked`とボタンの見た目を確実にリセット（次に作る/入るルームへロック状態が漏れない）。**【⑥ルームID伏字化】** `room-info`のID表示（画面共有/スクショで映り込むと第三者が入室できてしまう）をデフォルトで伏字にした。`renderRoomIdDisplay()`が`this.roomIdRevealed`を見て`●`(実際の桁数だけ)か実IDかを`textContent`に設定、`#room-id-display`のクリックで`roomIdRevealed`をトグルして再描画。`updateRoomInfo()`内の直接`textContent`代入をこの関数呼び出しに置き換えただけなので既存の呼び出し元（作成/参加/接続変化のたび）はすべて自動的に伏字ロジックに乗る。コピー機能(`copyRoomId`)は表示テキストでなく`this.roomId`を直接使うため無影響。`hangup()`で`roomIdRevealed`をfalseにリセット（次のルームは常に伏字から始まる）。**【検証】** `node --check`OK・`git diff --stat`で3ファイルのみ（css/style.css, index.html, js/app.js）・Chromiumプレビューで**ロジック検証**（`window.comChat`を直接操作＝ID表示がデフォルトで`●`9個・クリックで実ID⇄伏字を往復・ロックトグルでアイコン切替(`display:block/none`)+`active`色+ラベル文言が連動・ゲスト(`isHost=false`)では`#room-lock-btn`が`hidden`・ロック中の`handleConnection`に偽の接続を渡すと`connections`に登録されず`{type:'room-locked'}`を送信・ゲスト側で`handleDataMessage({type:'room-locked'})`を呼んでも例外なく`hangup()`が走る）、コンソールエラー/警告ゼロ、リロードでも初期化エラーなし。**【本体（Fable）レビューでの修正2件（64ea76e）】** (1)**【高】`room-locked`受信の送信者検証欠落（実バグ）**＝初版は無検証で`hangup()`していたため、メッシュ内の任意のピアが`{type:'room-locked'}`をブロードキャストすると全員を退室させられた。ロックの門番は`isHost`ガード付き＝正規の送信者は必ずホストなので、`room-closed`と同型の`!isLeaving && !isHost && senderId === roomId`検証を追加。`room-full`も無検証だが、定員チェックはメッシュ形成時にゲスト側`handleConnection`でも正規に発火しうる（送信者をホストに限定できない）ため現状維持が正しいと判断。(2)**【低】`user-select:none`の無プレフィックス**＝Safari 15.3以前は無視するため伏字●がダブルタップで選択される→`-webkit-user-select:none`を併記。修正後プレビューで**5ケース全PASS**（詐称ピアからのroom-locked無視・ホストからは受理・自分がホスト時は無視・isLeaving時は無視・webkitUserSelect=none適用）・コンソールエラーゼロ。**注意**＝実端末間のデータチャネル配送（ロック中の実際の参加拒否）とiPhone/iPad実機でのタップ操作感はプレビューでは検証できないため実機確認を実施→**全項目OK・`v4.8-stable` を 64ea76e に付与・push・正式確定（セッション32完了）**。

**（旧）進捗状況（セッション31／ボタン並べ替えの実装）**: オーケストレーション方式（Fable 5指揮＋Sonnet実装＋本体が実コードでレビュー）でセッション31の合意仕様を実装した（**コミット 607e427・app.js v106 / style.css v50・デプロイ済み→同日実機確認全項目OK→`v4.7-stable` 正式確定**）。**【エントリと編集モード】** `#more-menu` に2行目「ボタンの並べ替え」（`#reorder-btn`）を追加。タップでメニューを閉じ編集モードへ＝`.controls.reorder-mode` で全 `.ctrl-item` が**iOSホーム画面風に揺れる**（`@keyframes ctrl-item-wiggle` ±2°・0.26s・nth-childの負のanimation-delayで開始タイミングをずらし機械的な同期を回避）＋バー直上に `#reorder-toolbar`（「完了」ターコイズ・「初期配置に戻す」グレー・案内文「ドラッグで並べ替え」は≤480pxで省略）。編集モード突入時に全パネルを閉じ、**キャプチャ段階のclickリスナー**で編集中はボタン本来の動作を完全抑止（退室モーダルやパネルが誤って開かない）。**【D&D＝Pointer Events自前実装】** HTML5 D&D APIはiOSタッチで動かないため不使用。pointerdown→`setPointerCapture`→pointermove→pointerup/**pointercancel**。編集モード中の `.ctrl-item` に `touch-action:none`（スクロール競合防止）・同時ドラッグ1つ・ドラッグ中は `.reorder-dragging`（揺れ停止＋scale1.12＋影の持ち上げ演出）。**他itemの中点を越えたら `insertBefore` でライブ並べ替え**（FLIPなしの即時reorder＝正しさ優先・仕様で許容済み）。**「その他」ボタン自体も並べ替え対象**（合意どおり）。`cancelReorderDrag()` に後始末を集約（pointerup/pointercancel/強制終了の全経路から呼ぶ）。**【永続化】** 「完了」で現在順（idの配列）を `localStorage['comchat-control-order']` へ保存し終了。起動時 `applyStoredControlOrder()`（initializeUI内）で適用。**堅牢マージ**＝保存配列から現存しないidを破棄し、保存に無い現存ボタン（将来の新機能）はデフォルト相対順で補完・壊れたJSONはtry/catchで無視。「初期配置に戻す」＝JS定数 `DEFAULT_CONTROL_ORDER` へ即時復帰＋localStorage削除（編集モードは継続）。`hangup()` 冒頭で `exitReorderMode(false)` を呼び、編集UI（ツールバー・揺れ・ドラッグ状態）をウェルカム画面へ漏らさない。**パネル類はセッション30の「開くたび実測＋クランプ」方式のためボタンがどこへ移動しても自動追従＝変更不要**（左端に移動した「その他」からもメニュー/フィルターパネルが画面内に開くことをテストで確認）。**【本体レビューでの修正2件＋CSS整理】** (1)**pointer capture先の変更（堅牢性バグ）**＝キャプチャをドラッグ中のitem自身に取っていたが、`insertBefore` はDOM上の「remove→insert」なのでエンジンによってはその瞬間キャプチャが解除され、指がバー外へ出るとpointermove/pointerupを取りこぼし**ドラッグが固着して以後並べ替え不能**になりうる→DOM移動しない `.controls` コンテナ側にキャプチャを変更（リスナーもコンテナ側なので受信に支障なし）。(2)**並べ替え時の視覚ジャンプ（UX不具合）**＝translateの基準が掴んだ時点のレイアウト位置のままなので、insertBeforeで自分のスロットが変わるたびにボタンが指から1スロット分飛ぶ→`_reorderDraggedItem()` で移動前後のレイアウト差分によりstartX/Yを補正し**指の真下に留める**（テストで見た目中心=ポインタX一致を確認）。(3)≤360pxブロック内のツールバーボタン縮小CSSが後方の≤480pxブロックに同詳細度後勝ちで上書きされる死にルール（セッション29教訓と同型）→整理。**【検証】** `node --check` OK・Chromiumプレビューで**テスト12項目全PASS**（編集モード突入＝揺れ/touch-action:none/ツールバー表示・編集中のhangup/more-btnクリック完全抑止・擬似PointerEventsでのドラッグ＝1スロット移動と左端への連続移動・**補正後の見た目中心がポインタと一致(454 vs 454)**・pointerupでclass/transform/状態の完全解消・完了で保存→リロードで復元・未知id混入/id欠落データのマージ・壊れたJSONでnull・初期配置に戻す＝デフォルト順+storage削除+編集継続・hangup経路の強制終了＝ドラッグ中でも残骸ゼロ・320pxでツールバー1行(308px)＋左端「その他」からのパネル画面内クランプ）・コンソールエラー/警告ゼロ。**Safari15制約**遵守（`:has()`/color-mix/oklch不使用・新規svhなし・`:scope`はSafari7+対応で問題なし）。**注意**＝実タッチのドラッグ感（追従・慣性・誤操作しにくさ）と実機Safariのpointer capture挙動はプレビューの擬似イベントでは検証できない→**実機確認が特に重要**。**次回最優先＝実機確認**（下記チェックリスト）→全OKなら`v4.7-stable`付与。

**（旧）進捗状況（セッション30／①＋②の実装）**: オーケストレーション方式（Fable 5指揮＋Sonnet実装＋本体が実コードでレビュー）でセッション30の合意2件を実装した（**コミット 482597a・app.js v105 / style.css v49・デプロイ済み・実機確認待ち**）。**【①リアクションの全体演出化】** タイル内浮上（overflow:hiddenでクリップ・プレゼンターモードで見えにくい）をやめ、`#reaction-overlay`（position:fixed・inset:0・**pointer-events:none**・overflow:hidden・**z-index:9000**＝チャットボトムシート60より上／トースト9999・モーダル10000・各パネル10100より下＝通知や確認ダイアログを決して覆わない。CSSコメントにz-index一覧を明記済み）へ描画する方式に変更。1リアクション＝`.reaction-overlay-item`（絵文字44px＋**送信者名タグ**＝0.7em・半透明黒ピル白文字・max-width:160px省略）。名前解決はリモート=`usernames.get(senderId)||'ユーザー'`／ローカルエコー=`this.username`、**いずれもtextContent挿入（XSS不可）**。アニメ＝`@keyframes reaction-overlay-rise` 2.8秒で bottom:100px（コントロールバーの上）→ -92vh（画面上端）、`--sway`カスタムプロパティ（±20〜30pxランダム）で左右ゆらぎ・15%時点で1.15倍ポップ・終盤フェードアウト。水平開始位置は画面幅15〜85%ランダム（連打時の重なり防止）。同時最大12個（超過は最古を即削除）・`animationend`＋3.3秒保険タイマーの**冪等remove**（DOM残骸なし）。許可リスト検証・200ms送信スロットル・ローカルエコーは不変。旧`.reaction-float`/`reaction-rise`/`showReactionOnTile`は完全削除（grepで残存ゼロ）。iPhoneネイティブ全画面中は見えない制約は従来どおり許容。**【②コントロールバー再構成】** 常設6個＝左から**カメラ・マイク・画面共有・リアクション・チャット・退室**＋右端に**「その他…」ボタン**（`#more-btn`・三点リーダーSVG・グレー#6b7280）の7スロット（現行と同幅＝既存レスポンシブgap規則（≤768:4px/≤480:2px/≤360:gap0・40px）がそのまま活きることを確認済み）。`#more-menu`は縦リスト（行＝アイコン＋ラベル・min-height:44pxタップターゲット・将来の録音/ホワイトボード/ルームロック等は行追加だけで拡張可能な汎用構造）。背景フィルターは`#bg-filter`のid・クリックハンドラ・`.active`トグルを維持したままメニュー内の行へ移動。**アンカー変更**＝filter-panel/bg-image-panelの位置基準を、メニューが閉じるとrectが全て0になる#bg-filterから**常時表示の#more-btn**へ変更。reaction-panelの画面端クランプ処理を`clampPanelToViewport()`にヘルパー化し、filter-panel/more-menu/bg-image-panelにも適用。プリコールの`#precall-filter-btn`系は一切無変更。**【本体レビューでの修正2件】** (1)**bgImagePanelのクランプ欠落（実バグ）**＝アンカーが右端の#more-btnに変わったのにクランプが無く、375pxで「背景画像」を選ぶとパネル（幅254px）が右へ約93pxはみ出す→`showBgImagePanel`にクランプ追加（クランプ後right=367<375を確認）。(2)**フィルターON表示の消失（UX退行）**＝JSが#bg-filterへ付け外しする`.active`の旧スタイル`.control-btn.filter.active`は`.more-menu-item`には効かない→`.more-menu-item.active`（ティール#0f766e＋白文字）を新設（旧ルールはprecall-filter-btnで現役のため残置が正解と確認）。**【検証】** `node --check` OK・diff精査で仕様外変更なし（3ファイルのみ・+228/-55行）・Chromiumプレビューで**テスト14項目全PASS**（ローカルエコーの絵文字＋名前タグ・200msスロットル・許可リスト外絵文字（scriptタグ/任意文字列）の無視・悪意ある送信者名がHTML化されない・最大12個キャップ・2.8sアニメ適用・3.3s後の自動除去・375px/320pxで7ボタン1行（右端368/313＝画面内）・その他メニュー/フィルターパネル/背景画像パネルの開閉と画面内クランプ・外側クリックで全パネル閉止・activeティール表示）＋スクリーンショットで演出良好（絵文字＋名前ピルが昇り上部でフェード・長い名前も省略）・コンソールエラー/警告ゼロ。**Safari15制約**（`:has()`/color-mix/oklch不使用・新規svhなし・インライン`<style>`との競合なし）遵守。**注意**＝データチャネル越しの実端末間配送とSafari実機のアニメ挙動はプレビューでは検証不能→実機で要確認。**次回最優先＝実機確認**（下記チェックリスト）→全OKなら`v4.6-stable`付与→セッション31（ボタン並べ替え・左利き対応）へ。

**（旧）進捗状況（セッション29／④の実装）**: オーケストレーション方式（Fable 5指揮＋Sonnet実装＋本体が実コードでレビュー）でセッション27統合タスクリストの④を実装した（**コミット 5578372・app.js v103 / style.css v47・デプロイ済み・実機確認待ち**）。**【リアクション】** コントロールバーの「フィルター」と「チャット」の間に「リアクション」ボタン `#reaction-btn`（スマイルSVG・ターコイズ#0891b2）を追加。クリックで `#reaction-panel`（filter-panelと同作法＝position:fixed・ボタン上に中央揃え・外側クリックで閉じる・stopPropagation）を開き、絵文字6種（👍👏😂🎉❤️😮 3×2グリッド・48px角）をタップで送信。送信＝`broadcast({type:'reaction', emoji})`（チャットと同経路のデータチャネル）＋**自タイルに即ローカルエコー**（broadcastは自分に届かないため）。**200msスロットル**で連打による帯域詰まりを防止。パネルは絵文字タップ後も開いたまま＝連続送信しやすく（意図的）。受信＝`handleDataMessage` に `case 'reaction'`、**6絵文字の許可リスト検証**（任意文字列を画面に流させない防御。textContent挿入なのでXSSも不可）。表示＝`showReactionOnTile(id, emoji)` がタイル内に `span.reaction-float` を生成し `@keyframes reaction-rise`（1.8秒・下から-80px浮上＋20%時点で1.15倍に膨らみ＋フェードアウト）→ `animationend`＋2.2秒保険タイマーの**冪等removeで確実に除去**（DOM残骸なし）。水平位置は±30pxランダムで連打時の重なり防止、**1タイル最大8個**（超過は最古を即削除）。`.video-container` は overflow:hidden なのでタイル内でクリップ＝仕様どおり。**【挙手】** パネル下段に「✋ 挙手」トグル `#hand-toggle-btn`（挙手中＝「✋ 挙手をやめる」＋アンバー#fbbf24のactive表示）。実装は**mute-stateと完全に同型**＝`handStates` Map保持・`broadcast({type:'hand-state', raised})`・`conn.on('open')` で新規参加者へ初期状態送信（user-joinの後なので受信側でユーザー名解決済み）・conn close/error両ハンドラで `handStates.delete`・`hangup()`/`createRoom`・`joinRoom` catchで `clear()`＋`isHandRaised=false`＋ボタン表記リセット。表示＝タイル**左上**に✋丸バッジ `.hand-indicator`（30px・アンバー半透明。muteインジケーター=右下・ラベル=左下と重ならない配置。`.hidden` は `!important` なので display:flex に勝つ）。挙手時のみ「◯◯さんが挙手しました」トースト（`showStatus` の既存 `toast-connected` 緑）。プレゼンターモードのサムネイルでも表示されることを確認済み。**【本体レビューでの修正2件】** (1) `showReactionOnTile` の第一セレクタ `#video-${id} .video-container` は決してマッチしない死にコード（`#video-${id}` 自身がコンテナ）→ `getElementById` 単独に簡素化。(2) **375px幅（iPhone SE3）でパネル右端が3pxはみ出す**のをプレビューのモバイルリサイズで発見→ 表示後に実寸を測り左右8pxマージン内へ寄せるクランプ処理を追加（クランプ後 right=367<375 を確認）。**【検証】** `node --check` OK・diff精査で仕様外変更なし（3ファイルのみ・+279/-2行）・Chromiumプレビューで**ロジックテスト11項目全PASS**（ローカルエコー・スロットル・許可リスト外絵文字（scriptタグ/任意文字列）の無視・挙手受信でバッジ＋トースト・解除でバッジ消灯・自分のトグル往復とボタン表記/active切替）＋パネル開閉・スクリーンショットで見た目良好・コンソールエラー/警告ゼロ。**Safari15制約**（`:has()`/color-mix/oklch不使用）遵守。`backdrop-filter` 無プレフィックスは既存 `.mute-indicator` と同作法（Safari15では単に効かないだけの装飾＝背景色があるので視認性は保たれる）。**注意**＝データチャネル越しの実端末間の送受信はプレビューでは検証不能→実機で要確認。**既知の許容事項**＝`showStatus` は新トースト表示時に既存トーストを消すため、複数人がほぼ同時に挙手するとトーストは最後の1人分のみ表示（バッジは全員分正しく出る・既存仕様の踏襲）。**次回最優先＝実機確認**（下記チェックリスト）→ 全OKなら `v4.5-stable` 付与。**【→ 同日の実機テストで不具合3件の報告→修正（コミット 3aaa04c・app.js v104 / style.css v48・デプロイ済み・再実機確認待ち）】** 実機結果＝リアクション・挙手の機能自体はOK（「後は大丈夫なよう」）。**報告①（最重大：退室・入室を繰り返すと実人数2〜3人なのに「ルームは満員です」で再入室不能・iPhone/iPad両方で発生 IMG_8469）**: 真因＝**ゲストの退室が無通知**（`conn.close()`のみ＝受動的ICE切断検知頼み。ホストだけはセッション28で `room-closed` 明示送信になったがゲスト側は未対応だった）。iPhoneはICE切断検知が数十秒遅れ・旧Safariは発火しないため、ホストの `connections` Mapに退室済みピアの**ゴーストが蓄積**→定員判定 `connections.size >= 5` に達して満員誤判定。修正3段構え＝(a)`hangup()` でゲストは `broadcast({type:'peer-leaving'})` を明示送信（room-closedと同じ設計の水平展開・SCTPフラッシュで配送保証）、受信側は新設 `cleanupPeer(peerId)` で即時片付け（conn/call close＋全Map削除＋タイル除去＋共有者ならexitRemotePresenterMode＋ファイル転送中断。senderId本人しか消せないので詐称不能。後から届く遅延close/errorはMap不在とcallのidentityガードで実質no-op）、(b)index.htmlの `beforeunload` でもベストエフォート送信（ホスト=room-closed/ゲスト=peer-leaving＝タブ閉じゴースト対策）、(c)ホストの定員判定前に `connectionState` が failed/closed の接続を `cleanupPeer` で掃除（クラッシュ等で通知が届かなかった最後の穴。'connecting'や状態が取れない環境には触れない安全設計）。**報告②（iPhoneの全画面縮小版＝PiPプレイヤー残骸が再発 IMG_8467）**: 真因＝IMG_8467は**PiP状態**のネイティブプレイヤー（右上PiPアイコン・LIVEバッジ）。iPhoneのネイティブ全画面中にホームへスワイプするとiOSが**自動でPiPに移行**するが、PiP中は `webkitDisplayingFullscreen` が **false** になるため、セッション28で入れた「endfullscreen待ち」分岐を**素通り**して即時 `srcObject=null` が走り、同じメカニズムで残骸発生。修正2段構え＝(a)`exitRemotePresenterMode` にPiP分岐を追加：`webkitPresentationMode === 'picture-in-picture'` なら `webkitSetPresentationMode('inline')` を呼び **`webkitpresentationmodechanged`（inlineになった時）を待ってから片付け**（2秒保険タイマー・done二重実行防止・新共有ガードは既存踏襲）、`exitScreenShareFullscreen` にもPiP脱出を追加、(b)**入口を塞ぐ**＝`#screen-share-video` に `disablepictureinpicture` 属性を付与（対応SafariではPiPボタンが消え自動PiPも抑止。非対応でも(a)が出口で対処）。※ユーザーの端末に既に残った残骸はiPhone再起動で消える。**報告③（SE3でコントロールバーの「退室」が2行目に折り返し左右中央に表示）**: 真因＝ボタン7個化で幅超過だが、直接原因は**index.htmlのインライン`<style>`の `@media(max-width:480px){.controls{gap:10px}}` が外部style.cssを後勝ちで上書き**していたこと（インラインstyleは外部CSSより後に読まれるため同一詳細度では常に勝つ＝セッション25で「値の矛盾なし」とされたスタイル分散リスクが今回実害化）。修正＝インライン側のgap指定を撤去しstyle.cssに一本化（≤768px:4px／≤480px:2px＋ラベル0.62em／≤360px:gap 0＋ボタン40px＋ラベル0.56em。360px以下はインラインの`.control-btn`46px指定に勝つよう`.controls .control-btn`で詳細度を上げた）。**検証**＝`node --check` OK・Chromiumプレビューで (a)375px/320pxとも7ボタン1行（右端368/313px＝画面内）(b)peer-leaving受信でconn/call close＋全Mapクリア＋タイル除去、定員判定前のゴースト4体掃除＋新規受理＋生存接続・connecting中は無傷、ゲストhangupがpeer-leaving送信（room-closedは送らない）(c)PiPスタブでsetPresentationMode('inline')呼び出し・イベント到着まで片付け保留・到着後に完了・待機中の新共有保護、**全項目PASS**・コンソールエラーゼロ。**再実機確認待ち**（PiP実挙動とデータチャネル実配送は実機でしか検証できない）。

**（旧）進捗状況（セッション28／②＋③の実装）**: オーケストレーション方式（Fable 5指揮＋Sonnet実装＋本体が実コードでレビュー）でセッション27統合タスクリストの②③を実装した（**コミット 9c55c2c・app.js v101 / style.css v46・デプロイ済み・実機確認待ち**）。**【②画面共有フルスクリーン】** `#screen-share-container` 右下に⛶ボタン `#screen-fullscreen-btn` を追加（左下の stop-share-btn／share-viewer-label と位置競合なし）。API分岐は設計どおり3系統＝(a)標準 `requestFullscreen()`（`document.fullscreenEnabled` で判定・Mac/iPad/Windows）→(b)旧Safari `webkitRequestFullscreen()`（`webkitFullscreenEnabled` で判定・2011 MBA等）→(c)iPhone `video.webkitEnterFullscreen()`（要素フルスクリーン非対応のためネイティブ全画面＝**自動横回転**付き。未再生時のInvalidStateErrorはtry/catchで握る）。**全画面対象はvideoでなくコンテナ**なので⛶ボタン自身が全画面内に残り終了トグルとして機能する。`fullscreenchange`/`webkitfullscreenchange`（document登録）で `is-fullscreen` クラス＋title/aria-labelを同期＝**Escや標準UIでの終了も拾う**。アイコンは拡大/縮小2枚のSVGをCSSで切替。videoが `hidden`（プレースホルダー表示中）の間は後続兄弟セレクタ `#screen-share-video.hidden ~ .screen-fullscreen-btn { display:none }` でボタンごと非表示＝**JSでの表示管理が不要**（`handleCall` の遅延stream到着でvideoが復活するケースも自動追従）。**後始末** `exitScreenShareFullscreen()` を新設し、`exitRemotePresenterMode` 先頭＋`stopScreenShare` のガード直後で呼ぶ＝**共有終了・退室時に全画面のまま取り残さない**（iPhoneネイティブ全画面は `webkitDisplayingFullscreen` を見て `webkitExitFullscreen()`）。CSSの `:fullscreen` と `:-webkit-full-screen` は**カンマ結合せず別ルール**（未知の擬似クラスがセレクタリスト全体を無効化するSafari15対策）。**【③通話中ヘッダー圧縮】** CSSのみ・JS変更ゼロ（in-callクラスは既存JSが付け外し済み）。`.container.in-call .header { padding:3px 16px }`＋`.container.in-call .logo { font-size:0.9em }`（通話中は全画面幅で圧縮＝8px→3px）、さらに `@media (max-height:700px)` で**通話中のみヘッダー完全非表示**。該当端末＝iPhone横(≈390px)・SE3縦(≈550-660px)・2011 MBA 11インチ(≈630-650px)・iPad横(≈690px)。M4 MBA(≈790px+)は圧縮表示のまま。**しきい値700pxは実機の見え方で調整可**。通話外（ウェルカム画面）は従来どおり。**【潜在バグ修正（本体レビューで発見・今回同時修正）】** 他人の共有を**視聴中に退室**すると、`hangup()` が `currentRemoteSharerId` を先に null にするため conn close ハンドラのガード（`currentRemoteSharerId === conn.peer`・app.js:667）が効かず `exitRemotePresenterMode()` が一度も呼ばれない→ `screen-share-container` の表示と `presenter-mode` クラスが残留し**再入室時にプレゼンターモード固着**（他に container を隠すコードパスなし＝grep で全5参照を確認）。フルスクリーン導入後は「全画面のまま取り残される」形に悪化するため今回修正＝`hangup()` の `stopScreenShare` 呼び出し直後に `if (this.currentRemoteSharerId) this.exitRemotePresenterMode();` を追加（正規フローはガードで不変）。**【検証】** `node --check` OK・diff精査で仕様外変更なし（3ファイルのみ・+124/-2行）・Chromiumプレビューで (a)⛶ボタンの存在/右下配置/z-index/アイコン切替(is-fullscreen)/video非表示時の連動非表示 (b)ヘッダー圧縮(padding 8px→3px・logo 0.9em)＋高さ390pxでin-call時のみdisplay:none・通話外は通常表示 (c)hangup残留修正でcontainer隠蔽＋presenter-mode除去＋sharerIdクリア、の全項目OK。**注意**＝プレビュー環境はFullscreen API自体が権限ポリシーでブロック(`Permissions check failed`)されるため**全画面遷移そのものは実機でしか確認できない**（拒否時に例外なく安全に握りつぶされることは確認済み）。**次回最優先＝実機確認**（下記チェックリスト）→ 全OKなら `v4.4-stable` 付与。**【→ 同日の実機テストで不具合2件の報告→修正（コミット 77292b0・app.js v102・デプロイ済み・再実機確認待ち）】** 実機結果＝③ヘッダー圧縮OK・共有者側⛶OK。**報告①（iPhoneだけ共有終了時に全画面がキャンセルされず真っ黒＋iOSにプレイヤー残骸が残留）**: 症状＝共有終了で黒画面のまま／退出→再入室しても共有映像が黒い(IMG_8461)／Safariを完全終了してもホーム画面に黒いネイティブプレイヤーの残骸が浮く(IMG_8464/8466)。真因＝`exitRemotePresenterMode` が `webkitExitFullscreen()`（**非同期**の終了アニメーション開始）を呼んだ**直後に同期的に** `srcObject=null`＋`classList.add('hidden')`(display:none)を実行しており、iOSのネイティブ全画面プレイヤーは表示中のvideoを即座に隠す/空にすると**終了処理が中断されて宙に浮く**(WebKitの既知挙動)。残骸プレイヤーがデコーダを掴んだままになるため再入室後の共有映像まで黒くなる（Safari終了でも消えない＝システム層の残骸。**ユーザーの端末に残った残骸はiPhone再起動で消える**）。修正＝iPhoneネイティブ全画面中(`webkitDisplayingFullscreen`)は片付け一式を `finish()` に括り、`webkitendfullscreen`（終了完了イベント）を待ってから実行（2秒の保険タイマー付き・`done`フラグで二重実行防止・`currentRemoteSharerId` ガードで待機中に始まった新しい共有を壊さない）。非全画面/要素全画面(Mac/iPad)は従来どおり即時同期。**報告②（全画面のままホスト退出→iPad Air2=即復帰・iPhone=数十秒遅れて復帰＋ホームに黒残骸・2011 MBA=入室したまま戻らない）**: 真因＝ホスト退出検知がconn close/error（＝受動的な**ICE切断検知**）のみで、iPhoneはICEタイムアウト待ちで遅延、2011 MBAの古Safariはcloseイベント自体が発火しない。修正＝`hangup()` 冒頭でホストなら `broadcast({type:'room-closed'})` を**明示送信**（チャットと同経路のデータチャネル＝全端末で確実・即時。後続の `conn.close()` はSCTP仕様で送信バッファをflushしてから閉じるため配送される）。受信側は `handleDataMessage` に `room-closed` ケースを追加し `!isLeaving && !isHost && senderId === roomId` のみ受理（なりすまし防止）。**タブ閉じ・クラッシュ時は従来のICE切断検知がフォールバック**として残る（2011 MBAはこの経路が効かないままだが、明示退室が主経路になるため実用上解消。残る穴はホストのクラッシュ時のみ＝将来ハートビートで対応可）。iPhoneの黒残骸はこの遅延中に報告①と同じメカニズムで発生していたため①の修正で同時に解消される見込み。call close/errorへのホスト検知追加は、一時的なメディアICE断で誤ってルーム終了させるリグレッションリスクがあるため**見送り**。**検証**＝`node --check` OK・Chromiumプレビューで単体テスト11ケース全PASS（room-closed受理/偽装無視/ホスト自身無視/isLeaving無視・非全画面パスの即時性・iOSスタブでの遅延片付け/endfullscreen後の実行/保険タイマー/staleタイマーが新共有を壊さない）・コンソールエラーなし。**再実機確認待ち**。

**（旧）進捗状況（セッション27／設計相談・実装なし）**: セッション26の相談の続き。実装ゼロ・コード変更なし（安定版 `v4.3-stable` 不変）。**【ルームロックの設計】** 部屋の入口はホスト1箇所（ルームID＝ホストPeer ID／参加者は必ず先にホストへ接続し `peer-list` を貰ってメッシュ形成／定員チェックも `handleConnection` の1箇所）。よって**ロックはホストの門番に数行足すだけ**で成立＝`this.roomLocked` を持ち、`handleConnection` の定員チェック隣(app.js:587付近)で `if (this.isHost && this.roomLocked){ conn.on('open',()=>{send 'room-locked'; close}); return; }`、受け手は `handleDataMessage` に `room-locked` を1ケース追加、UIはホスト限定の🔓/🔒トグル。**約30分・全端末・低リスク・データチャネル不要**。限界＝暗号的防御ではなく"うっかり侵入"防止（公開されてる入口はホストIDだけなのでそこを閉めれば実質塞がる）。設計判断2つ＝(a)再接続の扱いはv1は「ロック＝全部拒否」のシンプル版を推奨、(b)ロック(開閉トグル・軽)と待機室(参加ごと承認・中規模)は別物でまずロックで十分。**【複数ルーム/配布の確認】** 関わるサーバーは2種＝①GitHub Pages＝アプリを配るDL置き場(ルーム状態は持たない)、②PeerJSクラウド(`0.peerjs.com`)＝相手を見つけるシグナリング(`new Peer(id,{debug:0})`＝ホスト指定なしで無料共有サーバー使用)。ルームID＝`generateRoomId()`の9文字ランダム英数字(36⁹≈約100兆)で衝突なし→**URLを知る友人2人が同時に「ルーム作成」しても別々の独立ルームができ干渉しない**(同室になるのはIDを共有して片方が参加した時だけ)。**複数ルームは何個でも並行可・GitHub側に制限なし・アプリにも制限なし**。真の天井＝無料PeerJS共有サーバー(同時接続/レート制限あり・稼働保証なし・数値非公開)。ただしシグナリングは"最初の握手"だけでメディアはP2P直結→サーバー負荷は一瞬。混雑/停止時は**新規のルーム作成・参加が失敗**(`initializePeer` 10秒タイムアウト app.js:405)、**既存通話は生存**。→ 友人配布は余裕、**公開フェーズでは単一障害点＋TURN未設定(厳しいNAT下は繋がらない事あり)が穴**。将来は自前PeerServer/TURNの検討余地。**配布＝URL 1本だけ**(友人はコード コピー/デプロイ/インストール不要／同じ1つのアプリを開く／デプロイはあなたのpush時だけ・友人は何もしない・キャッシュバスターで最新配信)。**【UI相談→本命入り2件】** 小画面(特に2011 MBA 11インチ・iPhone横)で縦が窮屈な件。**(1)画面共有フルスクリーンが本命**＝共有は専用 `<video id="screen-share-video">`(index.html:56・見る側は enterRemotePresenterMode app.js:1316 で流し込む)＝**本物のvideo要素なのでApple全端末で実現可**。3端末分岐＝①Mac Safari/Chrome＋iPad→`requestFullscreen()`(2011 MBAの古Safariは `webkitRequestFullscreen()`)、②iPhone→要素単位API非対応だが `video.webkitEnterFullscreen()` でネイティブ全画面＋**自動横回転**(縦長iPhoneで横長デスクトップ共有が激変に読める)。**最大の利点＝フルスクリーンはDOMに被さるだけでグリッド再配置しない→過去に苦しんだSafari15グリッド固着地雷を完全回避＝低リスク**。入口は共有画面隅に⛶ボタン推奨(共有者の「クリックで停止」と競合しない)。iPhoneはネイティブ全画面中に純正プレイヤーUIが被る(許容)。**(2)通話中ヘッダー圧縮/非表示**＝`.header`(ComChatロゴ帯 index.html:16)が通話中も居座り、映像が上(ヘッダー)と下(コントロール＋ルーム情報)から挟まれる。小画面では通話中だけヘッダーを細く/隠すと"上"の圧迫が消える(ヘッダーはグリッド外で低リスク)。フルスクリーン(下=共有時)＋ヘッダー圧縮(上=通話時)で上下とも解決。**(3)ルームID伏字化**＝`room-info` にID常時表示(index.html:142)→画面共有/スクショで漏れ第三者侵入→「普段は伏字・クリックで表示」でロックと合わせ技。**保留**＝英語主見出し(index.html:25「The Web Conference Room for the rest of us.」日本人友人向けは任意で日本語化検討)・デスクトップのチャット初期320px占有(好み)。**【セッション26＋27の統合タスクリスト(本命=優先度順)】** ①端末検証(Surface＋友人ベータ・非コード・並走・最優先) ②画面共有フルスクリーン(小・低リスク・小画面一次課題の筆頭) ③通話中ヘッダー圧縮(小・低リスク・②とセット) ④リアクション＋挙手同梱(小・低リスク・合意済み・配布を盛り上げ) ⑤ルームロック(約30分・低リスク) ⑥ルームID伏字化(小・⑤と守りセット) ⑦共有メモ(中・議事録ゴール直撃だが6人同期/後入り再送/undoの仕様詰め要・⑧と役割重複) ⑧音声録音(中〜重・合意済み・最後)。**将来候補(本命外)**＝待機室(⑤で当面足りる)・ホワイトボード(中規模・議事録へは⑦の方が近い)。**不採用(確定)**＝ブレイクアウト(重)・AI要約/翻訳(柱破壊)・ブラウザ内Whisper(弱端末死)・Web Speech字幕(Chrome限定＋Google送信でP2P毀損)・文字起こしのアプリ内実装(用途=会議後議事録→アプリ外MacWhisperに確定)。**推す着手順**＝②→③(小画面上下解決)→④(配布盛り上げ)→⑤＋⑥(公開の守り)→⑦ or ⑧(議事録ゴール・二択)、①は全体並走。**守るべき4本柱**＝完全無料(メディアサーバー無し)・P2Pプライバシー・インストール不要・弱端末対応。**セッション27はここまで(実装なし・記録のみ)**。

**（旧）進捗状況（セッション26／設計相談・実装なし）**: ユーザーと**今後の開発方針**を協議し確定した（実装は未着手・コード変更ゼロ）。**大前提**＝まず友人に配布→将来フリーウェア公開の可能性。守るべき4本柱＝完全無料(サーバー無し)・P2Pプライバシー・インストール不要・弱端末でも動く。重い機能は「使う時だけ動的import」で0コスト化する既存作法(背景フィルターのMediaPipe)に乗せる方針。**【文字起こしの結論】** 用途は「会議後に残す議事録テキスト」。→ **アプリ内は音声録音だけにする／文字起こしは会議後にアプリ外(例: MacWhisper＝Mac無料枠・ローカル完結)で行う**。理由＝(a)ブラウザ内WhisperはモデルDL数十〜百MB＋常時推論で弱端末が死ぬ(背景フィルター地獄の再来・より重い)、(b)Web Speech字幕はChrome系のみ＋音声をGoogleに送るためP2Pの売りを毀損、(c)サーバー文字起こしは従量課金＝「完全無料」の柱を壊す。生成コスト(録音=軽/文字起こし=重)と保存コスト(録音=重/テキスト=軽)は逆で、テキストが軽いのは"保存"の話に過ぎない点を共有。**【録音機能の仕様】** 音声のみ(採用)。全員の声をミックスして録音＝既存 `buildMixedAudioTrack`/AudioContext(画面共有の音声ミックス実績)を流用。実装時は**「録音中」表示＋開始通知(同意)を必ず入れる**前提(勝手に録れると怖いため)。**【端末検証の現実解】** 実機テストは全てApple製(iPhone SE3・iPad Air2縦・M4 MBA・2011 MBA)＋PCで、**Android/Firefox/Windowsが完全に未検証**＝フリーウェア公開に向けた最大の穴。ユーザー環境＝**Androidデバイス無し／手元に5〜6年前のWindows Surfaceあり**。→ Windows(Chrome/Edge/Firefox)はSurfaceで確認(弱端末テストとしても好都合)、**AndroidはデバイスなしなのでAndroid Chromeは友人ベータで拾う**(どうせ友人配布が最初＝既存の `diag.html` でGPU/ブラウザ情報を集めて切り分け)。補足＝P2Pメッシュのため各自が(人数-1)を送受信、快適な実用人数は3〜4人・6人は上限、という期待値設定。**【機能の優先順位(合意)】** ①端末検証(Surface＋友人ベータ) → ②リアクション(絵文字をデータチャネルで飛ばす＝極軽・全端末・低リスクの当たり、チャットと同経路で実装も素直) → ③音声録音。**【追加候補の棚卸し(次回以降の地図)】** データチャネルに乗る軽い協調機能が4本柱と最高に相性◎。**推し＝共有メモ(みんなで同時に書く議事録テキスト・軽・全端末＝議事録ゴールに直撃、録音の補完/代替になりうる)**。次点＝挙手(極軽)、**ホワイトボード**(過去候補と記憶。Excalidraw等の重ライブラリは使わず素のHTML canvas＋線をデータチャネル配信ならDL0・全端末pointer events・P2P相性◎で実現可。ただし6人分の同期・後入り者への現状再送・消去/undoの作り込みで中規模の手間)、**待機室/ルームロック**(ルームID＝ホストPeer IDで知られたら誰でも入れる→公開に向けた安全弁)。**対象外/後回し**＝ブレイクアウトルーム(設計が重い)、AI要約・翻訳(サーバー/重モデルで柱を壊す)、ブラウザ内Whisper文字起こし・Web Speech字幕(前述)。**音声品質**＝`getUserMedia({audio:true})` はブラウザ既定でノイズ抑制/エコーキャンセルがONのため明示指定は保険程度で目玉にならない、と確認済み。**次回**＝②リアクションの実装 or ①共有メモの詰めから(ユーザーの選択待ち)。**セッション26はここまで(実装なし・記録のみ)**。

**（旧）進捗状況（セッション25）**: 恒例の**全コードのバグ調査**をオーケストレーション方式で実施（Sonnetサブエージェント2体並列＝①app.jsロジック精査②ファイル横断整合性点検、報告は本体が実コードで裏取りして誤検知を排除）。**致命的バグはゼロ**、確定所見3件＋掃除1件をすべて修正した（**コミット df2cc9d・app.js v100・実機確認は未実施**）。**所見①（高・修正済み）**: `handleCall` は同一ピアからの新callが来ると旧callを `close()` して置き換えるが（695-697行）、旧callの遅延close/errorイベントが**ピアIDだけをキーに** `calls.delete`＋`removeVideoElement` を実行し、**生きている新callの登録・映像タイル・発話解析を誤って消す**（再接続・瞬断後の再ダイヤルで発生。以後 `calls.forEach` によるトラック差し替え＝画面共有/背景フィルターがそのピアをスキップし古い映像のまま固着）→ close/error/streamの3ハンドラ先頭に `if (this.calls.get(call.peer) !== call) return;` のidentityガードを追加（streamは旧callの遅延streamによるタイル上書き防止）。**所見②（高・修正済み）＋③（中・同根）**: `applyBgFilter` の初回セットアップは `grabFrame`/`loadeddata`/`initSelfieSegmentation`（モデルDLで数秒）を await で待つが**再入ガードが無く**、待機中に「なし」→再設定と素早く操作すると2つのセットアップが並走（rAFループ二重化=CPU倍で片方止められない・captureStreamトラック孤立・cleanup済みでnullの `bgSourceVideo.videoWidth` 参照でTypeError→誤フォールバック。破綻自動オフ `handleBgFilterBreakage` 経由の 'none' とユーザー操作の競合＝所見③も同根）→ **世代トークン `_bgFilterGen` を導入**。genを進めるのは**'none'パスと初回起動パスのみ**（`wasActive=true` の型変更軽量パスは触らない＝blur→image変更中のセットアップを中断させないため）。各await後とcatch冒頭で `gen !== this._bgFilterGen` ならreturn（**stale側は何も片付けない**＝中間生成物は'none'側の `cleanupBgFilterResources` が回収済みで、下手に触ると後続呼び出しの生成物を壊す。旧2254行の `bgFilterType==='none'` チェック＋cleanup一式はgenチェックに置換。segmenterが残っても次回 `initSelfieSegmentation` が再利用 or hangup時cleanupで回収）。**掃除④（低・修正済み）**: index.htmlインライン`<style>`のデッドCSS（`.loading`/`.notification`/`.recording`＋`@keyframes spin`/`pulse`/`slideIn`、JS/HTMLから参照ゼロをgrepで確認）を削除。**クリーンだった項目**: ID/クラス整合（54要素すべて対応）・Safari15対応（`:has()`なし・svh二段書き完備・color-mix/oklch不使用）・innerHTML安全性（全6箇所静的orクリアのみ）・ターコイズ置換漏れゼロ・キャッシュバスター整合・`attemptReconnect`/`connectToHost`/`waitForBuffers`/`teardownSpeakingDetection`等の非同期整合。**見送り（対応不要と判断）**: スタイルがstyle.cssとインライン`<style>`に分散している構造的指摘（値の矛盾なしを確認済み・動作バグではない）、stale退場時にsegmenterインスタンス1個が一時残置されうる極小ケース（hangupで回収・発生条件が極端に狭い）。**検証**: `node --check` OK・diff精査で仕様外変更なし・**単体テスト19ケース全PASS**（①stale close/error/streamが新callに不干渉＋正規イベントは正常動作、②'none'割り込みでAのvideo回収・stale Aが退場しCの生成物無傷・ループ非起動・軽量パスはgen不変）。**→ 2026-07-04 実機確認3項目すべてOK（①通常通話の回帰②フィルターの素早い切替③フィルターの回帰、いずれも問題なし）→ `v4.3-stable` を df2cc9d に付与・push・正式確定。セッション25完了**。

**セッション36追記（同日・M4 MBA 1台での録音実機確認）**: 録音→.m4a保存→**クイックルックで再生し録音成功を確認**（機能は正常動作）。所見3件＝(1)ダブルクリックで開くと**ミュージックappに取り込まれるが再生できない**→MediaRecorderのaudio/mp4出力がフラグメント化MP4(fMP4)であるための既知特性・ファイル自体は正常（クイックルック/QuickTime/ブラウザでは再生可）・アプリ側の対処はコーデック変更かリマックスが必要なため現状は仕様として許容。(2)macOSシステム設定のサウンド入力レベルメーターが無反応→ComChatとは無関係の環境要因としてユーザーが自分で確認予定。(3)**mp3化の相談→不採用で確定**（MediaRecorderはmp3非対応・JSエンコーダーは弱端末の柱に反する・そもそもAAC/Opusの方がmp3より圧縮効率が良くmp3化はサイズ増）。**ビットレート指定（64kbps軽量化）も一度実装したがユーザー判断で取り消し・ブラウザ既定のまま**（作業ツリーはb3dd362＝v112と完全一致・本番無変更）。複数台での録音検証（通知・途中参加・iPhone保存挙動）と後勝ち検証は引き続き持ち越し。

### ✅ セッション38の実機確認チェックリスト（599ee07・app.js v114 / style.css v55・**全項目OK → `v4.9-stable` 確定**）
1. **共有者タイルの在席表示（新方式）**: 誰かが画面共有→視聴側では共有者のタイルが**黒地＋中央に名前**になり（共有画面の2重表示が消える）、ミュートアイコン・名前ラベルは見え続ける。**共有音声（タブ/システム音声）は聞こえ続ける**（Mac同士で音声付き共有を試すのが確実）
2. **後勝ち交代**: A共有中にBが共有開始→Aのタイルがカメラ映像に復帰し、Bのタイルが黒地＋名前になる
3. **共有終了**: 共有者のタイルがカメラ映像に復帰し全員のグリッド表示に戻る
4. **共有者自身の画面**: 自分のタイルは従来どおりカメラのまま（自分側は変化なし）
5. **ミュートアイコンの再同期（未報告）**: iPad/iPhoneをホーム画面に出す→ComChatに戻る、の間に他の端末でミュート切替→**戻った直後にアイコンが正しい状態になる**（挙手・カメラOFF・録音中でも同様）
6. **回帰**: 通常の通話・ミュート切替の即時反映・チャット等が従来どおり
- 全項目OKなら `v4.9-stable` を 599ee07 に付与（録音複数台・後勝ち・6ボタン化・文字選択防止は確認済み）

### ✅ セッション37の実機確認チェックリスト（01a6b8e・app.js v113 / style.css v53・**実機確認済み＝6ボタン化・文字選択防止OK。共有者タイル2重表示とミュートアイコン欠落はセッション38で対応**）
1. **iPhone/iPadで画面共有ボタンが消えている**: コントロールバーが6ボタン（カメラ・マイク・リアクション・チャット・退室・その他）になり、間隔に余裕がある
2. **Mac/PCでは画面共有ボタンが従来どおり表示**され、共有の開始・停止も従来どおり動く（回帰確認）
3. **ボタン名の選択防止**: iPhoneでボタン名（カメラ・マイク等）を長押し/タップミスしても文字が選択状態にならない・虫眼鏡や長押しメニューも出ない
4. **フィルターパネルの選択肢**（なし/ぼかし/背景画像）も同様に文字選択されない
5. **ボタン並べ替え**: iPhoneで編集モード→ドラッグしても「ドラッグで並べ替え」の文字が選択されない・並べ替え自体も従来どおり動く（画面共有ボタンが無い6個での並べ替え）
6. **チャットは従来どおり本文をコピーできる**（選択防止の巻き込みがないこと）
- 全項目OK（＋下記セッション35の後勝ち・36の複数台録音もOK）なら `v4.9-stable` を 01a6b8e に付与

### ✅ セッション36の実機確認チェックリスト（dcbbbf0・app.js v112・**デプロイ済み・1台分は確認済み（録音成功）・複数台分は持ち越し**）
1. **録音の基本フロー**: その他メニュー→「録音を開始」→赤トースト「録音を開始しました」＋画面上部に「🔴 録音中 mm:ss」→「録音を停止」→ファイルが自動ダウンロードされる（Mac Safari=.m4a）→再生して**自分と相手の声が両方入っている**
2. **相手側の同意通知**: 録音を開始すると**他の端末全員**に「◯◯さんが録音を開始しました」トースト＋「🔴 ◯◯さんが録音中」インジケーターが出る・停止で消える
3. **録音中の退室**: 録音したまま退室→ファイルが保存される・他の端末のインジケーターが消える
4. **途中参加**: 録音中に新しい人が入室→その人にもインジケーターが見える・その人の声も以降の録音に入る
5. **マイクミュート中の録音**: 自分がミュート中は自分の声が入らない（仕様）
6. **iPhone/iPadでの録音**: 開始→停止でファイルが保存できるか（iOS Safariの保存挙動＝共有シート/ダウンロードを確認）
7. **メニュー表示**: その他メニューの順序＝並べ替え→背景フィルター→録音→（ホストのみ）ロック・録音中は行が赤くなる
- 全項目OK（＋下記セッション35の後勝ちもOK）なら `v4.9-stable` を dcbbbf0 に付与

### ✅ セッション35の実機確認チェックリスト（bef7077・app.js v111・**デプロイ済み・実機確認待ち＝Mac/PC 2台が揃ってから**）
1. **画面共有の後勝ち（今回の修正の本丸）**: Mac/PC 2台（例: M4 MBA＋2011 MBA）で、Aが共有中にBが共有を開始→**Bの共有に切り替わる**（Aは自動で共有停止・両者ともBの画面が表示され、表示が崩れたり消えたりしない）。**双方向**（A→B、B→Aの順番を入れ替えて）で確認
2. **後勝ち後の停止**: Bが共有を停止→全員がグリッド表示に正常復帰する
3. **通常の画面共有の回帰**: 誰も視聴していない状態からの共有開始・停止・「◯◯が共有中」表示・⛶全画面が従来どおり
4. **第三者視聴の回帰（3台あれば）**: A共有中にBが後勝ち→Cの表示がBの共有に正しく切り替わる
- 全項目OK（＋セッション34の残項目に問題なし）なら `v4.9-stable` を bef7077 に付与
- 参考: チャットのB-6閾値＝本文2000字/名前50字/表示300件/毎秒20通（境界値テストは任意）

### ✅ セッション34の実機確認チェックリスト（6539944・app.js v110・**実機確認済み＝後勝ちNG（セッション35で修正）・チャット境界値未検証（許容）・他は全てOK**）
1. **その他メニューの並び（UI要望）**: 「その他…」を開くと**最上部が「ボタンの並べ替え」**、次に「背景フィルター」、ホストなら「ルームをロック」
2. **iOS画面共有の明示メッセージ（項目4）**: iPhone/iPadで画面共有ボタンを押すと「この端末は画面共有の発信に対応していません（iPhone/iPad等）。PCからお試しください」と出る（真っ黒/無反応ではなく理由が分かる）。**画面共有の"後勝ち"は共有発信できる2台＝Mac/PC同士**（例: M4 MBA＋Windows Surface）で検証すること
3. **ファイル転送の回帰（B-5）**: 通常の画像/ファイル送受信が従来どおり成功する（境界検証で正規転送が壊れていないか）
4. **チャットの回帰（B-6）**: 通常のチャット送受信・名前表示が正常・長文でも表示が壊れない・大量投稿でも重くならない
5. **リアクションの回帰（B-6）**: 通常のリアクションが正常に流れる（レート制限で正規操作が弾かれない）
6. **ロック運用の確認（項目3）**: ロック中に元メンバーを戻したいときは、ホストが一時解除→入室→再ロックで対応できる（仕様どおり）
7. **通常通話の回帰**（背景フィルター＋カメラON/OFF＝A-1・並べ替え・挙手・ID伏字）
- 全項目OKなら `v4.9-stable` を 6539944 に付与
- **注意**: iOSの画面共有非対応・データチャネル越しのDoS防御の実効性は実機/複数端末でしか確認できない

### ✅ セッション33の実機確認チェックリスト（0f05a88・app.js v109・**実機確認: 概ねOK＝A-1/B-2/B-3/B-4/D良好。報告3件はセッション34で対応**）
1. **背景フィルター＋カメラON/OFFの回帰（A-1）**: フィルター（ぼかし/背景画像）ON中にカメラをOFF→ONを数回繰り返しても、映像が正常・端末が過度に発熱しない・fpsが落ちない（増殖ループが根絶されているか＝**弱端末で特に重要**）
2. **フィルターの素早い切替の回帰**: なし⇄ぼかし⇄背景画像を素早く切り替えても破綻しない（セッション25の世代トークンと今回のループ世代トークンが競合しないか）
3. **ルームロックの回帰（B-1）**: ロック中に新規参加が拒否される・既存メンバーの通話は無影響（正規UIからの参加者で確認。改造クライアント経路は実機では再現不要）
4. **画面共有の回帰（B-2）**: 共有開始→他の参加者に「◯◯が共有中」と正しく表示される・共有停止で解除される・共有の重なり（後勝ち）も従来どおり
5. **チャットの回帰（B-4）**: 発言者名が正しく表示される・名前変更後の発言も新名で表示される
6. **リアクションの回帰＋残像（D）**: リアクションが正常に流れる・**退室直後にウェルカム画面へ絵文字の残像が残らない**
7. **通常通話の回帰**（参加/退室の繰り返し・満員判定・挙手・ボタン並べ替え・ID伏字）
- 全項目OKなら `v4.9-stable` を 0f05a88 に付与
- **注意**: データチャネル越しの実端末間配送（B-1〜B-4の実効性）とカメラ実挙動（A-1の発熱）はプレビューでは検証不能なため実機確認が必須

### ✅ セッション32の実機確認チェックリスト（64ea76e・**全項目OK → `v4.8-stable` 確定**）
1. **ルームロックの表示**: ホストで「その他」メニューを開くと「ルームをロック」が見える。ゲストには表示されない
2. **ロックの効果**: ホストがロック→別端末から新規参加を試みると「このルームはロックされています」で入れない（既存メンバーの通話は無影響）
3. **ロック解除**: 解除後は新規参加が通常どおりできる
4. **アイコン/表示の連動**: ロック中は🔒＋ティール色＋「ロック中（タップで解除）」に切り替わる
5. **ルームID伏字**: 参加/作成直後、ルームIDが伏字（●●●…）で表示される
6. **タップで表示切替**: IDをタップすると実IDが見える→もう一度タップで伏字に戻る
7. **コピー機能の回帰**: 伏字表示中でも「ルームIDをコピー」ボタンで正しい実IDがコピーされる
8. **退室後リセット**: 退室→再度ルーム作成/参加で、ロックは解除状態・IDは伏字状態から始まる
9. **通常通話の回帰**（画面共有・リアクション・チャット・ボタン並べ替え）
- 全項目OKなら `v4.8-stable` を付与

### ✅ セッション31の実機確認チェックリスト（607e427・**全項目OK → `v4.7-stable` 確定**）
1. **編集モード突入**: その他メニュー→「ボタンの並べ替え」→ボタンが揺れ・ツールバー表示（iPhone/iPad/Mac）
2. **タッチドラッグ**: 指でボタンを掴んで左右に動かすと並べ替わる・ボタンが指に追従する・**指をバーの外まで滑らせても固着しない**（本体レビュー修正(1)の確認）・画面がスクロールしない
3. **編集中の誤動作防止**: 編集モード中に退室・カメラ等をタップしても何も起きない
4. **保存と復元**: 完了→退室→再入室（またはリロード）で並び順が保持されている
5. **初期配置に戻す**: デフォルト順へ戻り、以後も保持される
6. **移動後のパネル追従**: 「その他」を左端等へ移動した後もメニュー・フィルターパネルが画面内に開く
7. **通常通話の回帰**（リアクション・チャット・画面共有）
- 全項目OKなら `v4.7-stable` を 607e427 に付与

### ✅ セッション30の実機確認チェックリスト（482597a・**全項目OK → `v4.6-stable` 確定**）
1. **リアクション全体演出**: 絵文字＋送信者名タグが画面下部から上端へ昇る（送信側・受信側の両端末で見える・6種すべて・連打でも重ならず流れる）
2. **プレゼンターモード中のリアクション**: 画面共有の視聴中でも画面全体に見える（旧タイル方式の弱点解消の確認）
3. **コントロールバー新配置**: 7ボタンが1行（SE3縦375px含む）・並び順＝カメラ・マイク・画面共有・リアクション・チャット・退室・その他
4. **その他メニュー→背景フィルター**: メニューが開く→「背景フィルター」行でフィルターパネルが画面内に開く→ぼかし/背景画像/なしの回帰（背景画像パネルも画面内に収まるか＝本体レビュー修正(1)の確認）
5. **フィルターON中の表示**: その他メニューの「背景フィルター」行がティール色になる
6. **通常通話の回帰**
- 全項目OKなら `v4.6-stable` を 482597a に付与 → セッション31（ボタン並べ替え）へ

### ✅ セッション29の再実機確認チェックリスト（3aaa04c・**全項目OK → `v4.5-stable` 確定**）
1. **【報告①の修正】退室・入室の繰り返し**（満員誤判定が出ない）→ OK
2. **【報告②の修正】iPhoneのPiP残骸** → OK
3. **【報告③の修正】SE3縦でコントロールバー7個が1行** → OK
4. **リアクション/挙手の回帰** → OK（初回テストで機能自体は確認済み）
5. **通常通話の回帰** → OK

### 📌 セッション30〜31の実装内容（ユーザーと合意済み）→ **1と2はセッション30で実装済み（v4.6-stable確定）。3もセッション31で実装済み（607e427・実機確認待ち）**
1. **リアクションの全体演出化（小・低リスク）**: 絵文字の追加先を送信者タイル内→画面全体の透明オーバーレイ（pointer-events:none）に変更。コントロールバーの上あたりから画面上端へ2.5〜3秒かけて昇りフェードアウト、左右にゆらぎ。**絵文字の下に小さく送信者の名前タグを付けて一緒に昇らせる**（誰のリアクションか分かるように）。タイル方式の弱点（overflow:hiddenクリップ・プレゼンターモードで見えにくい）が消える。iPhoneネイティブ全画面中は見えない制約は現状と同じで許容
2. **コントロールバー再構成（中）**: 常設6個＝**左から カメラ・マイク・画面共有・リアクション・チャット・退室**、右端に**「その他（…）」メニュー**（計7スロット＝現在と同幅）。フィルターはその他へ移動。将来の録音・ホワイトボード・ルームロック等はすべてその他に収容（何個増えても破綻しない）。全端末で統一構成（友人への説明が1種類で済む）
3. **（次々回＝セッション31）ボタン並べ替え・左利き対応**: その他メニュー内に「ボタンの並べ替え」→編集モード（iOSホーム画面風にボタンが揺れる）→ドラッグ&ドロップで順番変更→「完了」で確定・localStorage保存（次回以降も保持）・「初期配置に戻す」ボタン付き。**「その他…」ボタン自体も並べ替え対象に含める（合意済み）**。iOSはHTML5 D&D APIがタッチで動かないためPointer Eventsで自前実装。通話中の長押しドラッグ方式は誤爆リスクがあるため不採用（編集モードに閉じ込める）。2の土台の上に載せる
- 備考: iPhoneは画面共有の送信が技術的に不可（getDisplayMedia非対応）だが、ユーザー意向により画面共有ボタンは常設に含める

### ✅ セッション28変更の実機確認チェックリスト（全項目OK・完了 → `v4.4-stable` 確定）
1. **画面共有フルスクリーン（視聴側・3系統）** → OK
2. **共有終了時の自動復帰（iPhone全画面残留の修正確認）**: 全画面が自動で閉じる・ホームに黒残骸なし → OK
3. **共有者側⛶（停止ボタンと共存）** → OK（初回テストで確認）
4. **通話中ヘッダー圧縮/非表示** → OK（初回テストで確認）
5. **共有中に退室→再入室で共有映像が正しく映る** → OK
6. **ホスト退出の即時検知（iPad Air2・iPhone・2011 MBA全員が即復帰）** → OK
7. **通常通話の回帰** → OK

### ✅ セッション25変更の実機確認チェックリスト（全項目OK・完了）
1. **通常通話の回帰確認（2台以上）**: 入室→映像・音声・チャット・退室が従来どおり動くこと（所見①のガードが正規イベントを妨げないことの確認）→ OK
2. **背景フィルターの素早い切替**: フィルター初回ON（「読み込み中...」表示の間）に「なし」→すぐ「ぼかし」と連打しても、最終的に選んだ状態が正しく適用され、映像固着・コンソールエラーが出ないこと → OK
3. **背景フィルターの回帰確認**: ぼかし/背景画像/なし の通常切替、カメラオフ時の黒タイル、プリコールでの設定が従来どおり動くこと → OK

**（旧）進捗状況（セッション24）**: 冒頭でセッション23変更の**実機確認が完了（全項目OK）→ `v4.0-stable` 正式確定**（コミット efcec83 でサマリー更新・タグpush済み）。続いて恒例の**全コードのバグ調査**をオーケストレーション方式で実施（Sonnetサブエージェント2体を並列投入＝①app.jsロジック精査②ファイル横断整合性点検、報告は本体が実コードで裏取りして誤検知を排除）。**致命的バグはゼロ**、確定所見4件＋掃除1件をすべて修正した（**コミット 5643f97・app.js v98 / style.css v44・デプロイ済み・実機確認は未実施**）。**所見①（中・修正済み）**: `handleCall` の `call.on('stream')` にhangup済みガードが無く、退室ボタンを押した瞬間に別ピアのメディア接続が確立しかけていた場合、`hangup()` 完了後の遅延streamイベントで (a)ウェルカム画面にゴースト映像タイルが追加され次回入室まで残留しうる、(b)teardown済みの発話検知AudioContext＋setIntervalが `attachSpeakingAnalyser` 経由で再生成され誰も止めない（リーク）。→ データ接続側の既存イディオム（663行 `if (!this.peer) return`）と同形の `if (this.isLeaving || !this.peer) return;` をハンドラ先頭に追加。**所見②（低・修正済み）**: `initializePeer` の `onOpen` に素のデバッグ `console.log('Peer connected with ID:', ...)` が残存→削除。**所見③（低・修正済み）**: `.container.in-call`（height/min-height:100svh）とモバイルチャットボトムシート（height:72svh）に vh フォールバックが無く、svh非対応のSafari 15.0-15.3では宣言ごと無視される（冒頭の `.container` は二段書き済みなのにこの2箇所だけ抜けていた。手持ち端末は全て15.4以上なので実害は現状ゼロ＝予防修正）→ 直前に同値のvh宣言を追加。**所見④（低・防御強化・修正済み）**: 発話検知AudioContextがsuspendedのまま `resume()` に失敗すると（iOSでユーザーアクティベーション失効時）その通話中インジケーターがサイレントに無効化され再試行の機会が無かった→ `registerSpeakingResumeRetry()`/`unregisterSpeakingResumeRetry()` を新設し、次のユーザー操作（`pointerdown`、PointerEvent非対応なら`touchend`）でresumeを再試行。成功(running)/context closed/teardownで解除、多重登録ガードあり、`teardownSpeakingDetection` でも確実に解除。**掃除⑤（修正済み）**: `id="status"` 要素はHTML削除済みで到達不能だった `.status`/`.status.connecting`/`.connected`/`.error` CSS一式（旧833-854行）を削除（JS側の `this.statusDiv` nullガードは意図的な後方互換なので残置。`.toast` 系は使用中につき無傷）。**クリーンだった項目**: ターコイズ化の置換漏れゼロ（旧紫系6色＋紫系全般を3ファイル検索）、ID整合・クラストグルとCSSの対応・innerHTML安全性（全7箇所静的orクリアのみ）・キャッシュバスター・`:has()`残骸、いずれも問題なし。**グレーゾーン（対応不要と判断）**: conn close/errorがcalls側をcloseしない件（call側のclose/errorが別途発火して整合）、生マイクトラックを解析用/ミックス用2つのAudioContextで共有する件（セッション23実機確認で動作OK済み）。**検証**: `node --check` OK・diff精査で仕様外変更なし・エージェントのプレビュー起動確認でコンソールエラーなし。**同日中に実機確認3項目OK → `v4.1-stable` 付与・正式確定（2026-07-04）**。続いて実機テストで**新たな不具合報告2件→原因分析・修正（コミット 01bb624・app.js v99 / style.css v45・デプロイ済み・実機確認待ち）**。**報告①（カメラオフでタイルが黒くならず直前画像が固着。SE3は毎回、M4は確率的に発生）**: スクショでSE3のタイルに背景フィルターのリゾート画像だけが残っていたのが決定的ヒント。真因＝`toggleVideo`はフィルター使用時に`stopBgFilterLoop()`→キャンバス黒塗りするが、`startBgFilterLoop`のループ本体は`await grabFrame`等をまたぐ非同期関数で、`cancelAnimationFrame`では**実行中の残り処理（segment→`onSegmentationResults`描画）を止められない**。黒塗りの後に遅延着弾した結果が上書きし、入力はdisabled（黒）→人物マスクゼロ→背景画像だけが描かれて固着（M4で「やり直すと直る」のはレースの典型）。修正2段構え＝**(a)送信側**: `onSegmentationResults`冒頭にカメラオフガード（黒塗り＋`result.close?.()`＋return。resultは関数内解放の契約）、`startCSSFilterLoop`にも同等ガード。**(b)表示側**: `.video-container.camera-off .video-element { visibility:hidden }` を新設し、`toggleVideo`／`camera-state`受信／`addVideoElement`の3箇所で`camera-off`クラスを付け外し。コンテナ背景が#000なので、iOS Safariの「disabledトラックのフレーム固着」等の端末差によらず黒＋中央名を保証。**報告②（ホストが先に退出するとゲストが取り残される・ホストは同IDで再入室不可）**: ルームID＝ホストのPeer IDのため、ホスト退出後は誰も参加できない死んだルームになる（設計上の宿命、セッション12で現状維持と合意していた件）。設計判断として**ホスト退出＝ルーム終了**に変更（Zoomと同じ）。実装は**ゲスト側検知**方式＝`handleConnection`の`conn.on('close')`/`conn.on('error')`冒頭に「`!isLeaving && !isHost && conn.peer === roomId`ならhangup＋『ホストが退出したためルームは終了しました』表示」を追加。ホストからの通知メッセージ方式でなく切断検知なので、**退室ボタン・タブ閉じ・クラッシュ・回線断すべてで同じに動く**。同IDの再入室（ルーム復活）は外部DB等が必要な大工事のため見送り＝解散後は新ルームを作る運用。**検証**: `node --check` OK・diff精査（実装はSonnet、`result.close`漏れ1件を本体レビューで検出し修正）。**同日中に実機確認全項目OK → `v4.2-stable` 付与・正式確定（2026-07-04）。セッション24完了**（このセッションで v4.1-stable と v4.2-stable の2つのセーブポイントを確定）。

**（旧）進捗状況（セッション23）**: 今回からオーケストレーション方式（Claude本体が指揮・レビュー、実装はSonnet/Opusサブエージェント）で進行。ユーザー要望の新機能2件を実装・デプロイした（**コミット 7e36fb2・app.js v97 / style.css v43・実機確認は未実施**）。**①カラーテーマのターコイズ化（style.css v42）**: 紫系を全面置換。メイングラデーション `#667eea→#0891b2`(cyan-600)・`#764ba2→#0d9488`(teal-600)、ファイルリンク濃色 `#4f46e5→#0e7490`、淡色背景 `#eef2ff→#e6fbfa`、背景フィルターボタン `#9b59b6→#0f766e`/`#6c3483→#115e59`（他のコントロールボタン緑/青/黄/赤との識別性を保つため意図的にメインと別のteal濃色）。白文字コントラストは旧紫と同等を維持（3.66→3.68。明るい#06b6d4は2.43まで落ちたため不採用）。エラー赤・警告黄・成功緑は意味色なので不変。Safari15対応のためcolor-mix/oklch不使用。**②発話インジケーター（app.js v97 / style.css v43）**: 話している参加者のタイルにアンバー(#fbbf24)の枠3px＋glowを表示（枠色はターコイズテーマと被らないようユーザーと協議しアンバーに決定）。実装＝解析専用AudioContext（画面共有ミックス用`mixAudioContext`とは別）＋各参加者にAnalyserNode(fftSize512)、150ms間隔の単一setIntervalで全員のRMSを計測。しきい値は静的ゲッター `SPEAK_ON_RMS=0.045`(即時点灯)/`SPEAK_OFF_RMS=0.030`(ヒステリシス)/`SPEAK_HOLD_MS=500`(無音継続で消灯＝ちらつき防止)/`SPEAK_INTERVAL_MS=150`。自分の解析は**生マイクトラック**（`localStream`）を使い画面共有ミックス音声での誤点灯を防止。ミュート連動（自分=track.enabled＋isAudioMuted、リモート=muteStates）で点灯抑止。クリーンアップは `removeVideoElement→detachSpeakingAnalyser`、`hangup`/`createRoom`・`joinRoom`のcatch→`teardownSpeakingDetection`(ループ停止＋AudioContext close)。**レビューで1件修正**: 当初の `inset box-shadow` 枠はコンテナ背景に描画され上に被さる映像(.video-element)に遮蔽されて見えないことをデモDOMで発見→ `.speaking::after` オーバーレイ（`border:3px solid #fbbf24; border-radius:inherit; z-index:6; pointer-events:none`）方式に修正。プレゼンターモードのサムネイルは既存borderの色をアンバー化し `::after` は `display:none`（二重枠防止）。検証＝判定ロジック単体テスト7ケース全PASS（即時点灯・500ms猶予・300ms瞬断では消えない・ミュート中は大音量でも非点灯等）、Chromiumプレビューで通常タイル/プレゼンターサムネイルとも枠がくっきり視認・レイアウトシフトなし・`node --check`OK。**次回最優先＝実機確認**（色味の好み確認、発話インジケーターは2台以上で、しきい値の実地調整が必要かも）。**→ 2026-07-04 全項目実機確認OK（カラーテーマ・発話インジケーター・ミュート連動・負荷とも問題なし）→ `v4.0-stable` 正式確定（HEAD にタグ付与・push 済み）。セッション23完了**。

**（旧）進捗状況（セッション22）**: 冒頭でセッション21変更（v3.8-stable）の**実機確認が完了（全項目OK）→ v3.8-stable 正式確定**。続いて**全コードのバグ調査**（app.js 2450行＋index.html＋style.css。構文・ID整合・JSトグルクラスのCSS存在・`:has()`残骸・デバッグ残骸を点検）を実施し、実バグ2件を発見・修正した（**app.js v95・コミット e8c368b・デプロイ済み**）。**所見①（修正済み）**: 全角文字等サニタイズ後に空になるルームID（例：日本語IMEの全角数字「１２３」）で参加すると、form submit は生入力の非空チェックのみ→プリコールでカメラ取得＋3ボタン無効化→`joinRoom` のサニタイズで空になり **try/catch より前の早期return** でクリーンアップが走らず、**カメラ掴んだまま全ボタン無効でリロードするまで詰む**。form submit 時に同じサニタイズで事前検証して弾き（「ルームIDは半角英数字で入力してください」）、joinRoom 側にもカメラ/フィルター解放＋ボタン復元の保険を追加。**所見②（修正済み）**: `screen-share-stop` 受信が送信者を確認せず無条件に `exitRemotePresenterMode()` するため、共有が重なった場合（A共有中にB開始→A停止）にBの共有を見ている全員が誤解除される。`senderId === currentRemoteSharerId` ガードを追加（conn close/error 側には元々同等ガードあり）。あわせて、自分が共有中に他人が共有開始したら **自分の共有を停止して譲る後勝ち処理** を `enterRemotePresenterMode` 冒頭に追加（従来は stopShareBtn が隠されて自分の共有を止めるUIが消えていた）。**所見③（現状維持）**: `waitForBuffers` のストール（セッション21で合意済み・対応せず）。**検証**: Chromiumプレビューで①全角ID→プリコール開かずエラートースト表示・ボタン無効化なし、半角IDは従来どおりプリコールへ進む、②`handleDataMessage` 単体テストで非共有者からの stop は無視・共有者からの stop で解除、を確認。さらに**同日の実機テスト（4台）でプレゼンターモードの重大レイアウト問題3件が判明→修正（style.css v41・コミット 361ba1c・デプロイ済み）**。**(a) 全環境でコントロールバーが操作不能**＝`#screen-share-container` の `absolute inset:0` オーバーレイが call-main 全体（コントロール・ルーム情報含む）を覆っていた。**(b) 横向き/PCでサムネイル帯が共有映像上部に被る**＝帯も `absolute top:10px` のオーバーレイだった。**(c) iPhone縦でサムネイル2人分のみ・横スクロール不可**＝モバイルの `.video-grid { flex-wrap:wrap }` が帯に効き、3人目以降が2行目に折り返され `height:90px` でクリップ（横にあふれないため横スクロールも発生せず）。→ **オーバーレイをやめ、`order` で「サムネイル帯→共有画面→コントロール→ルーム情報」の通常フローflexカラムに変更**（Zoomと同構造）。帯は `flex-wrap:nowrap`＋`overflow-x:auto`＋`justify-content:flex-start`（centerだと左端タイルへスクロールで届かない）＋`-webkit-overflow-scrolling:touch`。`#screen-share-container` は `position:relative; flex:1 1 0; min-height:0`（stop-share-btn/share-viewer-label のabsolute基準は維持）。Chromiumプレビューで 1280px(PC)/375px(iPhone縦) とも帯・共有・コントロール全て可視・被りなし・375pxで4タイル1行＋横スクロール（scrollW 664>363）を確認。続いて**iPhoneのみ「プリコールのカメラ/マイク/背景ボタンを押した瞬間にウェルカム画面へ戻る・以後どのボタンも反応しなくなる」実機報告→修正（app.js v96・コミット 426e563・デプロイ済み）**。原因＝iOS Safariはソフトキーボード表示中(直後)に `position:fixed` 要素の**描画位置とタップ判定がずれる**ため、ルームID入力→キーボードが開いたままプリコールを開くと、ボタンを押したタップが背景(バックドロップ)判定になり**即キャンセル**が発火（ずれが残ると以後のタップも全て外れる）。iPadは影響が出にくくPCはキーボードなしのためiPhoneのみで発生。対策2段構え：**① `showPreCallDialog` 冒頭でキーボードを閉じ(blur)＋`scrollTo(0,0)`**（セッション18問題③=`showCallScreen`と同じ対策の水平展開）、**② バックドロップキャンセルを「押し始め(pointerdown)も背景」の場合に限定**（ずれたclickだけでは誤キャンセルしない。PointerEvent非対応の旧Safariは従来どおりclickのみ判定）。プレビューでゴーストクリック維持/正規バックドロップタップで閉じる/旧環境互換の3ケース検証OK。**補足**: GitHub Pagesのデプロイ失敗メール2件（8cba308・361ba1c）は一時的なもので、最新デプロイ成功により全変更が公開済みであることをAPI＋公開サイトの配信バージョンで確認済み（対応不要）。**全項目を実機確認（iPhone/iPad/PC）→ ①ルームID全角弾き②画面共有の新レイアウト（コントロールバー表示・サムネイル帯の横スクロール）③iPhoneプリコールのボタン反応、いずれもOK。→ `v3.9-stable` 付与・正式確定（2026-07-03・コミット fa81fa1）。セッション22完了**。

**（旧）進捗状況（セッション21）**: **全コードのバグ調査**（app.js 2428行＋index.html＋style.css、セッション17以来の通し点検）を実施。致命的バグはゼロ、所見3件を発見し2件を修正した。**所見①（修正済み・app.js v92）**: `presenter-mode` クラスを切り替える4箇所（`shareScreen`/`stopScreenShare`/`enterRemotePresenterMode`/`exitRemotePresenterMode`）が `relayoutVideoGrid()` を呼んでおらず、モバイル3人以上でタイルのインラインwidth（`calc(50% - 4px)`）が画面共有サムネイル帯（CSS `width:160px`）に残留していた。ResizeObserverで後追い修正されるため実害は1フレーム程度だが、4箇所で同期的に再レイアウトするよう修正。**所見②（修正済み・app.js v93 / style.css v39）**: モバイル通話画面の全画面固定レイアウトを `.container:has(#call-screen:not(.hidden))` で判定していたが、`:has()` は Safari 15.4未満・Chrome 105未満で丸ごと無視される。`showCallScreen`/`showWelcomeScreen` で `.container` に `in-call` クラスを付け外しし CSS は `.container.in-call` で判定する方式へ変更（セッション18の申し送り「他の:has()使用箇所の確認」の恒久対応）。**所見③（現状維持と合意）**: グループファイル送信中にピアが切断すると `waitForBuffers` の `Promise.all` が5秒タイムアウトまで待つストールがありうるが、稀な条件かつデータ破損なしのため対応せず。さらにユーザー要望2件を実装：**タブタイトルを「完全無料ビデオ会議」→「無料ビデオ会議」に変更**、**ルームID入力後の誤操作対策（app.js v94 / style.css v40）**＝ID欄に入力があるあいだ「参加する」ボタンを紫グラデーション＋opacity 1↔0.55の呼吸アニメ(1.8s)で強調し「ルーム参加」ボタンをグレー化（`updateJoinReadyState`、ID削除・ウェルカム復帰で解除。ルーム参加は無効化せず押せるまま）。**全変更デプロイ済み・Chromiumプレビュー検証OK・`v3.8-stable` 付与。実機確認も完了（2026-07-03・①タイトル②参加ボタン誘導UI③通話画面レイアウト④画面共有すべてOK）→ `v3.8-stable` 正式確定**。

**（旧）進捗状況（セッション20）**: セッション19までで未解決だった **iPad Air2 縦向きの映像グリッド列数固着** に対し、**アプローチを転換**して修正(app.js v90 / style.css v37)。症状「回転させると直る＝`grid-template-columns` の値は変えずフル再レイアウトで直る」から、原因は **Safari15系がCSS Gridの列トラック変更を子タイルの再配置に反映しない描画バグ** とほぼ断定。`:has()` も JS の `grid-template-columns` も `display:none→''` のツリー再構築も効かなかった（＝グリッドを使う限り固着する）ため、**モバイル(≤768px)だけ CSS Grid をやめ flexbox に変更**。列数は `relayoutVideoGrid` が各タイルの `width`(1〜2人=100%/3人以上=`calc(50% - 4px)`)で決める方式に。flex は「子の width 変更」を確実に再レイアウトするためグリッド固有の固着バグを踏まない。PC(≥769px)は従来の grid ロジックのまま無変更（モバイルで付与した inline width は wide/presenter 分岐で除去）。**Chromiumプレビューで検証OK**：375px幅で 1人→1列/2人→1列(縦積み)/3〜4人→2列、さらに **4→3→2 と減らしても列数が正しく戻る**（Safariが grid で失敗していた挙動）。`?griddbg=1` は flex 用に表示更新(display/tileW computed)して**残置**（実機で確認するため）。**未タグ・デプロイ済み想定**。**【実機フィードバック→追加修正 css v38】**：SE3/iPad Air2 実機で3人=2列になったものの、**タイルが縦に間延びして16:9が崩れる**不具合が出た（iPadは回転後さらに悪化）。原因＝モバイル `.video-grid` の `align-content: start` を Safari15系が認識せず初期値 `stretch` に落ち、背の高いグリッド領域いっぱいに行を引き伸ばし＋`align-items` 既定の `stretch` でタイルも縦伸び。→ `align-content: flex-start` ＋ `align-items: flex-start` ＋ `.video-container { height:auto }` に修正（`align-items:flex-start` が本命の安全弁：行が伸びてもタイルはaspect-ratioの自然な高さを保ち上詰めになる）。Chromium 768pxで 1/2人=1列16:9・3人=2列16:9(364×205)・上詰めを確認。**【実機OK・完了】全端末(iPhone SE3・iPad Air2縦・M4 MBA)で3人=2列・16:9維持・回転しても崩れないことを確認。→ `?griddbg=1` 診断オーバーレイと `_updateGridDebug` を撤去(app.js v91)し `v3.7-stable` を付与**。長年の iPad Air2 縦向き列数固着はこれで解決。

**（旧）進捗状況（セッション19）**: セッション18の実装(v86)を実機確認。**iPhone SE3・M4 MBA(PC)はOK**だが、**iPad Air2縦向きで映像グリッドの列数が固着する問題が継続**。症状の詳細：①縦向きのまま3人入室→1列のまま(本来2列)、②端末を回転(縦→横)させると2列になる=強制リフローで直る、③縦で人数を増減しても列数が変わらず、回転して初めて変わる、④3人縦向き(2列表示中)から1人退出して2人にしても2列のまま(本来1列)。→ Safari(WebKit)が既存gridへの `grid-template-columns` 変更を子要素の再配置に反映しない描画バグと推定。**対策を2段階で試みたが未解決**：(A) `void grid.offsetHeight` 強制リフロー＋rAF再適用(app.js v87)→効かず。(B) `display` を一瞬 `none→''` に切替える強制再レイアウト(app.js v88)→3人入室時に画面が一瞬チラつくが**依然1列のまま**。→ 当て推量をやめ実測に切替。**`?griddbg=1` 診断オーバーレイを再追加(app.js v89)**：画面左上に `N / wide / innerW / inline(列指定) / computed(実際に算出された列トラック)` をライブ表示。**次回：iPad Air2で `https://iitsubu-cloud.github.io/comchat/?griddbg=1` を開き、縦向き3人時の値を取得**。`computed` が2トラック(例 `372px 372px`)なのに画面1列なら純粋な描画バグ→DOM再アタッチ等のより強力な手法へ。`computed` が1トラックなら指定が効いていない→指定方法の問題。`inline=1fr` ならN判定バグ。ロジック自体はプレビュー(別エンジン)で正常動作確認済み(N=1,2→1列/N=3,4→2列、増減とも正しい、displayは''に戻る)。**全変更(v87〜v89)デプロイ済み・未タグ**。
- 補足：ホストのマイクオフアイコンが表示されない事象が一度あったが、その後の再テストで自然に直った。`mute-state` 送受信ロジックにコード上の恒常バグは見当たらず、受信タイミングのズレと判断し様子見。

**（旧）進捗状況（セッション18）**: セッション17の送信ボタン見切れ修正を実機OK確認し `v3.6-stable` を付与。次に**モバイル映像グリッドの2列化**（合意済み：1〜2人=1列/3人以上=2列）を実装(style.css v35)。実機テストで**3件の問題**が判明し対応：①PC/タブレットで2人が縦積み、②iPad Air2で3人が縦1列、③入室時に画面が上にずれる。①②は `relayoutVideoGrid` の列選択で「行数>列数」の縦積み配置を候補から除外(app.js v83)。③は `showCallScreen` でページスクロールを先頭リセット＋キーボードblur(app.js v84)。しかし**iPad Air2縦で3人が1列のまま再現**→ `?griddbg=1` 一時オーバーレイ(v85)で実値取得し、原因は **Safari 15系の `:has()` が子要素の動的追加時に再評価されない不具合**（2人目で1列確定→3人目で更新されない。N=1初回ロード時は正しい）と確定。**列数決定をCSSの`:has()`からJSへ移行**し、`relayoutVideoGrid` のモバイル分岐で人数に応じて `grid-template-columns` を明示設定(1〜2人=1列/3人以上=2列)、`:has()`ルールを撤去、デバッグも撤去(app.js v86 / style.css v36)。**全修正デプロイ済み・プレビュー検証OK（動的増減で列数が正しく更新）だが、v86の実機最終確認は未実施（次回最優先）**。問題なければ `v3.7-stable` 付与。

**（旧）進捗状況（セッション17）**: 全コードのバグ調査を実施。過去のセッション9・12の掃除もあり全体は良好だったが、**参加フローのバグ2件**を発見・修正し実機OK。①存在しない/オフラインのルームに参加しても接続成立を待たず通話画面に遷移し「一人ぼっち」で取り残されていた → `connectToHost` をデータ接続オープン待ち(10秒タイムアウト)に変更し、失敗時はウェルカム画面へ戻してエラー表示。②さらに、失敗後に同じIDで再参加すると入室できてしまうバグ（catchが `connections` 等のMapをクリアせず残骸が残り、`connectToHost` 冒頭の `connections.has(hostId)` ガードが残骸にヒットして即成功扱い）も発見し、両catchでセッション用Mapを `clear()` するよう修正。あわせてCSS撤去済みの死んだ `has-unread` クラストグルをJSから除去。実機で①成功・②失敗→ウェルカム復帰・③同IDで再参加も復帰、を確認済み（満員テストのみ端末不足で未実施）。**v3.5-stable / app.js v82**。

**（旧）進捗状況（セッション16）**: 「iPad2」と呼んでいた端末が実は **iPad Air2**（A8X / RAM 2GB / iPadOS 15.6.8）と判明。診断ページの値（WebGL2あり・Apple GPU・MAX_TEXTURE_SIZE 8192・4コア）は正常端末と区別できず、静的判定での線引きは不可能と確定。そこで **実行時にセグメンテーション破綻を検知して背景フィルターを自動オフにする仕組み**を実装（app.js v80）。Air2実機の計測で `person=0.0% / motion=0.0`（画面外videoスロットリングでフレーム固着）を確認し、当初入れた動きゲートは「人がいても0」で判定を阻害かつ誤検知防止にも無力と判明したため撤去。最終ロジックは **「人物<0.5%」＋「起動後一度も正常分離せず(healthySeen=false)」がウォームアップ後2.5秒継続したら自動オフ**。1回目は「うまく動作していないようです。オフにしました。」＋再挑戦可、2回目は「この端末では背景フィルターを利用できません。」で確定無効化。会議途中の席外しは離席前にhealthySeen=trueになるため無干渉。**Air2実機で自動オフ動作を確認済み（OK）**。診断HUDは撤去済み。

---

## 🎯 プロジェクト概要

- **名前**: ComChat
- **目的**: 離れた友達との無料ビデオ通話・ミーティング
- **技術**: PeerJS (WebRTC P2P)、HTML/CSS/JS、ブラウザのみで動作
- **制限**: 最大6人同時通話
- **特徴**: 完全無料、時間制限なし、インストール不要
- **公開URL**: https://iitsubu-cloud.github.io/comchat/
- **リポジトリ**: https://github.com/iitsubu-cloud/comchat

---

## ✅ 実装済み機能

- ビデオ通話（最大6人、制限実装済み）
- リアルタイムチャット
- 画面共有（プレゼンターモード）
- カメラ・マイクのオン/オフ
- ルーム作成・参加
- ルームID ワンクリックコピーボタン
- 参加後のユーザー名変更
- SVGピクトグラム風コントロールアイコン＋ラベル
- 退室確認モーダル
- マイクミュートインジケーター
- ファイル共有（最大200MB、プログレスバー付き）
- 背景フィルター（ぼかし・背景画像）
  - MediaPipeセグメンテーション + Safari対応フォールバック
  - プリセット3種（室内・花畑・リゾート）
  - 画像アップロード機能・履歴3件保持（localStorage）
- プリコールダイアログ（カメラプレビュー・カメラ/音声ON-OFF・背景設定）
- カメラオフ時に中央にユーザー名表示
- 初期画面ヒーローデザイン（Klee Oneフォント）・ボタン横並び
- **モバイル通話画面スクロール対応**（:has() セレクターによる通話中のみ適用）
- **新着メッセージ バッジ通知**（チャットヘッダーに赤丸数字、IntersectionObserver）
- **モバイルレイアウト案A**（映像主役・チャットをボトムシート化・トグルボタン）
- **ルームID入力のform化**（キーボードのEnter/Goキーで参加、参加ボタン自動スクロール）
- **絵文字リアクション**（6種・データチャネル送信・**画面全体オーバーレイを絵文字＋送信者名タグが昇る演出**・許可リスト検証）
- **挙手**（✋バッジ・トースト通知・新規参加者への状態同期）
- **コントロールバー「その他…」メニュー**（常設6個＋右端その他の7スロット・フィルターはメニュー内・将来機能は行追加で拡張）
- **ボタン並べ替え・左利き対応**（編集モード＝iOSホーム画面風・Pointer Events自前D&D・localStorage保存・初期配置に戻す）
- **招待リンク**（?room=付きURLをコピー・リンクで開くと参加フォームにID入力済み＋名前入力へフォーカス・自動参加はしない）
- **QRコード招待**（招待URLをモーダルでQR表示・js/vendor/qrcode.js＝qrcode-generator v1.4.4 MIT同梱・白パネルでダークテーマでも読取可）
- **通話経過時間表示**（room-info先頭・Date.now()差分方式・タップで数字⇔アイコンのみ切替・毎通話デフォルト表示で永続化なし）

---

## 🔧 セッション18で実施した変更

### 1. 送信ボタン見切れ修正(所見C)を実機OK → `v3.6-stable` 付与
- セッション17の 827c2b4（iPad Air2横向き等で送信ボタン見切れ・style.css v34）を実機確認OK → `v3.6-stable` を 827c2b4 に付与・push

### 2. モバイル映像グリッドの2列化（合意方針：1〜2人=1列/3人以上=2列）
- 当初CSSの `:has()` で実装(style.css v35)→ 後述の不具合で **JS方式へ作り直し**

### 3. 実機テストで判明した3問題への対応

**問題①：PC/タブレットで2人が縦積み（app.js v83 / コミット 028b670）**
- 原因：`relayoutVideoGrid` が「タイル最大化」で列数選択するため、縦長/正方形エリアでは2人=1列(481px)>2列(332px)となり縦積みを選択
- 修正：列数ループで `if (rows > cols) continue;`（縦積み配置を候補から除外、横並び優先）。タイル最大化の計算自体は維持。ユーザー方針＝「現状のタイル最大化を改良」
- 効果：PC/タブレットで2人=横2列、3〜4人=2列、5〜6人=3列に。画面に適応

**問題②：iPad Air2縦で3人が縦1列（①と同根 → 最終的に :has不具合と判明）**
- 当初①の修正でカバーされる想定だったが**実機で1列のまま再現**
- `?griddbg=1` で実値取得：`branch=mobile/CSS, innerW=768, mm(max768)=true, :has support=yes` だが N=3 で `computedCols=736px`(1列)
- **真因＝Safari 15系の `:has()` 動的再評価バグ**：`:has(.video-container:nth-child(2):last-child)` が2人目で1列に確定し、3人目を追加しても再評価されず1列のまま（N=1初回ロードは `:only-child` が正しく効く）

**問題③：入室時に画面が上にずれる（app.js v84 / コミット b5583df）**
- 原因：参加前にルームID入力欄へフォーカスした際の `confirmJoinBtn.scrollIntoView`（セッション11のキーボード対策）でページがスクロールし、入室後もリセットされずヘッダーが画面外へ
- 修正：`showCallScreen` 冒頭で `document.activeElement.blur()` ＋ `window.scrollTo(0,0)`（rAFでもう一度）
- 既存バグ（今回の変更とは無関係）。**実機最終確認は未実施**

### 4. 列数決定を :has() → JS へ移行（app.js v86 / style.css v36 / コミット ce0658d）
- `relayoutVideoGrid` のモバイル分岐(≤768px)で、人数に応じて `grid-template-columns` をJSで明示設定：`(N <= 2) ? '1fr' : 'repeat(2, 1fr)'`
- CSSの `.video-grid:has(...)` ルールを撤去（CSSは初期フォールバック `1fr` のみ）
- relayoutは参加者の増減・チャット開閉・showCallScreen・ResizeObserverで呼ばれるため、Safariでも確実に列数が更新される
- `?griddbg=1` デバッグオーバーレイ（v85）も撤去
- **プレビュー検証OK**：768pxで参加者を1人ずつ追加→1,1,2,2列、2人に減→1列、と動的に正しく更新

### ⚠️ 次回最優先：v86の実機最終確認（未実施）
- iPad Air2（縦）で**3人=2列**になるか（今回の本命）。1〜2人で1列、人数を変えて動的に切り替わるか
- iPhone SE3：1〜2人=1列/3人以上=2列、入室時に画面がずれないか（問題③）
- M4MBA(PC)：2人で横2列
- **全OKなら `v3.7-stable` 付与**

### 📌 申し送り：他の `:has()` 使用箇所の確認
- `style.css` の `.container:has(#call-screen:not(.hidden))` 等は**クラス切替**であり子要素の動的追加ではないため今回の不具合とは別だが、Safari 15系での挙動を一度確認しておくと安全（grep: `grep -n ":has(" css/style.css`）

---

## 🔧 セッション17で実施した変更

### 全コードのバグ調査 → 参加フローのバグ2件を修正（コミット dfff8e7・473db21 / app.js v82）

**調査範囲：** app.js(2361行) + index.html + style.css の通し点検。構文チェック・getElementById/HTMLのID整合・JSがトグルするCSSクラスの存在確認・撤去済みデバッグ残骸の有無を確認。XSSはtextContent使用で安全。**致命的バグは無し**（セッション9・12の掃除が効いている）。発見したのは下記2件。

**所見A：存在しない/オフラインのルームに参加すると一人ぼっちで取り残される（app.js v81）**
- 原因：`joinRoom` が `await connectToHost()` していたが、実体 `connectToPeer()` は `peer.connect()`/`peer.call()` を撃ちっぱなしで**接続成立を待たず即返る**。そのためID誤入力やホスト退出済みでも成功扱いになり、誰もいない通話画面に遷移していた（`peer-unavailable` トーストは出るが取り残される）
- 修正：`connectToHost` をデータ接続の `open` をタイムアウト(10秒)付きで待つ Promise に変更。開かなければ reject → 既存の `joinRoom` catch がクリーンアップしてウェルカム画面へ戻しエラー表示。iOS向けの入力サニタイズ・form送信(セッション11)は不変。成功時は接続成立後に通話画面へ（遷移が1〜2秒後になる）

**所見A′：所見A修正後、失敗後に同じIDで再参加すると入室できてしまう（app.js v82）← 実機検証で発覚**
- 原因：`joinRoom`/`createRoom` の catch が `peer.destroy()` のみで **`connections` 等のセッション用Mapをクリアしていなかった**。失敗時に `connectToHost` が `connections.set(hostId, conn)` した残骸が残り、同じIDで再参加すると `connectToHost` 冒頭の `if (this.connections.has(hostId)) { resolve(); return; }` ガードが**残骸にヒットして即成功扱い** → 通話画面に入って一人ぼっち
- 修正：両catchで `connections`/`calls`/`usernames`/`muteStates`/`cameraStates` を `clear()` してセッション状態を確実にリセット

**所見B：死んだ `has-unread` クラストグルを除去（app.js v81）**
- `updateUnreadBadge`/`clearUnreadBadge` が `.chat-btn` に `has-unread` を付け外ししていたが、対応するCSSはセッション15で撤去済み（無害な残骸）。JSから完全除去

**実機検証（2026-06-30）：** ①正しいIDで参加=OK、②でたらめIDで参加→エラー＋ウェルカム復帰=OK、③同じでたらめIDで再参加→同じく復帰（取り残されない）=OK。**満員(7人目)テストのみ端末不足で未実施**（コード上は従来どおり room-full 通知→切断→満員表示で復帰の想定）。ここまでで `v3.5-stable` タグを付与。

### 所見C：チャット送信ボタンの見切れを修正（コミット 827c2b4 / style.css v34・v3.5-stableタグの後）
- 症状：iPad Air2 横向き等の狭いチャット右カラムで「送信」ボタンが右端で見切れる（実機スクショ iPadAir2ss03 で確認）
- 原因：`.chat-input-container`(flex) の `.chat-input` に `min-width:0` が無く、flexアイテム既定の `min-width:auto` のため入力欄がintrinsic幅以下に縮まず、送信ボタンを枠外へ押し出して `.chat-container { overflow:hidden }` でクリップされていた
- 修正：`.chat-input { min-width:0 }`（入力欄を縮められるように）＋ `.chat-send { flex-shrink:0 }`（ボタンは縮ませない）。`.file-attach-btn` は既に `flex-shrink:0` 済み
- 検証：ブラウザプレビュー(1024×768)で送信ボタン右端(991px) < チャット枠右端(1008px) を確認。**実機(iPad Air2横向き)での最終確認は未実施**。このコミットは `v3.5-stable` タグより後なので、実機OKなら `v3.6-stable` 付与を検討

---

## 🔧 セッション16で実施した変更

### 「iPad2」の正体判明＋背景フィルター破綻の実行時検知を実装（app.js v77）

**端末の正体：** 旧セッションで「iPad2」と呼んでいた端末は **iPad Air2**（A8X / RAM 2GB / iPadOS 15.6.8）。`diag.html` の値は WebGL2あり・GPU renderer=`Apple GPU`（Safariがマスク）・MAX_TEXTURE_SIZE=8192・4コアで、**正常動作する最新端末と全く区別がつかない**。UAも `Macintosh; Intel Mac OS X 10_15_6`（iPadOSのデスクトップ表示）で実Macとも区別不可。→ `canUseBgFilter()` の静的値強化では弾けないと確定。

**症状の確定（実機スクショ5枚より）：**
- ぼかし → Air2タイルだけ人物も背景も全体ボケ（人物が分離できていない）
- 背景画像 → Air2タイルが背景画像だけになり人物消失
- 進行すると全タイル真っ黒＝クラッシュ（RAM 2GBのメモリ圧迫が濃厚）
- → いずれも根は1つ：「**人物と判定されるピクセルがほぼ0%**」。早めにフィルターを切ればクラッシュも予防できる

**実装：実行時マスク健全性チェック（`checkSegmentationHealth` / `detectMotion` / `handleBgFilterBreakage`）**
- `onSegmentationResults` 内の既存ループで人物ピクセル数を集計し、人物割合を算出（追加ループなし）
- **動き検出**：生カメラを32×24に縮小しフレーム間の平均輝度差を測定 → 「人が実際にいるか」を人物割合とは独立に判定（席外しと破綻を区別）
- **発動条件**：ウォームアップ(600ms)後に「人物<0.5%」＋「動き≥閾値(6)」＋「起動後一度も人物≥8%に達していない」が **2.5秒連続**
- **挙動**：1回目=「背景フィルターがうまく動作していないようです。オフにしました。」＋フィルターオフ・生カメラ復帰（ボタンは再度押せる＝誤検知端末の逃げ道）。2回目=「この端末では背景フィルターを利用できません。」＋`_bgFilterRuntimeBlocked=true` で `canUseBgFilter()` を false 固定（セッション無効確定・無限ループ防止）
- **離席への無干渉**：動きなし(静止/無人)や、過去に正常分離できた高機能端末では発動しない。メッセージも出さない
- 既存の静的 `canUseBgFilter()`（WebGL2非対応等の明確なもの）はそのまま残置

**Air2実機での切り分け（重要）：** `?segdebug=1` のライブ計測オーバーレイを一時追加して計測した結果、`person=0.0%`（マスクは本当に破綻＝前提は正しい）だが `motion=0.0`（画面外videoのスロットリングでセグメンテーションへ渡るフレームが固着）と判明。当初の動きゲート(`motion>=6`)は人がいても0でブロックされ、かつ高機能端末の空室も0付近で誤検知防止にも使えないため**撤去**。判別は `healthySeen`（起動後に一度でも人物≥8%を分離したか）に一本化。

**最終ロジック（v80）：** `person<0.5%` かつ `healthySeen=false` がウォームアップ600ms後に2.5秒継続 → `handleBgFilterBreakage()`。1回目=柔らかい案内＋再挑戦可、2回目=`_bgFilterRuntimeBlocked=true` で `canUseBgFilter()` を false 固定。**Air2実機で自動オフを確認済み（OK）**。確認後、診断HUD(`?segdebug=1`)と `detectMotion` は撤去。

**残りの誤検知余地（許容）：** 高機能端末でフィルター設定後、一度も映らずに2.5秒以上離席した場合のみ1回目メッセージが出るが、再オンで回復可能。プリコールで設定する人はプレビューに映る＝healthySeenがtrueになり保護される。

---

## 🔧 セッション15で実施した変更

### セッション14修正の実機検証 → 判明した不具合を修正（コミット cd9b9d5・3e2474e / app.js v76 / style.css v33）

**実機検証で確認できたこと（2026-06-29）：**
- フィルター誤無効化なし（iPad/iPhone/PCで正常）、ホスト名ラベル表示OK、タブレット縦2列OK ＝ セッション14のC/F等は効いている
- 一方で、以下の不具合が判明し修正した

**E：PCのチャットをオーバーレイ → 横並びドッキング化（真のZoom挙動）**
- セッション14ではチャットを `position:absolute` のオーバーレイにしたため、開くと**右端の参加者の映像が隠れる**問題があった
- `@media (min-width:769px)` でチャットを flex の横並びに変更（`width:320px`／`.collapsed` で `width:0`）。開くと映像エリアが縮んで重ならない／閉じると全幅
- 併せて、映像タイルの `aspect-ratio:16/9` 固定が原因で**名前ラベル(下端)が見切れる**問題も解消

**D：未読バッジの数字見切れを解消**
- 原因は `.control-btn` の `overflow:hidden`（丸ボタンのクリップ）が、ボタン外側に出る `.chat-toggle-badge` を切っていたこと（セッション14のアイコン縮小では直っていなかった）
- `.control-btn.chat-btn { overflow: visible }` を追加（詳細度を上げて後勝ちを解消）。失敗していた縮小ハックは撤去

**チャットの重複送信（IME）を解消**
- 日本語入力の**変換確定Enter**でも送信が発火し、古い端末でテキストが残り再Enterで重複送信していた
- keydownで `!e.isComposing && e.keyCode !== 229` をガード（チャット欄＋名前編集の両方）

**#1：PC映像のアスペクト比を常に16:9で一定化**
- Eで導入した「セルを埋める」方式の副作用で、チャット開閉や人数変化でカメラの切り取り(アスペクト比)が変わっていた
- 純CSSでは「縦横どちらにも収まる最大の16:9」を表現できないため、`relayoutVideoGrid()`(JS)で列数とタイル幅を算出（Zoom方式）。`ResizeObserver` でチャット開閉アニメ・リサイズに追従。モバイル/プレゼンターモードはCSSに委譲
- プレビュー実測：1/2/3/6人 × チャット開閉、全パターンで 1.778(=16:9) 固定を確認

**#2：通話開始時のチャット初期状態を画面幅で分岐**
- PC・横向きiPad(≥769px)は**開いた状態**、モバイル(≤768px)は閉じた状態で開始（`showCallScreen`）

**診断用ページ追加：`diag.html`**
- iPad2のフィルター誤判定を詰めるため、URLを開くだけでGPU情報（WebGL2/renderer/MAX_TEXTURE_SIZE/コア数等）と現状判定を表示する診断ページを追加
- **iPad2の診断手順**：iPad2のSafariで `https://iitsubu-cloud.github.io/comchat/diag.html` を開き、表示された値（特に WebGL2 / GPU renderer / MAX_TEXTURE_SIZE）を伝える → それを基に `canUseBgFilter()` の判定条件を強化する

---

## 🔧 セッション14で実施した変更

### セッション13修正の実機テスト報告 → 不具合6件を修正（コミット 52fd212 / app.js v73 / style.css v30）

**背景：** セッション13の修正（v3.3）を 2011 MacBook Air / iPad2 / iPad横向き / PC(M4 MBA) で実機テストした結果、以下が判明：
- 旧端末でフィルター無効化メッセージがプリコールダイアログの裏に出て読めない
- iPad2 でフィルターガードが全く効かず、全体ボケ・人物消え・ダイアログクラッシュが発生
- iPad横向きでホスト名ラベルが表示されない／iPad縦向きで未読バッジ数字が見切れる
- PC でチャットを畳むと映像アスペクト比が崩れ下が切れる
- 普通のPC/スマホでは背景フィルターは正常（誤無効化なし）を確認

**A+B：iPad2/旧端末のフィルター判定強化＆プリコール内メッセージ表示**
- `canUseBgFilter()` を強化：WebGL2非対応で即 `false`（iPad2はWebGL2非対応）、デバッグ情報非公開時は `MAX_TEXTURE_SIZE < 2048` で旧GPUを弾く
- `precallFilterBtn` クリック時に `canUseBgFilter()` を早期チェックし、非対応ならフィルターパネル自体を開かない（ダイアログクラッシュ防止）
- `showPrecallStatus()` を新設：プリコール中はダイアログ内に黄色の警告メッセージを3秒表示（裏に隠れて読めない問題を解消）。フィルターパネル/`applyBgFilter` のガードもプリコール中はこちらを使う

**C：ホスト名ラベルが表示されない**
- `.video-label` に `z-index: 5` を追加（他要素の重なりで隠れていた）

**D：未読バッジ数字の見切れ**
- 未読バッジ表示中は `.chat-btn` に `has-unread` クラスを付与し、チャットアイコン(svg)を `scale(0.82)` に縮小してバッジが枠内に収まるよう調整（`updateUnreadBadge`/`clearUnreadBadge` でクラス開閉）

**E：PCでチャットを畳むと映像アスペクト比が崩れる → オーバーレイ化（Zoomスタイル）**
- 従来の「右カラム方式（畳むと映像が横に広がる）」を廃止
- `@media (min-width: 769px)` でチャットを `position: absolute` のオーバーレイに変更。映像エリアは常に全幅固定、`.collapsed` で `translateX(100%)` して右外へスライドアウト
- `.call-layout` に `position: relative` を付与、`gap` を0に。閉じている間は `pointer-events: none`

**F：タブレット縦向きで映像を横2列に**
- モバイル(≤768px)の `.video-grid` を `grid-template-columns: 1fr`（1列固定）から `repeat(auto-fit, minmax(260px, 1fr))` に変更。タブレット縦向き(768px幅)で2列、スマホ幅では実質1列

**検証：** Node構文チェック通過。**実機検証は未実施**（次回セッションで要確認）。

**次回への申し送り（このセッションでの判断）：** 全コードのバグ調査は、今回の6件が実機で効くか確認してから新鮮なコンテキストで行う方が、切り分け・優先順位付けがしやすいと判断し次回に持ち越し。

---

## 🔧 セッション13で実施した変更

### 旧端末の実機検証で判明した不具合に対応（app.js v72 / style.css v29）

**背景：** 2011 MacBook Air（macOS 10.13.6 / Intel HD Graphics 3000）と iPad2 で実機テストした結果、
- 2011 Mac：背景ぼかし/画像を設定するとカメラが真っ黒になりハング（相手側は直前フレームでフリーズ）
- iPad2：ぼかしで全体がボケる・画像で人物が消える

**原因：** MediaPipe の人物セグメンテーションが旧GPUに非対応。GPU(WebGL2)が使えず CPU フォールバックに落ちるが、`segmentForVideo()` が**同期処理でメインスレッドをブロック**するため固まる（iPad2はマスク破綻で人物が背景扱い）。ネイティブアプリのZoomが動くのは、Metal直叩き＋裏スレッド推論＋専用軽量モデルのため。ブラウザ製のComChatでは同等品質は非現実的と判断。

**① 非対応端末では背景フィルターを無効化（方針B）**
- `canUseBgFilter()` を新設。WebGL2非対応／ソフトウェアレンダラ（SwiftShader等）／旧Intel GPU（HD Graphics 2000・3000、GMA、X3100 等）を判定（結果はメモ化）
- 非対応端末では起動を中止し「この端末では背景フィルターを使えません」を表示
- ぼかし・画像の**両入口**（フィルターパネルのクリック／画像プリセット経由の `applyBgFilter`）にガードを設置

**② PC/タブレット横向きでチャット欄を開閉可能に**
- 従来PCはチャット常時表示・トグルボタン非表示で、押しても切り替わらなかった（`.chat-container.open` のスタイルがモバイル用メディアクエリ内にしか無かった）
- モバイル（ボトムシート＝`.open`で表示）とPC/タブレット（右カラム＝`.collapsed`で非表示）で開閉の極性が逆なため、`isChatCurrentlyOpen()` で判定を分離
- `.ctrl-item-chat` を全幅で表示。`@media (min-width: 769px)` に `.chat-container.collapsed { width:0 }` を追加（閉じると映像が広がる）
- タブレット幅（769〜1024px）は右カラムを260pxに

**検証（ローカルプレビュー）：** PC(1280px)＝既定表示300px・トグルで畳む/再表示、タブレット横(900px)＝横並び260px、モバイル(≤768px)＝ボトムシート維持、をすべて確認。
**未検証（実機必須）：** ①②のメッセージ表示は実機（2011 Mac / iPad2）で、③のiPad横向き横並びはSafariサイドバーを閉じた状態で要確認。

---

## 🔧 セッション12で実施した変更

### コード全体のバグ調査（app.js 2138行 + index.html + CSS）
- 構文チェック・HTML/JSのID整合性・XSS（textContent使用で安全）を確認
- **致命的バグは無し**。エッジケースの軽微な不具合3件と設計上の弱点1件を発見

### 軽微バグ修正3件（コミット 5f068c9 / app.js v71）

**① 0バイトファイル送信が受信側で誤って「チャンク欠損」エラー**
- 原因：空チャンクのbase64が `''`（falsy）になり、受信判定 `if (!chunks[i])` が欠損と誤検知
- 修正：`=== undefined` 判定に変更（handleDataMessageの受信カウントとfile-done再構成の2箇所）

**② signaling切断時の再接続リトライが多重起動しうる**
- 原因：`peer.on('disconnected')` が複数回発火すると `attemptReconnect` 連鎖が並行起動
- 修正：`isReconnecting` フラグでガード（constructor/initializePeer/hangupでリセット）

**③ プリコール画面の loadeddata リスナー蓄積**
- 原因：映像読込前にキャンセルすると once未発火のリスナーが残留、開閉の繰り返しで蓄積
- 修正：ハンドラ参照を `_precallLoadedHandler` に保持し、cancel/confirm時に除去

### 設計上の弱点（今回は対応せず・方法0で現状維持と決定）
- **ホストが退出すると新規参加できなくなる**（ルームID＝ホストのPeer IDのため）
- ホストの再入室・新規参加復活もできない（IDを再利用する仕組みが無い）
- 「ホストが途中で落ちる場面はあまり無い」ため、現状維持を選択。
  将来必要になれば「同じID再利用（軽量）」「ホスト移譲」「ルームID分離（要外部DB）」の順で検討

### セキュリティ対応（コード外）
- git設定の remote URL に平文で埋め込まれていたGitHubトークンを除去
- 新トークンを発行し macOSキーチェーンに保存、古いトークンは失効済みを確認

---

## 🔧 セッション11で実施した変更

### iPhone実機での「ルーム参加できない」問題を3段階で解消

**① スマートクォート／自動大文字化によるルームID汚染（app.js v67）**
- 原因：iOS Safari がルームID入力にスマート引用符・自動大文字化を適用し、実際のIDと不一致
- 修正：入力欄に `autocapitalize="off" autocorrect="off" autocomplete="off" spellcheck="false"` を付与し、`joinRoom` で `.toLowerCase().replace(/[^0-9a-z]/g, '')` にサニタイズ

**② 失敗原因が分からない問題 → 診断強化（app.js v69）**
- `initializePeer` に10秒タイムアウトを追加（シグナリング接続が滞ってもハングせずエラー表示）
- `describeMediaError()` を新設し、カメラ/マイク失敗時に原因別メッセージを表示（権限拒否／HTTPS必須／他アプリ使用中／不明エラー）。`getUserMedia` と `showPreCallDialog` の両方に適用
- これにより「カメラでもネットワークでもない」ことが切り分けられ、真因（③）が判明

**③ 真因：参加ボタンがキーボードに隠れて押せない（app.js v70）**
- 原因：ルームID入力後、「参加する」確認ボタンがソフトキーボードの背後に隠れて見えず、誤って「ルーム参加」ボタンを押していた
- 修正：入力欄＋参加ボタンを `<form>` 化（`enterkeyhint="go"`）。キーボードのEnter/Goキーでそのまま参加可能に。入力欄フォーカス時に参加ボタンを画面内へスクロール。click/keydown の二重ハンドラを `form submit` に統一

### モバイルレイアウト刷新（案A・CSS v28）
- モバイル（≤768px）で映像を主役にし、チャットを下からスライドするボトムシート化
- 通話画面に「チャット」トグルボタン＋未読バッジを追加、チャットヘッダーに閉じる（✕）ボタン
- video-grid がスクロール可能なフルハイト構成に

### 補足
- 公開サイト（GitHub Pages）での実機テストには毎回 commit & push でのデプロイが必要（ローカル修正は反映されない）

---

## 🔧 セッション10で実施した変更

### 先送り改善2件を実装（app.js v66）

**① シグナリングサーバ切断時の自動再接続**
- `peer.on('disconnected')` ハンドラを追加し、`attemptReconnect(0)` を起動
- `attemptReconnect(n)`：`peer.reconnect()` を最大5回、バックオフ `2000 * (n+1)` ms でリトライ
- 意図的な退室と切断を区別するため `isLeaving` フラグを追加（constructor / `initializePeer` で初期化、`hangup` でセット）
- 再接続成功・peer破棄・退室中はリトライを停止

**② 画面共有の音声共有（タブ/システム音声を相手に届ける）**
- `getOutgoingStream()` ヘルパーを新設：発信ストリームに必ず音声トラック（ミックス or マイク）を付与
  - 副次的バグ修正：背景フィルター有効時の参加/着信で canvas captureStream（音声なし）を送ってしまいマイク音声が欠落していた問題を解消
  - `connectToPeer` と `handleIncomingCall` の両方を本ヘルパー経由に統一
- `shareScreen`：画面共有に音声トラックがある場合、`buildMixedAudioTrack()` が Web Audio API（`AudioContext` → `MediaStreamDestination`）でマイク＋画面音声をミックスし、既存通話の音声senderへ `replaceTrack`（WebRTC再ネゴ不要）
- `stopScreenShare`：マイク単独トラックへ復元し、`teardownMixedAudio()` で AudioContext をクリーンアップ
- `hangup`：安全策として `teardownMixedAudio()` を実行

### 未検証事項
- 画面共有の音声ミックスは2人目のピアがいないと完全な疎通確認ができないため、ブラウザでの単体動作確認のみ実施

---

## 🔧 セッション9で実施した変更

### バグ修正5件（コミット d15399b / app.js v65）

**① 自分の送信メッセージで未読バッジが増える**
- 原因：`sendMessage` → `displayChatMessage` がチャット非表示時に自分のメッセージも未読カウント
- 修正：`displayChatMessage(username, message, isOwn)` に `isOwn` 引数を追加し、自分のメッセージはバッジ加算をスキップ

**② リモート画面共有の閲覧時に共有者の音声が二重再生（エコー）**
- 原因：プレゼンターモードでもグリッドのサムネイル動画が音声を再生し続けるため、同じストリームを大画面 `screen-share-video` に流すと二重再生
- 修正：`enterRemotePresenterMode` で `screen-share-video` を `muted=true`（音声はグリッド側から再生され欠落なし）

**③ `shareScreen` の `replaceTrack` に `.catch()` 欠落**
- 原因：失敗時に未処理 Promise 拒否 → グローバルエラーハンドラが誤ったエラートーストを出す恐れ
- 修正：他箇所と同様 `.catch(() => {})` を追加

**④ 退室後に `isChatVisible` 未リセット**
- 原因：再入室直後に届いたメッセージで誤って未読バッジが出る可能性
- 修正：`hangup` で `isChatVisible = true` にリセット

**⑤ 7人目が満員ルームに無言で入り壊れた状態になる**
- 原因：定員超過時にホストが接続を即 `close()` するだけで、参加者は「ホスト切断」と区別できず通話画面に遷移したまま誰とも繋がらない
- 修正：ホストが `room-full` メッセージを通知してから切断し、参加者は「ルームは満員です（最大6人）」エラーを表示してウェルカム画面へ戻る

### 先送りした改善候補（→ セッション10で両方実装済み）
- ~~**シグナリングサーバ切断時の自動再接続**~~ → セッション10で実装
- ~~**画面共有の音声共有**~~ → セッション10で実装

---

## 🔧 セッション8で実施した変更

### バグ修正2件

**① joinRoom失敗時にボタンが永久に無効化されるバグ**
- 原因：`joinRoom` の catch ブロックで `confirmJoinBtn` のみ再有効化し、`createRoomBtn`・`joinRoomBtn` を忘れていた
- 修正：catch ブロックに3ボタンすべての `disabled = false` を追加

**② 接続切断時に `cameraStates` がクリアされないバグ**
- 原因：`conn.on('close')` と `conn.on('error')` で `muteStates.delete()` はしていたが `cameraStates.delete()` がなかった
- 修正：両ハンドラーに `this.cameraStates.delete(conn.peer)` を追加

### モバイルUIの大幅改善（iPhone SE3 実機確認済み）

**① ウェルカム画面のボタンはみ出し修正**
- `@media (max-width: 480px)` の inline スタイルが逆効果（`padding: 15px` 等で要素を拡大していた）
- `.header` → `padding: 6px 12px`、`.main-content` → `padding: 8px 6px`、ロゴフォントオーバーライド削除

**② 通話画面のスクロール対応（CSS :has() セレクター活用）**
- 通話中のみ `.container` を `height: auto; min-height: 100svh; overflow: visible` に切り替え
- `#call-screen`・`.call-layout` も `flex: none; overflow: visible` に変更
- PCレイアウトには影響なし

**③ カメラ・チャットの高さ最適化**
- カメラ（`.video-grid`）: `max-height: 200px`（顔＋上半身が自然に見える高さ）
- チャット（`.chat-container`）: `height: 380px` 固定（約13行表示）

**④ 新着メッセージ バッジ通知**
- `IntersectionObserver (threshold: 1.0)` でチャット欄の完全表示を監視
- 非完全表示時に新メッセージ → チャットヘッダーに赤丸数字バッジ表示
- チャット欄が完全に画面に入ったとき → バッジ自動クリア
- 99件超は「99+」表示

---

## 📊 現在のGit状態

- **ブランチ**: main
- **最新安定タグ**: **v4.2-stable**（カメラオフ固着修正＋ホスト退出でルーム終了・実機確認済み・2026-07-04 確定）。タグ以降の未タグコミットなし（docsコミットのみ）
- **JSバージョン**: app.js?v=99（v99=カメラオフガード(セグメンテーション遅延着弾レース)・camera-offクラス切替・ホスト切断検知でルーム終了）**実機確認OK**
- **CSSバージョン**: style.css?v=45（v45=.video-container.camera-off .video-element{visibility:hidden}追加）**実機確認OK**
- **診断ページ**: diag.html（端末の値取得用）のみ。`?griddbg=1` は撤去済み
- **解決済み**: iPad Air2縦の映像グリッド列数固着。**Safari15系はCSS Gridの列トラック変更を子タイルの再配置に反映しない描画バグがあり、`:has()`/JSの`grid-template-columns`/`display:none→''`のいずれでも解消しなかった**。→ モバイル(≤768px)は Grid をやめ **flexbox＋各タイルwidth指定(1〜2人=100%/3人以上=calc(50% - 4px))** に転換。さらに Safari15が `align-content:start` を認識せず stretch に落ちてタイルが縦伸びする副作用を `align-content/align-items:flex-start` ＋ `.video-container{height:auto}` で解消。PC(≥769px)は従来のgridロジックのまま無変更。全端末実機OK

---

## Git タグ（セーブポイント）

- `v1.0-stable` — MVP（ビデオ通話・チャット・画面共有）
- `v1.1-stable` — ファイル共有 200MB・バグ修正
- `v1.2-stable` — 背景フィルター機能
- `v1.3-stable` — 10件バグ修正
- `v1.4-stable` — 背景フィルターエッジ品質改善（マスクフェザリング）
- `v1.5-stable` — バグ修正7件
- `v1.6-stable` — バグ修正2件 + UI変更 + Safariぼかし改善
- `v1.7-stable` — 背景画像機能追加・バグ修正4件
- `v1.8-stable` — プリコールダイアログ
- `v1.9-stable` — 背景画像アップロード履歴3件保持
- `v2.0-stable` — カメラオフ時に中央ユーザー名表示
- `v2.1-stable` — 背景セグメント閾値調整（sigmoid中心128→150）
- `v2.2-stable` — 6件バグ修正
- `v2.3-stable` — 初期画面ヒーローデザイン刷新・Klee Oneフォント・ボタン横並び
- `v2.4-stable` — バグ修正5件（フィルターループ残留・リソースリーク等）
- `v2.5-stable` — モバイル表示改善 第1弾
- `v2.6-stable` — モバイル表示改善 第2弾
- `v2.7-stable` — バグ修正3件（プリコール・エラーハンドラー・ファイル進捗）
- `v2.8-stable` — モバイルレイアウト改善（チャット拡大・ボタンはみ出し修正）
- `v2.9-stable` — バグ修正2件＋スクロール対応通話画面＋バッジ通知
- `v3.0-stable` — 先送り改善2件（シグナリング自動再接続・画面共有の音声共有）
- `v3.1-stable` — iPhone参加問題3段階解消＋モバイルレイアウト案A
- `v3.2-stable` — 軽微バグ修正3件（0バイトファイル誤検知・再接続多重起動・プリコールリスナー蓄積）
- `v3.3-stable` — 旧端末対応（非対応GPUでフィルター無効化）＋PC/タブレット横向きのチャット開閉
- `v3.4-stable` — Air2等のフィルター破綻を実行時検知して自動オフ（PC映像16:9固定・チャット既定オープン等のセッション15分も内包）
- `v3.5-stable` — 参加フローのバグ2件修正（存在しないルーム参加で取り残される／失敗後の同ID再参加で入室できる）＋死んだhas-unread除去
- `v3.6-stable` — チャット送信ボタン見切れ修正（iPad Air2横向き等・style.css v34）実機OK
- `v3.7-stable` — モバイル映像グリッドを Grid→flexbox に転換し iPad Air2縦の列数固着を解決＋16:9維持（app.js v91 / style.css v38）全端末実機OK
- `v3.8-stable` — バグ調査所見①②修正（presenter切替relayout・:has()→in-callクラス化）＋タイトル変更＋ルームID入力後の参加ボタン誘導UI（app.js v94 / style.css v40）**実機確認済み・2026-07-03 正式確定**
- `v3.9-stable` — バグ調査所見2件（ルームID事前検証・screen-share-stopのsenderIdガード＋共有後勝ち）＋プレゼンターモードのレイアウト刷新（オーバーレイ→flexカラム・コントロールバー復活・サムネイル帯を全環境で横スクロール化）＋iPhoneプリコール誤キャンセル対策（キーボードずれ）（app.js v96 / style.css v41）**全項目実機確認OK・2026-07-03 正式確定**
- `v4.0-stable` — カラーテーマを紫→ターコイズに全面刷新＋発話インジケーター（話者タイルにアンバー枠＋glow・Analyser計測/ヒステリシス/ミュート連動/クリーンアップ）（app.js v97 / style.css v43）**全項目実機確認OK・2026-07-04 正式確定**
- `v4.1-stable` — バグ調査所見5件修正（遅延streamガード・発話検知resumeリトライ・svhのvhフォールバック・console.log/到達不能CSS掃除）（app.js v98 / style.css v44）**実機確認OK・2026-07-04 正式確定**
- `v4.2-stable` — カメラオフのフレーム固着解消（セグメンテーション遅延着弾レースのガード＋camera-offクラスでvideo非表示）＋ホスト退出でルーム終了（ゲスト側切断検知・タブ閉じ/回線断も対象）（app.js v99 / style.css v45）**全項目実機確認OK・2026-07-04 正式確定** ← 最新

---

## ⚠️ 既知の制限事項

### 背景フィルターの精度限界
- MediaPipe `selfie_segmenter_landscape` は「人物 vs 背景」の2クラスのみ
- カメラ内に人物の近くにある物体（棒・家具等）が人物と誤分類されることがある
- sigmoid閾値150が品質と精度のベストバランス
- 根本解決は物理的に物体を移動するか、より高精度なモデルへの切り替えのみ

### バッジ通知の動作仕様
- `IntersectionObserver threshold: 1.0` を使用
- モバイルでは通話開始直後（scroll=0）でもチャット末尾が数px隠れるため、バッジが表示される場合がある（正常動作）

---

## 🔄 次回の作業候補

### （完了）実機フィードバック2件修正（コミット 01bb624 → v4.2-stable）の実機確認
2026-07-04 に全項目実機確認OK → **`v4.2-stable` 正式確定**（カメラオフの黒塗り・ホスト退出でルーム終了、いずれも期待どおり動作）

### （完了）セッション24前半（バグ修正5件・コミット 5643f97 → v4.1-stable）の実機確認
2026-07-04 に3項目（基本動作・発話インジケーター・ボトムシート）実機確認OK → **`v4.1-stable` 正式確定**

### （完了）セッション23（ターコイズ化＋発話インジケーター・コミット 7e36fb2）の実機確認
2026-07-04 に全項目実機確認OK → **`v4.0-stable` 正式確定**（HEAD にタグ付与・push 済み）。確認項目：
- [x] **カラーテーマ**: ウェルカム画面・参加ボタン誘導UI・通話画面・チャット等の色味OK
- [x] **発話インジケーター**（2台以上）: 話者タイルにアンバー枠が出る・自分のタイルも点灯
- [x] ミュート中は点灯しない／ミュート解除で点灯
- [x] 画面共有中のサムネイルでも枠が出る／自分の共有音声で誤点灯しない
- [x] iPad Air2 で負荷問題なし・退室→再入室の繰り返しもOK（AudioContextリークなし）
- 補足：しきい値調整が今後必要になれば app.js の静的ゲッター4つ（`SPEAK_ON_RMS`/`SPEAK_OFF_RMS`/`SPEAK_HOLD_MS`/`SPEAK_INTERVAL_MS`）で集約調整可能

### （完了）セッション22修正（app.js v96 / style.css v41 → v3.9-stable）の実機確認
2026-07-03 に全端末（iPhone/iPad/PC）で実機確認完了・**`v3.9-stable` 正式確定**。確認項目：
- [x] **iPhoneプリコール**: カメラ/マイク/背景ボタンが正常に反応（ウェルカムに戻らない・ボタンが死なない）
- [x] **プレゼンターモードレイアウト**: 「サムネイル帯（横スクロール可）→共有画面→コントロールバー」の縦並び。全環境でコントロールバーが見える・押せる／横向き・PCで被らない／サムネイルは全環境で横スクロール可（プレビューで6人1行・scrollW>clientWを実測）
- [x] **所見①**: 全角ルームID弾き＋正常系OK
- [x] **所見②**（共有の後勝ち）: プレビューで単体検証OK（実機3台での重畳テストは未実施だが挙動は確認済み）

### （完了）セッション21変更（v92〜v94・v3.8-stable）の実機確認
2026-07-03 に iPhone で実機確認完了。①タブタイトル②参加ボタン誘導UI③通話画面レイアウト（in-call化）④画面共有、すべてOK。**`v3.8-stable` 正式確定**。

### （完了）セッション20 flex化修正の iPad Air2 実機最終確認
セッション20で **モバイルを Grid→flexbox に転換**して列数固着を修正済み(app.js v90 / css v37・デプロイ済み)。**iPad Air2（縦）で最終確認**する：
- 3人=**2列**、1〜2人=**1列**になるか。**人数を動的に増減**して列数が追従するか（回転させなくても切り替わるか）
- 念のため `?griddbg=1` で `display=flex` / `tileW(computed)` が 3人時に約半幅(例 178px前後)になっているか確認
- **OKなら**：`?griddbg=1` オーバーレイと関連コード(`_updateGridDebug` 呼び出し2箇所＋メソッド定義)を撤去 → app.js/cssのバージョンを上げてデプロイ → **`v3.7-stable` 付与**（`git tag -a v3.7-stable -m "..." && git push origin v3.7-stable`）
- **万一まだ固着する場合**（可能性は低いが）：`?griddbg=1` の `tileW(computed)` を確認。半幅なのに画面が1列＝flexでも描画バグ→タイル差し替え等。半幅でない＝width計算/呼び出しの問題
- 既に効かなかった手法（重複回避）：`:has()`(css) / JSの`grid-template-columns` / `void offsetHeight`(v87) / `display:none→''`(v88)。**いずれも Grid 前提だったため flex 化で回避**
- 参考：SE3・M4 MBA(PC)はセッション19で実機OK。Chromiumプレビューではセッション20の flex 化で 1人=1列/2人=1列/3〜4人=2列、4→3→2の減少でも正しく列数が戻ることを確認済み

### 着手済み（セッション18）— モバイルカメラ2列化
- 合意方針「1〜2人=1列/3人以上=2列」を `relayoutVideoGrid` のモバイル分岐にJS実装済み。CSS `:has()` は撤去（Safari15系の動的再評価バグ回避）。SE3では実機OK、**Air2縦のみ描画反映されず未解決（上記参照）**

### Air2のフィルター破綻検知 → 完了（v3.4-stable）
- 実行時検知で自動オフ、Air2実機で確認済み。デバッグHUDも撤去済み
- 閾値は `SEG_*` 静的ゲッターに集約（警告継続2.5秒/人物0.5%/健全8%/ウォームアップ600ms）

### 任意：残った確認項目（実機があれば）
- [ ] 高機能端末でフィルターON維持のまま席外し→戻りで誤オフされないこと（healthySeenで保護される想定）
- [ ] iPad横向きのホスト名ラベル（セッション15からの保留・縦向きはOK）
- [ ] Air2でクラッシュ（全タイル黒）が再発しないか（早期オフで負荷を断つ狙い）

### 保留：iPad横向きのホスト名ラベル確認（iPad実機が必要）
- セッション15では確認できず。縦向き(③)ではラベル表示OKなので、横向き固有で消えるかだけ要確認

### セッション15修正の実機確認（M4 MBA・横向きiPad）
- [ ] チャット開閉・人数変化でカメラの映り(アスペクト比)が変わらない（16:9一定）
- [ ] PC・横向きiPadは入室時にチャットが開いた状態
- [ ] 問題なく、かつiPad2系も片付いたら v3.4-stable タグを付与

### その後：全コードのバグ調査

### Phase 2 機能
- [ ] リアクション機能（Periscopeスタイルのハート）
- [ ] ブレイクアウトルーム
- [ ] ホワイトボード共有
- [ ] 録画機能

---

**次回セッション開始時はこのファイルを確認してから作業を再開してください！**
